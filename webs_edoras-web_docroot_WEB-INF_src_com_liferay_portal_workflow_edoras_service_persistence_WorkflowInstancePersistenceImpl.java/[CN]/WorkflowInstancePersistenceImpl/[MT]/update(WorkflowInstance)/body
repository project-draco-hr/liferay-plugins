{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(WorkflowInstance workflowInstance) method. Use update(WorkflowInstance workflowInstance, boolean merge) instead.");
  }
  return update(workflowInstance,false);
}
