{
  if (isEscapedModel()) {
    return (SVNRepository)this;
  }
 else {
    return (SVNRepository)Proxy.newProxyInstance(_classLoader,_escapedModelProxyInterfaces,new AutoEscapeBeanHandler(this));
  }
}
