{
  String[] actorIds=null;
  if ((userId != null) && (userId > 0)) {
    actorIds=new String[]{String.valueOf(userId)};
  }
  return getWorkflowTasks(workflowInstanceId,actorIds,false,completed,start,end,orderByComparator);
}
