{
  Type type=fetchByPrimaryKey(typeId);
  if (type == null) {
    if (_log.isWarnEnabled()) {
      _log.warn(_NO_SUCH_ENTITY_WITH_PRIMARY_KEY + typeId);
    }
    throw new NoSuchTypeException(_NO_SUCH_ENTITY_WITH_PRIMARY_KEY + typeId);
  }
  return type;
}
