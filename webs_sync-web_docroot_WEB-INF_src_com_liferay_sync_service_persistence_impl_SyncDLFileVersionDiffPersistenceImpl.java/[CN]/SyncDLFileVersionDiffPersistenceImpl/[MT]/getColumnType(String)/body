{
  Integer type=SyncDLFileVersionDiffModelImpl.TABLE_COLUMNS_MAP.get(columnName);
  if (type == null) {
    throw new IllegalArgumentException("Unknown column name " + columnName + " for table "+ SyncDLFileVersionDiffModelImpl.TABLE_NAME);
  }
  return type;
}
