{
  for (  JIRAChangeItem jiraChangeItem : jiraChangeItems) {
    if (EntityCacheUtil.getResult(JIRAChangeItemModelImpl.ENTITY_CACHE_ENABLED,JIRAChangeItemImpl.class,jiraChangeItem.getPrimaryKey()) == null) {
      cacheResult(jiraChangeItem);
    }
 else {
      jiraChangeItem.resetOriginalValues();
    }
  }
}
