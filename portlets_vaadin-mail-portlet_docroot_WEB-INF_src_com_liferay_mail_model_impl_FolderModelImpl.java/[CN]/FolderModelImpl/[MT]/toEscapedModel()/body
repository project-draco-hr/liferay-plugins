{
  if (isEscapedModel()) {
    return (Folder)this;
  }
 else {
    return (Folder)Proxy.newProxyInstance(Folder.class.getClassLoader(),new Class[]{Folder.class},new AutoEscapeBeanHandler(this));
  }
}
