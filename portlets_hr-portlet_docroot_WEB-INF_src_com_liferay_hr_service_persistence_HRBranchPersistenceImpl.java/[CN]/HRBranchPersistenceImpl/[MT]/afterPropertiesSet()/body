{
  String[] listenerClassNames=StringUtil.split(GetterUtil.getString(com.liferay.util.service.ServiceProps.get("value.object.listener.com.liferay.hr.model.HRBranch")));
  if (listenerClassNames.length > 0) {
    try {
      List<ModelListener<HRBranch>> listenersList=new ArrayList<ModelListener<HRBranch>>();
      for (      String listenerClassName : listenerClassNames) {
        listenersList.add((ModelListener<HRBranch>)InstanceFactory.newInstance(listenerClassName));
      }
      listeners=listenersList.toArray(new ModelListener[listenersList.size()]);
    }
 catch (    Exception e) {
      _log.error(e);
    }
  }
  containsHRJobTitle=new ContainsHRJobTitle(this);
  addHRJobTitle=new AddHRJobTitle(this);
  clearHRJobTitles=new ClearHRJobTitles(this);
  removeHRJobTitle=new RemoveHRJobTitle(this);
}
