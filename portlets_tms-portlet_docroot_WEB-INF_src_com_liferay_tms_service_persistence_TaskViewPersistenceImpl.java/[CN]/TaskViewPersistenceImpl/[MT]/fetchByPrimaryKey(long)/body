{
  Session session=null;
  try {
    session=openSession();
    return (TaskView)session.get(TaskViewImpl.class,new Long(taskViewId));
  }
 catch (  Exception e) {
    throw processException(e);
  }
 finally {
    closeSession(session);
  }
}
