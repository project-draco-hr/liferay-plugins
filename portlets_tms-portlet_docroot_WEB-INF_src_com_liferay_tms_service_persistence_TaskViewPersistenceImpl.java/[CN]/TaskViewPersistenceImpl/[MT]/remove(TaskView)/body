{
  for (  ModelListener listener : listeners) {
    listener.onBeforeRemove(taskView);
  }
  taskView=removeImpl(taskView);
  for (  ModelListener listener : listeners) {
    listener.onAfterRemove(taskView);
  }
  return taskView;
}
