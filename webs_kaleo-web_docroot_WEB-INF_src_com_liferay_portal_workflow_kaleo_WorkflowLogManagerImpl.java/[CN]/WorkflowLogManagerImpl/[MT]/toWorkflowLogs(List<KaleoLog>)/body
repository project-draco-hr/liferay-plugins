{
  List<WorkflowLog> workflowLogs=new ArrayList<WorkflowLog>(kaleoLogs.size());
  for (  KaleoLog kaleoLog : kaleoLogs) {
    workflowLogs.add(WorkflowModelConverterUtil.toWorkflowLog(kaleoLog));
  }
  return workflowLogs;
}
