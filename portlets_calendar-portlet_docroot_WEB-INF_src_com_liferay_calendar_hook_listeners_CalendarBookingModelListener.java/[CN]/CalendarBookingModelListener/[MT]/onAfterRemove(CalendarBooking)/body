{
  CalEvent calEvent=CalEventLocalServiceUtil.fetchCalEventByUuidAndGroupId(calendarBooking.getUuid(),calendarBooking.getGroupId());
  if (Validator.isNotNull(calEvent)) {
    CalEventLocalServiceUtil.deleteCalEvent(calEvent);
  }
}
