{
  Session session=null;
  try {
    session=openSession();
    return (JIRAChangeItem)session.get(JIRAChangeItemImpl.class,new Long(jiraChangeItemId));
  }
 catch (  Exception e) {
    throw processException(e);
  }
 finally {
    closeSession(session);
  }
}
