{
  Object[] finderArgs=new Object[]{new Long(jiraChangeGroupId)};
  Long count=(Long)FinderCacheUtil.getResult(FINDER_PATH_COUNT_BY_JIRACHANGEGROUPID,finderArgs,this);
  if (count == null) {
    Session session=null;
    try {
      session=openSession();
      StringBuilder query=new StringBuilder();
      query.append("SELECT COUNT(jiraChangeItem) ");
      query.append("FROM JIRAChangeItem jiraChangeItem WHERE ");
      query.append("jiraChangeItem.jiraChangeGroupId = ?");
      query.append(" ");
      Query q=session.createQuery(query.toString());
      QueryPos qPos=QueryPos.getInstance(q);
      qPos.add(jiraChangeGroupId);
      count=(Long)q.uniqueResult();
    }
 catch (    Exception e) {
      throw processException(e);
    }
 finally {
      if (count == null) {
        count=Long.valueOf(0);
      }
      FinderCacheUtil.putResult(FINDER_PATH_COUNT_BY_JIRACHANGEGROUPID,finderArgs,count);
      closeSession(session);
    }
  }
  return count.intValue();
}
