{
  int kbCommentsCount=0;
  if (AdminPermission.contains(getPermissionChecker(),groupId,ActionKeys.VIEW_KB_SUGGESTIONS)) {
    kbCommentsCount=kbCommentPersistence.countByG_S(groupId,status);
  }
  return kbCommentsCount;
}
