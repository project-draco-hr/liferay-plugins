{
  List<KBComment> kbComments=new ArrayList<KBComment>();
  if (AdminPermission.contains(getPermissionChecker(),groupId,ActionKeys.VIEW_KB_FEEDBACK)) {
    kbComments=kbCommentPersistence.findByG_S(groupId,status,start,end);
  }
  return kbComments;
}
