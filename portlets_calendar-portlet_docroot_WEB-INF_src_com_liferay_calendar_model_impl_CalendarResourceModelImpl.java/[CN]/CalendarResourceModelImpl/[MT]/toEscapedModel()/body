{
  if (isEscapedModel()) {
    return (CalendarResource)this;
  }
 else {
    return (CalendarResource)Proxy.newProxyInstance(CalendarResource.class.getClassLoader(),new Class[]{CalendarResource.class},new AutoEscapeBeanHandler(this));
  }
}
