{
  if (isEscapedModel()) {
    return (CalendarResource)this;
  }
 else {
    return (CalendarResource)Proxy.newProxyInstance(_classLoader,_escapedModelProxyInterfaces,new AutoEscapeBeanHandler(this));
  }
}
