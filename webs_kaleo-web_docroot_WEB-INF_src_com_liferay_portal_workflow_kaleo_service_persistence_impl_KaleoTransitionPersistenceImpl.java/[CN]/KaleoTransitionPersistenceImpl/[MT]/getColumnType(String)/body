{
  Integer type=KaleoTransitionModelImpl.TABLE_COLUMNS_MAP.get(columnName);
  if (type == null) {
    throw new IllegalArgumentException("Unknown column name " + columnName + " for table "+ KaleoTransitionModelImpl.TABLE_NAME);
  }
  return type;
}
