{
  if (isEscapedModel()) {
    return (HRTaskStatus)this;
  }
 else {
    return (HRTaskStatus)Proxy.newProxyInstance(HRTaskStatus.class.getClassLoader(),new Class[]{HRTaskStatus.class},new AutoEscapeBeanHandler(this));
  }
}
