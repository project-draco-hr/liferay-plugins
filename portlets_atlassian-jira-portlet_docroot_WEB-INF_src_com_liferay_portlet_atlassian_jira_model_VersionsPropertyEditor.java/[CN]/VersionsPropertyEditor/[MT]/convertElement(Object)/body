{
  if (element == null) {
    return null;
  }
 else   if (element instanceof Version) {
    return element;
  }
  if (element instanceof String) {
    return Version.convert((String)element);
  }
  throw new IllegalArgumentException("Invalid versions element type: " + element);
}
