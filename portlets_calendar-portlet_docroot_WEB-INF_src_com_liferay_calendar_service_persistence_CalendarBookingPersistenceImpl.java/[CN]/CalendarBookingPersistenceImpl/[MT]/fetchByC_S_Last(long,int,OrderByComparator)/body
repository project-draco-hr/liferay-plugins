{
  int count=countByC_S(calendarResourceId,status);
  if (count == 0) {
    return null;
  }
  List<CalendarBooking> list=findByC_S(calendarResourceId,status,count - 1,count,orderByComparator);
  if (!list.isEmpty()) {
    return list.get(0);
  }
  return null;
}
