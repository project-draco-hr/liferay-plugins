{
  if (calendarBooking.isNew()) {
    Object[] args=new Object[]{calendarBooking.getUuid(),Long.valueOf(calendarBooking.getGroupId())};
    FinderCacheUtil.putResult(FINDER_PATH_COUNT_BY_UUID_G,args,Long.valueOf(1));
    FinderCacheUtil.putResult(FINDER_PATH_FETCH_BY_UUID_G,args,calendarBooking);
    args=new Object[]{Long.valueOf(calendarBooking.getCalendarId()),Long.valueOf(calendarBooking.getParentCalendarBookingId())};
    FinderCacheUtil.putResult(FINDER_PATH_COUNT_BY_C_P,args,Long.valueOf(1));
    FinderCacheUtil.putResult(FINDER_PATH_FETCH_BY_C_P,args,calendarBooking);
  }
 else {
    CalendarBookingModelImpl calendarBookingModelImpl=(CalendarBookingModelImpl)calendarBooking;
    if ((calendarBookingModelImpl.getColumnBitmask() & FINDER_PATH_FETCH_BY_UUID_G.getColumnBitmask()) != 0) {
      Object[] args=new Object[]{calendarBooking.getUuid(),Long.valueOf(calendarBooking.getGroupId())};
      FinderCacheUtil.putResult(FINDER_PATH_COUNT_BY_UUID_G,args,Long.valueOf(1));
      FinderCacheUtil.putResult(FINDER_PATH_FETCH_BY_UUID_G,args,calendarBooking);
    }
    if ((calendarBookingModelImpl.getColumnBitmask() & FINDER_PATH_FETCH_BY_C_P.getColumnBitmask()) != 0) {
      Object[] args=new Object[]{Long.valueOf(calendarBooking.getCalendarId()),Long.valueOf(calendarBooking.getParentCalendarBookingId())};
      FinderCacheUtil.putResult(FINDER_PATH_COUNT_BY_C_P,args,Long.valueOf(1));
      FinderCacheUtil.putResult(FINDER_PATH_FETCH_BY_C_P,args,calendarBooking);
    }
  }
}
