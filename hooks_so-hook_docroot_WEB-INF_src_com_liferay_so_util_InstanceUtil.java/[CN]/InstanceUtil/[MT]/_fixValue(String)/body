{
  if (value.endsWith(_AUTOMATIC_COPY)) {
    value=value.substring(0,value.length() - _AUTOMATIC_COPY.length());
  }
  if (value.endsWith(_AUTOMATIC_TRANSLATION)) {
    value=value.substring(0,value.length() - _AUTOMATIC_TRANSLATION.length());
  }
  return value;
}
