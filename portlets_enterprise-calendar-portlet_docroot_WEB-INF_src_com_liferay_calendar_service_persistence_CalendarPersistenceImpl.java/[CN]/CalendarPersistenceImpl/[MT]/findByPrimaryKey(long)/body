{
  Calendar calendar=fetchByPrimaryKey(calendarId);
  if (calendar == null) {
    if (_log.isWarnEnabled()) {
      _log.warn(_NO_SUCH_ENTITY_WITH_PRIMARY_KEY + calendarId);
    }
    throw new NoSuchCalendarException(_NO_SUCH_ENTITY_WITH_PRIMARY_KEY + calendarId);
  }
  return calendar;
}
