{
  resourceLocalService.addModelResources(calendarEvent.getCompanyId(),calendarEvent.getGroupId(),calendarEvent.getUserId(),CalendarEvent.class.getName(),calendarEvent.getCalendarEventId(),communityPermissions,guestPermissions);
}
