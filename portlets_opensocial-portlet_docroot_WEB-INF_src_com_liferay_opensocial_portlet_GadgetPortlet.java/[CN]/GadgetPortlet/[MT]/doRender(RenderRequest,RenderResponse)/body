{
  Gadget gadget=getGadget();
  GadgetManager gadgetManager=GadgetManagerFactory.getGadgetManager(gadget);
  renderResponse.setContentType(ContentTypes.TEXT_HTML_UTF8);
  String content=getContent(renderRequest,renderResponse,gadgetManager);
  PortletResponseUtil.write(renderResponse,content);
}
