{
  response.setContentType("text/html");
  PrintWriter writer=response.getWriter();
  PortletURL aurl1=response.createActionURL();
  aurl1.setParameter("param1","wildcardMatch");
  writer.println("<a href=\"" + aurl1 + "\">MatchLongestWildcard</a>");
  PortletURL aurl2=response.createActionURL();
  aurl2.setParameter("param2","multipleEvents");
  writer.println("<br><a href=\"" + aurl2 + "\">Send multiple "+ "events from PA()</a>");
  writer.println("<br>Event received due to propogation:" + request.getParameter("key"));
}
