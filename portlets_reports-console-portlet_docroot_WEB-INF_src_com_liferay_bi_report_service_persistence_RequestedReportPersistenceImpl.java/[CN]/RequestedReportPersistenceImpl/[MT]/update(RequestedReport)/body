{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(RequestedReport requestedReport) method. Use update(RequestedReport requestedReport, boolean merge) instead.");
  }
  return update(requestedReport,false);
}
