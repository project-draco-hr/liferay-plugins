{
  if (isEscapedModel()) {
    return (Checkout)this;
  }
 else {
    return (Checkout)Proxy.newProxyInstance(Checkout.class.getClassLoader(),new Class[]{Checkout.class},new AutoEscapeBeanHandler(this));
  }
}
