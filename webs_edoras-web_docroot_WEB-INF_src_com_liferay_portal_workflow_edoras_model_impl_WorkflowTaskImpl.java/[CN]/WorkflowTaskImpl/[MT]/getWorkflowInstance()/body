{
  try {
    return WorkflowInstanceUtil.findByPrimaryKey(getWorkflowInstanceId());
  }
 catch (  Exception e) {
    throw new ProcessException("Could not fetch the workflow instance for task with id [" + getPrimaryKey() + "]",e);
  }
}
