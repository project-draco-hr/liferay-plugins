{
  if (isEscapedModel()) {
    return (MicroblogsEntry)this;
  }
 else {
    return (MicroblogsEntry)Proxy.newProxyInstance(_classLoader,_escapedModelProxyInterfaces,new AutoEscapeBeanHandler(this));
  }
}
