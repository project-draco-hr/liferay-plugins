{
  response.setContentType("text/html");
  PrintWriter writer=response.getWriter();
  PortletURL aurl=response.createActionURL();
  writer.println("<br><a href=\"" + aurl + "\">SetRenderinPE</a>");
  writer.println("<br>Shared Render Param set in PE:" + request.getParameter("id0"));
}
