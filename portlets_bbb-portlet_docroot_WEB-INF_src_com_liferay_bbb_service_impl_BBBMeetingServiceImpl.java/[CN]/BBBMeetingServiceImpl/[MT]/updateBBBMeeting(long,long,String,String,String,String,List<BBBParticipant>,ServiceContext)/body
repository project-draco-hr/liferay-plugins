{
  BBBMeetingPermission.check(getPermissionChecker(),bbbMeetingId,ActionKeys.UPDATE);
  return bbbMeetingLocalService.updateBBBMeeting(bbbMeetingId,bbbServerId,name,description,attendeePassword,moderatorPassword,bbbParticipants,serviceContext);
}
