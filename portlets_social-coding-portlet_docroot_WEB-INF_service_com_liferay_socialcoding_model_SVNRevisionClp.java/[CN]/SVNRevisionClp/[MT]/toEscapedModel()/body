{
  if (isEscapedModel()) {
    return this;
  }
 else {
    return (SVNRevision)Proxy.newProxyInstance(SVNRevision.class.getClassLoader(),new Class[]{SVNRevision.class},new AutoEscapeBeanHandler(this));
  }
}
