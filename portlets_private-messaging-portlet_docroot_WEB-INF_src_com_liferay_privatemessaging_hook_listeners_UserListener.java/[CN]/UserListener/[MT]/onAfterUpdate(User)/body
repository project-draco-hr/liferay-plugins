{
  try {
    if (_log.isDebugEnabled()) {
      _log.debug("Updating private message username for user " + user.getUserId());
    }
    UserThreadLocalServiceUtil.updateUserName(user);
  }
 catch (  Exception e) {
    _log.error("Unable to update private message username for user " + user.getUserId());
  }
}
