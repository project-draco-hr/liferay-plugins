{
  if (isEscapedModel()) {
    return this;
  }
 else {
    return (HRAssetType)Proxy.newProxyInstance(HRAssetType.class.getClassLoader(),new Class[]{HRAssetType.class},new AutoEscapeBeanHandler(this));
  }
}
