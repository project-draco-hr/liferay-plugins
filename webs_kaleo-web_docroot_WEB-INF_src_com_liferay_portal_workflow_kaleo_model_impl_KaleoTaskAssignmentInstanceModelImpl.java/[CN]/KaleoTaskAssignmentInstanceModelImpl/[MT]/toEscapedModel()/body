{
  if (isEscapedModel()) {
    return (KaleoTaskAssignmentInstance)this;
  }
 else {
    return (KaleoTaskAssignmentInstance)Proxy.newProxyInstance(KaleoTaskAssignmentInstance.class.getClassLoader(),new Class[]{KaleoTaskAssignmentInstance.class},new AutoEscapeBeanHandler(this));
  }
}
