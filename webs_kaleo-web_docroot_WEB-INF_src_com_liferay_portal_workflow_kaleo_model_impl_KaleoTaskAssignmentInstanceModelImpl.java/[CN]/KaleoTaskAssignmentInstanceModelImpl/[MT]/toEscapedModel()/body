{
  if (isEscapedModel()) {
    return (KaleoTaskAssignmentInstance)this;
  }
 else {
    return (KaleoTaskAssignmentInstance)Proxy.newProxyInstance(_classLoader,_escapedModelProxyInterfaces,new AutoEscapeBeanHandler(this));
  }
}
