{
  CalendarResource calendarResource=calendarBooking.getCalendarResource();
  return ((userId == calendarEvent.getUserId()) && (userId == calendarResource.getUserId()) && (calendarBooking.getStatus() != CalendarBookingWorkflowConstants.STATUS_DENIED));
}
