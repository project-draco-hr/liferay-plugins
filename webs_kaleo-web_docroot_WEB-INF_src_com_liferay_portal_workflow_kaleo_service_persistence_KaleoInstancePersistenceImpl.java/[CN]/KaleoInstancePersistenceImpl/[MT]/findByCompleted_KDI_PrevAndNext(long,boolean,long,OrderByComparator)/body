{
  KaleoInstance kaleoInstance=findByPrimaryKey(kaleoInstanceId);
  Session session=null;
  try {
    session=openSession();
    KaleoInstance[] array=new KaleoInstanceImpl[3];
    array[0]=getByCompleted_KDI_PrevAndNext(session,kaleoInstance,completed,kaleoDefinitionId,orderByComparator,true);
    array[1]=kaleoInstance;
    array[2]=getByCompleted_KDI_PrevAndNext(session,kaleoInstance,completed,kaleoDefinitionId,orderByComparator,false);
    return array;
  }
 catch (  Exception e) {
    throw processException(e);
  }
 finally {
    closeSession(session);
  }
}
