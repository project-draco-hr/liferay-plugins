{
  if (isEscapedModel()) {
    return (HRAssetProduct)this;
  }
 else {
    return (HRAssetProduct)Proxy.newProxyInstance(_classLoader,_escapedModelProxyInterfaces,new AutoEscapeBeanHandler(this));
  }
}
