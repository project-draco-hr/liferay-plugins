{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(WorkflowTask workflowTask) method. Use update(WorkflowTask workflowTask, boolean merge) instead.");
  }
  return update(workflowTask,false);
}
