{
  if (isEscapedModel()) {
    return (HRTimeOffType)this;
  }
 else {
    return (HRTimeOffType)Proxy.newProxyInstance(_classLoader,_escapedModelProxyInterfaces,new AutoEscapeBeanHandler(this));
  }
}
