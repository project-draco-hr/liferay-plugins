{
  if (isEscapedModel()) {
    return (HRUserProject)this;
  }
 else {
    return (HRUserProject)Proxy.newProxyInstance(_classLoader,_escapedModelProxyInterfaces,new AutoEscapeBeanHandler(this));
  }
}
