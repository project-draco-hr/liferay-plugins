{
  if (isEscapedModel()) {
    return this;
  }
 else {
    return (HRExpense)Proxy.newProxyInstance(HRExpense.class.getClassLoader(),new Class[]{HRExpense.class},new AutoEscapeBeanHandler(this));
  }
}
