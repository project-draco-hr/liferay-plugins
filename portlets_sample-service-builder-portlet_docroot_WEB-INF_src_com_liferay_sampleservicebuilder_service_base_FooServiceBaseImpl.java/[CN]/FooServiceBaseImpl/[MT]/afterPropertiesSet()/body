{
  if (fooLocalService == null) {
    fooLocalService=(FooLocalService)PortletBeanLocatorUtil.locate(FooLocalService.class.getName() + ".impl");
  }
  if (fooPersistence == null) {
    fooPersistence=(FooPersistence)PortletBeanLocatorUtil.locate(FooPersistence.class.getName() + ".impl");
  }
}
