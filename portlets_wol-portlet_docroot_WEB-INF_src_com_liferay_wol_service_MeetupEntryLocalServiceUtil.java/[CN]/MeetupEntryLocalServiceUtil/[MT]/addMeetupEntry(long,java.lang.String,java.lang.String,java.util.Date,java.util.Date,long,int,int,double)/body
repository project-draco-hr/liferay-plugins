{
  MeetupEntryLocalService meetupEntryLocalService=MeetupEntryLocalServiceFactory.getService();
  return meetupEntryLocalService.addMeetupEntry(userId,title,description,startDate,endDate,addressId,totalAttendees,maxAttendees,price);
}
