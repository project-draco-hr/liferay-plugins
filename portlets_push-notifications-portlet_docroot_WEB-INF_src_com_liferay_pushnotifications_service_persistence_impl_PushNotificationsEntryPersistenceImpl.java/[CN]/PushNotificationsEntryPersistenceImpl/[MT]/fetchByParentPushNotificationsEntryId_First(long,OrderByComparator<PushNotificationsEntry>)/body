{
  List<PushNotificationsEntry> list=findByParentPushNotificationsEntryId(parentPushNotificationsEntryId,0,1,orderByComparator);
  if (!list.isEmpty()) {
    return list.get(0);
  }
  return null;
}
