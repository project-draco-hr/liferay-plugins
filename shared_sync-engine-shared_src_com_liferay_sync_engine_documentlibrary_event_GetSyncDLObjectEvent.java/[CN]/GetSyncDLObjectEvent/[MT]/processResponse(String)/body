{
  ObjectMapper objectMapper=new ObjectMapper();
  List<File> files=objectMapper.readValue(response,new TypeReference<List<File>>(){
  }
);
  for (  File file : files) {
    System.out.println(file);
  }
}
