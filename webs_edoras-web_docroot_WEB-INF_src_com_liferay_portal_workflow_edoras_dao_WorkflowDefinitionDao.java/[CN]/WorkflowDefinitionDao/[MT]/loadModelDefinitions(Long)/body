{
  try {
    if (tenantId == null) {
      return WorkflowDefinitionUtil.findAll();
    }
 else {
      return WorkflowDefinitionUtil.findByCompanyId(tenantId.longValue());
    }
  }
 catch (  SystemException se) {
    throw new ProcessException("Could not find workflow definitions",se);
  }
}
