{
  try {
    if (tenantId == null) {
      return WorkflowDefinitionUtil.findAll();
    }
 else {
      return WorkflowDefinitionUtil.findByC(tenantId.longValue());
    }
  }
 catch (  SystemException e) {
    throw new ProcessException("Could not fetch workflow definitions.",e);
  }
}
