{
  try {
    index=dn.size() - 1 - index;
    if (index < dn.size()) {
      Rdn rdn=dn.getRdn(index);
      return rdn.getValue(rdn.getNormType()).toString();
    }
  }
 catch (  Exception e) {
  }
  return null;
}
