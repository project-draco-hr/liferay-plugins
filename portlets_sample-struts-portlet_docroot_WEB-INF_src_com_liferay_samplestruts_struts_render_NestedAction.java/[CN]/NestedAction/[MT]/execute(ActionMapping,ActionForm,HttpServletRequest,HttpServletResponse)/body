{
  NestedForm nestedForm=(NestedForm)actionForm;
  if (_log.isInfoEnabled()) {
    _log.info(nestedForm.toString());
  }
  return actionMapping.findForward("portlet.sample_struts_portlet.nested");
}
