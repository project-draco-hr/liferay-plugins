{
  try {
    _portletPreferences.setValues(key,values);
  }
 catch (  ReadOnlyException roe) {
    _log.error("When preferences are used as the backend for settings they " + "should never be read only",roe);
  }
  return this;
}
