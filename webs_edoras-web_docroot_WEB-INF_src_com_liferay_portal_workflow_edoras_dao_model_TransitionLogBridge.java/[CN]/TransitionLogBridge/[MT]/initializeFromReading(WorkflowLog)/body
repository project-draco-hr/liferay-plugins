{
  _workflowLogDelegate.initializeFromReading(workflowLog,this);
  ProcessModel processModel=getProcessModel();
  Element oldState=processModel.searchElement(workflowLog.getOldState());
  Element newState=processModel.searchElement(workflowLog.getNewState());
  setStates(oldState,newState);
}
