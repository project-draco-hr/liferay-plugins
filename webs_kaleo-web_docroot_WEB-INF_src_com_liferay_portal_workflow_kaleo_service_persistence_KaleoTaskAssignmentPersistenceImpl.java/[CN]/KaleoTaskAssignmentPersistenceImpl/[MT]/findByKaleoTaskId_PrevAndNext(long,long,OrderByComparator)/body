{
  KaleoTaskAssignment kaleoTaskAssignment=findByPrimaryKey(kaleoTaskAssignmentId);
  Session session=null;
  try {
    session=openSession();
    KaleoTaskAssignment[] array=new KaleoTaskAssignmentImpl[3];
    array[0]=getByKaleoTaskId_PrevAndNext(session,kaleoTaskAssignment,kaleoTaskId,orderByComparator,true);
    array[1]=kaleoTaskAssignment;
    array[2]=getByKaleoTaskId_PrevAndNext(session,kaleoTaskAssignment,kaleoTaskId,orderByComparator,false);
    return array;
  }
 catch (  Exception e) {
    throw processException(e);
  }
 finally {
    closeSession(session);
  }
}
