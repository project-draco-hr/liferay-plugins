{
  if (isEscapedModel()) {
    return (HRTimeSheetDay)this;
  }
 else {
    return (HRTimeSheetDay)Proxy.newProxyInstance(_classLoader,_escapedModelProxyInterfaces,new AutoEscapeBeanHandler(this));
  }
}
