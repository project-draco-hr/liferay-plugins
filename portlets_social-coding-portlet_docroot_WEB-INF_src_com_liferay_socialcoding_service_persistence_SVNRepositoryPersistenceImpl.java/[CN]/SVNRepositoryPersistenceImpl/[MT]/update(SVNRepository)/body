{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(SVNRepository svnRepository) method. Use update(SVNRepository svnRepository, boolean merge) instead.");
  }
  return update(svnRepository,false);
}
