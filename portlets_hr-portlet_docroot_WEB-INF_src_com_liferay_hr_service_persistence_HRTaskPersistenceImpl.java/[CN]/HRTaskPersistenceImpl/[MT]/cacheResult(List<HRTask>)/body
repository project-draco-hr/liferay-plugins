{
  for (  HRTask hrTask : hrTasks) {
    if (EntityCacheUtil.getResult(HRTaskModelImpl.ENTITY_CACHE_ENABLED,HRTaskImpl.class,hrTask.getPrimaryKey(),this) == null) {
      cacheResult(hrTask);
    }
  }
}
