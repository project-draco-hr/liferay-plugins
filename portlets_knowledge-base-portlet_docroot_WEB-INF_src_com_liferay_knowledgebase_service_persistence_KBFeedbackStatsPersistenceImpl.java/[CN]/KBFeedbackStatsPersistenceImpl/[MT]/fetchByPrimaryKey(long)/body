{
  Session session=null;
  try {
    session=openSession();
    return (KBFeedbackStats)session.get(KBFeedbackStatsImpl.class,new Long(kbFeedbackStatsId));
  }
 catch (  Exception e) {
    throw processException(e);
  }
 finally {
    closeSession(session);
  }
}
