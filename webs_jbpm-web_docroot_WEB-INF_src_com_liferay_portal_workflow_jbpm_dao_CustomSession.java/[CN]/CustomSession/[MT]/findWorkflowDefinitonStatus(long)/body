{
  try {
    Criteria criteria=_session.createCriteria(WorkflowDefinitionStatusImpl.class);
    criteria.add(Restrictions.eq("processDefinition.id",processDefinitionId));
    return (WorkflowDefinitionStatusImpl)criteria.uniqueResult();
  }
 catch (  Exception e) {
    throw new JbpmException(e);
  }
}
