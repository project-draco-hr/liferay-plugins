{
  List<TaskInstance> taskInstances=new ArrayList<TaskInstance>(taskInstanceExtensions.size());
  for (  TaskInstanceExtensionImpl taskInstanceExtension : taskInstanceExtensions) {
    taskInstances.add(taskInstanceExtension.getTaskInstance());
  }
  return taskInstances;
}
