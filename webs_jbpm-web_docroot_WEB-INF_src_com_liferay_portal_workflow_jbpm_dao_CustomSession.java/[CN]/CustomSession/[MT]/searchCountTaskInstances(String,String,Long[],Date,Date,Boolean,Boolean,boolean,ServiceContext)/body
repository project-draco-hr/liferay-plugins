{
  try {
    Criteria criteria=buildTaskInstanceExtensionSearchCriteria(taskName,assetType,assetPrimaryKeys,dueDateGT,dueDateLT,completed,searchByUserRoles,andOperator,serviceContext);
    return (int)criteriaCount(criteria);
  }
 catch (  Exception e) {
    throw new JbpmException(e);
  }
}
