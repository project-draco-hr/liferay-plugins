{
  try {
    Criteria criteria=_session.createCriteria(WorkflowDefinitionExtensionImpl.class);
    criteria.add(Restrictions.eq("processDefinition.id",processDefinitionId));
    return (WorkflowDefinitionExtensionImpl)criteria.uniqueResult();
  }
 catch (  Exception e) {
    throw new JbpmException(e);
  }
}
