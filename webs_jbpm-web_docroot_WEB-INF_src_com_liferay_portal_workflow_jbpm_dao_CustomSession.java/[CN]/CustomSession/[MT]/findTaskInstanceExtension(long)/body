{
  try {
    Criteria criteria=_session.createCriteria(TaskInstanceExtensionImpl.class);
    criteria.add(Restrictions.eq("taskInstance.id",taskInstanceId));
    return (TaskInstanceExtensionImpl)criteria.uniqueResult();
  }
 catch (  Exception e) {
    throw new JbpmException(e);
  }
}
