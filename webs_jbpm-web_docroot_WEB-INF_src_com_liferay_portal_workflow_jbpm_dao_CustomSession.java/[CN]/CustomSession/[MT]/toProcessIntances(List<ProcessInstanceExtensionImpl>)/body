{
  List<ProcessInstance> processInstances=new ArrayList<ProcessInstance>(processInstanceExtensionImpls.size());
  for (  ProcessInstanceExtensionImpl workflowInstanceExtensionImpl : processInstanceExtensionImpls) {
    processInstances.add(workflowInstanceExtensionImpl.getProcessInstance());
  }
  return processInstances;
}
