{
  if (!InlineSQLHelperUtil.isEnabled(groupId)) {
    return findByR_G_L_PrevAndNext(articleId,resourcePrimKey,groupId,latest,orderByComparator);
  }
  Article article=findByPrimaryKey(articleId);
  Session session=null;
  try {
    session=openSession();
    Article[] array=new ArticleImpl[3];
    array[0]=filterGetByR_G_L_PrevAndNext(session,article,resourcePrimKey,groupId,latest,orderByComparator,true);
    array[1]=article;
    array[2]=filterGetByR_G_L_PrevAndNext(session,article,resourcePrimKey,groupId,latest,orderByComparator,false);
    return array;
  }
 catch (  Exception e) {
    throw processException(e);
  }
 finally {
    closeSession(session);
  }
}
