{
  Article article=findByPrimaryKey(articleId);
  Session session=null;
  try {
    session=openSession();
    Article[] array=new ArticleImpl[3];
    array[0]=getByG_P_M_PrevAndNext(session,article,groupId,parentResourcePrimKey,main,orderByComparator,true);
    array[1]=article;
    array[2]=getByG_P_M_PrevAndNext(session,article,groupId,parentResourcePrimKey,main,orderByComparator,false);
    return array;
  }
 catch (  Exception e) {
    throw processException(e);
  }
 finally {
    closeSession(session);
  }
}
