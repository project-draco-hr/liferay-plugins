{
  if (!InlineSQLHelperUtil.isEnabled(groupId)) {
    return findByG_R_L_PrevAndNext(articleId,groupId,resourcePrimKey,latest,orderByComparator);
  }
  Article article=findByPrimaryKey(articleId);
  Session session=null;
  try {
    session=openSession();
    Article[] array=new ArticleImpl[3];
    array[0]=filterGetByG_R_L_PrevAndNext(session,article,groupId,resourcePrimKey,latest,orderByComparator,true);
    array[1]=article;
    array[2]=filterGetByG_R_L_PrevAndNext(session,article,groupId,resourcePrimKey,latest,orderByComparator,false);
    return array;
  }
 catch (  Exception e) {
    throw processException(e);
  }
 finally {
    closeSession(session);
  }
}
