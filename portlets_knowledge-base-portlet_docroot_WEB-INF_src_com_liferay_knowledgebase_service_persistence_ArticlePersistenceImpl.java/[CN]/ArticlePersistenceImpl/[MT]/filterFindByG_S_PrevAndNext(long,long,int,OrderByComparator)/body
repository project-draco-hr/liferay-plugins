{
  if (!InlineSQLHelperUtil.isEnabled(groupId)) {
    return findByG_S_PrevAndNext(articleId,groupId,status,orderByComparator);
  }
  Article article=findByPrimaryKey(articleId);
  Session session=null;
  try {
    session=openSession();
    Article[] array=new ArticleImpl[3];
    array[0]=filterGetByG_S_PrevAndNext(session,article,groupId,status,orderByComparator,true);
    array[1]=article;
    array[2]=filterGetByG_S_PrevAndNext(session,article,groupId,status,orderByComparator,false);
    return array;
  }
 catch (  Exception e) {
    throw processException(e);
  }
 finally {
    closeSession(session);
  }
}
