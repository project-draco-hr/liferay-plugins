{
  Article article=findByPrimaryKey(articleId);
  Session session=null;
  try {
    session=openSession();
    Article[] array=new ArticleImpl[3];
    array[0]=getByR_S_PrevAndNext(session,article,resourcePrimKey,status,orderByComparator,true);
    array[1]=article;
    array[2]=getByR_S_PrevAndNext(session,article,resourcePrimKey,status,orderByComparator,false);
    return array;
  }
 catch (  Exception e) {
    throw processException(e);
  }
 finally {
    closeSession(session);
  }
}
