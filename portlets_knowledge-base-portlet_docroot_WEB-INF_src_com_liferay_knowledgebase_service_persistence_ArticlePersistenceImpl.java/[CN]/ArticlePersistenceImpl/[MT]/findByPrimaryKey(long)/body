{
  Article article=fetchByPrimaryKey(articleId);
  if (article == null) {
    if (_log.isWarnEnabled()) {
      _log.warn(_NO_SUCH_ENTITY_WITH_PRIMARY_KEY + articleId);
    }
    throw new NoSuchArticleException(_NO_SUCH_ENTITY_WITH_PRIMARY_KEY + articleId);
  }
  return article;
}
