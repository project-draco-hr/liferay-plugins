{
  Article article=findByPrimaryKey(articleId);
  Session session=null;
  try {
    session=openSession();
    Article[] array=new ArticleImpl[3];
    array[0]=getByP_L_PrevAndNext(session,article,parentResourcePrimKey,latest,orderByComparator,true);
    array[1]=article;
    array[2]=getByP_L_PrevAndNext(session,article,parentResourcePrimKey,latest,orderByComparator,false);
    return array;
  }
 catch (  Exception e) {
    throw processException(e);
  }
 finally {
    closeSession(session);
  }
}
