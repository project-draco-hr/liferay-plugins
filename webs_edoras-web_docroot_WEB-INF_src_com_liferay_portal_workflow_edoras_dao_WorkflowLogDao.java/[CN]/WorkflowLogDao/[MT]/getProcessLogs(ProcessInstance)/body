{
  try {
    List<WorkflowLog> workflowLogs=WorkflowLogUtil.findByWorkflowInstanceId(processInstance.getPrimaryKey());
    return WorkflowEntityBridgeUtil.wrapWorkflowLogs(workflowLogs);
  }
 catch (  Exception e) {
    throw new ProcessException(e.getMessage(),e);
  }
}
