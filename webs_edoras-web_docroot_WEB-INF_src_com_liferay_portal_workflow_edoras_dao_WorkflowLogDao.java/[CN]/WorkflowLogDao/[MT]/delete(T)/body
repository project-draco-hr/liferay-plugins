{
  WorkflowEntity workflowLog=(WorkflowEntity)entity;
  try {
    WorkflowLogUtil.remove(workflowLog.getPrimaryKey());
  }
 catch (  Exception e) {
    throw new ProcessException("Could not delete process log entry with id [" + workflowLog.getPrimaryKey() + "]",e);
  }
}
