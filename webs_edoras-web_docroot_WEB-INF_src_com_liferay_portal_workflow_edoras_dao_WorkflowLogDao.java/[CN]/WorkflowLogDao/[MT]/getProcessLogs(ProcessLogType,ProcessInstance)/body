{
  try {
    List<WorkflowLog> workflowLogs=WorkflowLogUtil.findByW_T(processInstance.getPrimaryKey(),processLogType.getId());
    return WorkflowEntityBridgeUtil.wrapWorkflowLogs(workflowLogs);
  }
 catch (  Exception e) {
    throw new ProcessException(e.getMessage(),e);
  }
}
