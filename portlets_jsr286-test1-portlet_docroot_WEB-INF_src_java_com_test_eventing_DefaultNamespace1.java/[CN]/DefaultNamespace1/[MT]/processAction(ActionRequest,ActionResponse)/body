{
  String s1=request.getParameter("op");
  if ("eventDefaultNS".equals(s1)) {
    try {
      String default1_Event="default1";
      String default1_Data="EventingUsesDefaultNamespace";
      response.setEvent(default1_Event,default1_Data);
    }
 catch (    Exception e) {
      e.printStackTrace();
    }
  }
  if ("renderDefaultNS".equals(s1)) {
    response.setRenderParameter("defaultrender1","RenderUsesDefaultNamespace");
  }
}
