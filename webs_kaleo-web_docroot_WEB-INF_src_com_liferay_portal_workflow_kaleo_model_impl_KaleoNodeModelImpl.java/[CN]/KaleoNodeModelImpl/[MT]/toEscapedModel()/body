{
  if (isEscapedModel()) {
    return (KaleoNode)this;
  }
 else {
    return (KaleoNode)Proxy.newProxyInstance(KaleoNode.class.getClassLoader(),new Class[]{KaleoNode.class},new AutoEscapeBeanHandler(this));
  }
}
