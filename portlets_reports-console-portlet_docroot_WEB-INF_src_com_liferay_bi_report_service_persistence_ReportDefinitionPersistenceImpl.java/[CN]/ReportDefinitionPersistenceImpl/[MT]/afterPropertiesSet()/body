{
  String[] listenerClassNames=StringUtil.split(GetterUtil.getString(com.liferay.util.service.ServiceProps.get("value.object.listener.com.liferay.bi.report.model.ReportDefinition")));
  if (listenerClassNames.length > 0) {
    try {
      List<ModelListener<ReportDefinition>> listenersList=new ArrayList<ModelListener<ReportDefinition>>();
      for (      String listenerClassName : listenerClassNames) {
        listenersList.add((ModelListener<ReportDefinition>)Class.forName(listenerClassName).newInstance());
      }
      listeners=listenersList.toArray(new ModelListener[listenersList.size()]);
    }
 catch (    Exception e) {
      _log.error(e);
    }
  }
}
