{
  for (  ReportDefinition reportDefinition : reportDefinitions) {
    if (EntityCacheUtil.getResult(ReportDefinitionModelImpl.ENTITY_CACHE_ENABLED,ReportDefinitionImpl.class,reportDefinition.getPrimaryKey(),this) == null) {
      cacheResult(reportDefinition);
    }
  }
}
