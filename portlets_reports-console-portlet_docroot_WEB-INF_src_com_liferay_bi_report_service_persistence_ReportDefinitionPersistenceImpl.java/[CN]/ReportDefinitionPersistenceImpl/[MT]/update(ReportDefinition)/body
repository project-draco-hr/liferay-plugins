{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(ReportDefinition reportDefinition) method. Use update(ReportDefinition reportDefinition, boolean merge) instead.");
  }
  return update(reportDefinition,false);
}
