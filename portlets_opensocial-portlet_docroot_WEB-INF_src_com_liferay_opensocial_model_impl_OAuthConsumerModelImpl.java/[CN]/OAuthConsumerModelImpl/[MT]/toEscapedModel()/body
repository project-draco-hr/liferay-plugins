{
  if (isEscapedModel()) {
    return (OAuthConsumer)this;
  }
 else {
    return (OAuthConsumer)Proxy.newProxyInstance(_classLoader,_escapedModelProxyInterfaces,new AutoEscapeBeanHandler(this));
  }
}
