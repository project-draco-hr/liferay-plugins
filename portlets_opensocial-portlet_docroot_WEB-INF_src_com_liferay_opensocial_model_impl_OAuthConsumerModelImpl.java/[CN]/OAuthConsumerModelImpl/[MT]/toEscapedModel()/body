{
  if (isEscapedModel()) {
    return (OAuthConsumer)this;
  }
 else {
    return (OAuthConsumer)Proxy.newProxyInstance(OAuthConsumer.class.getClassLoader(),new Class[]{OAuthConsumer.class},new AutoEscapeBeanHandler(this));
  }
}
