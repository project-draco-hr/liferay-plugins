{
  if (isEscapedModel()) {
    return (HRAssetCheckout)this;
  }
 else {
    return (HRAssetCheckout)Proxy.newProxyInstance(_classLoader,_escapedModelProxyInterfaces,new AutoEscapeBeanHandler(this));
  }
}
