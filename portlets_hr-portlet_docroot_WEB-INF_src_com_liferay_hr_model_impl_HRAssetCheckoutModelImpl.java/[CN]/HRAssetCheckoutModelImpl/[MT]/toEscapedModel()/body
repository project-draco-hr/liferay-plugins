{
  if (isEscapedModel()) {
    return (HRAssetCheckout)this;
  }
 else {
    return (HRAssetCheckout)Proxy.newProxyInstance(HRAssetCheckout.class.getClassLoader(),new Class[]{HRAssetCheckout.class},new AutoEscapeBeanHandler(this));
  }
}
