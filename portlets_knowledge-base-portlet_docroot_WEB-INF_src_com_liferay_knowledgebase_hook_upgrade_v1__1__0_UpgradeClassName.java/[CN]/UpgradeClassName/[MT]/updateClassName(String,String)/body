{
  long oldClassNameId=getClassNameId(oldClassName);
  long newClassNameId=getClassNameId(newClassName);
  runSQL("update ClassName_ set classNameId = " + newClassNameId + " value = '"+ newClassName+ "' where classNameId = "+ oldClassNameId);
}
