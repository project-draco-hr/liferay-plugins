{
  KaleoNode kaleoNode=fetchByPrimaryKey(kaleoNodeId);
  if (kaleoNode == null) {
    if (_log.isWarnEnabled()) {
      _log.warn(_NO_SUCH_ENTITY_WITH_PRIMARY_KEY + kaleoNodeId);
    }
    throw new NoSuchNodeException(_NO_SUCH_ENTITY_WITH_PRIMARY_KEY + kaleoNodeId);
  }
  return kaleoNode;
}
