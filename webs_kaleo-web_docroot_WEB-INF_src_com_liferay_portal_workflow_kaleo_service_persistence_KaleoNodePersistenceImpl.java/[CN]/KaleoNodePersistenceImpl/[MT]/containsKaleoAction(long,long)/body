{
  Object[] finderArgs=new Object[]{pk,kaleoActionPK};
  Boolean value=(Boolean)FinderCacheUtil.getResult(FINDER_PATH_CONTAINS_KALEOACTION,finderArgs,this);
  if (value == null) {
    try {
      value=Boolean.valueOf(containsKaleoAction.contains(pk,kaleoActionPK));
      FinderCacheUtil.putResult(FINDER_PATH_CONTAINS_KALEOACTION,finderArgs,value);
    }
 catch (    Exception e) {
      FinderCacheUtil.removeResult(FINDER_PATH_CONTAINS_KALEOACTION,finderArgs);
      throw processException(e);
    }
  }
  return value.booleanValue();
}
