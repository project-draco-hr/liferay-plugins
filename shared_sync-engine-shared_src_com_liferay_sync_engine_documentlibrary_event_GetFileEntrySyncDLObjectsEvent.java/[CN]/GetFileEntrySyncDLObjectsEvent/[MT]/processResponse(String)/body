{
  ObjectMapper objectMapper=new ObjectMapper();
  List<SyncFile> syncFiles=objectMapper.readValue(response,new TypeReference<List<SyncFile>>(){
  }
);
  for (  SyncFile syncFile : syncFiles) {
    System.out.println(syncFile);
  }
}
