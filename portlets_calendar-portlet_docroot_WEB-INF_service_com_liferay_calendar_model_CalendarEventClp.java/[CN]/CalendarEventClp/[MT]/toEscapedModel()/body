{
  if (isEscapedModel()) {
    return this;
  }
 else {
    return (CalendarEvent)Proxy.newProxyInstance(CalendarEvent.class.getClassLoader(),new Class[]{CalendarEvent.class},new AutoEscapeBeanHandler(this));
  }
}
