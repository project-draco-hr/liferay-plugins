{
  List<UserPresence> puserList=UserPresenceLocalServiceUtil.getAllUsers();
  boolean hasUser=false;
  for (  UserPresence puser : puserList) {
    if (puser.getPresenceUserId() == userId)     hasUser=true;
  }
  assertEquals(true,hasUser);
}
