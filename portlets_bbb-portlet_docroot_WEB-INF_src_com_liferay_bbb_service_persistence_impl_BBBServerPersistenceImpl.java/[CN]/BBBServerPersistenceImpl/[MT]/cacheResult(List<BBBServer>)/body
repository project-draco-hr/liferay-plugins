{
  for (  BBBServer bbbServer : bbbServers) {
    if (EntityCacheUtil.getResult(BBBServerModelImpl.ENTITY_CACHE_ENABLED,BBBServerImpl.class,bbbServer.getPrimaryKey()) == null) {
      cacheResult(bbbServer);
    }
 else {
      bbbServer.resetOriginalValues();
    }
  }
}
