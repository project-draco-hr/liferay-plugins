{
  final int prime=31;
  int result=1;
  result=prime * result + ((_priorityId == null) ? 0 : _priorityId.hashCode());
  result=prime * result + ((_priorityName == null) ? 0 : _priorityName.hashCode());
  return result;
}
