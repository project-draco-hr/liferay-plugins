{
  response.setContentType("text/html");
  PrintWriter writer=response.getWriter();
  if (request.getParameter("key") != null) {
    writer.println("Event recieved:" + request.getParameter("key"));
  }
}
