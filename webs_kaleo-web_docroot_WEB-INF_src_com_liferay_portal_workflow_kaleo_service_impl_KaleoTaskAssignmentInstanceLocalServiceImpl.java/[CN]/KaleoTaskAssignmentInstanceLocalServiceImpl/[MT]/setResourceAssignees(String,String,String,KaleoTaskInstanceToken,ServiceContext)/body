{
  List<Role> roles=RoleLocalServiceUtil.getResourceRoles(serviceContext.getCompanyId(),name,ResourceConstants.SCOPE_INDIVIDUAL,primKey,actionId);
  for (  Role role : roles) {
    addKaleoTaskAssignmentInstance(kaleoTaskInstanceToken,Role.class.getName(),role.getClassPK(),serviceContext);
  }
}
