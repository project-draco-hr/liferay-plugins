{
  if (isEscapedModel()) {
    return this;
  }
 else {
    return (HRUserProject)Proxy.newProxyInstance(HRUserProject.class.getClassLoader(),new Class[]{HRUserProject.class},new AutoEscapeBeanHandler(this));
  }
}
