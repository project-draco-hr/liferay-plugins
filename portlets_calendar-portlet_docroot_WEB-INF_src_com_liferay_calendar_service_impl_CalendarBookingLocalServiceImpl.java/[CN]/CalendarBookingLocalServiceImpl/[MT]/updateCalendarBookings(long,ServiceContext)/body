{
  List<CalendarBooking> calendarBookings=calendarBookingPersistence.findByCalendarEventId(calendarEventId);
  for (  CalendarBooking calendarBooking : calendarBookings) {
    updateCalendarBooking(calendarBooking.getCalendarBookingId(),calendarEventId,serviceContext);
  }
}
