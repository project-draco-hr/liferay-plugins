{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(KBArticleResource kbArticleResource) method. Use update(KBArticleResource kbArticleResource, boolean merge) instead.");
  }
  return update(kbArticleResource,false);
}
