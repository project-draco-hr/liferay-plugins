{
  try {
    List<WorkflowJob> jobList=WorkflowJobUtil.findAll();
    return WorkflowEntityBridgeUtil.wrapWorkflowJobList(jobList);
  }
 catch (  SystemException se) {
    throw new ProcessException("Could not load open jobs",se);
  }
}
