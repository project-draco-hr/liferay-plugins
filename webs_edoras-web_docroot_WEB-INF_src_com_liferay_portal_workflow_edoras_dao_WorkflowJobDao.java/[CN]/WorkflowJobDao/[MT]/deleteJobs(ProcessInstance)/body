{
  try {
    WorkflowJobUtil.removeByWorkflowInstanceId(instance.getPrimaryKey());
  }
 catch (  SystemException se) {
    throw new ProcessException("Could not remove workflow jobs for workflow instance with id [" + instance.getPrimaryKey() + "]",se);
  }
}
