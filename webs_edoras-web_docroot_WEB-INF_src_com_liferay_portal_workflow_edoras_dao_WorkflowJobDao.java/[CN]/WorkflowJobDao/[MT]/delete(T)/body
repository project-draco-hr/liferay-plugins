{
  WorkflowJobBridge job=(WorkflowJobBridge)entity;
  try {
    WorkflowJobUtil.remove(job.getPrimaryKey());
  }
 catch (  Exception e) {
    throw new ProcessException("Could not delete workflow job with id [" + job.getPrimaryKey() + "]",e);
  }
}
