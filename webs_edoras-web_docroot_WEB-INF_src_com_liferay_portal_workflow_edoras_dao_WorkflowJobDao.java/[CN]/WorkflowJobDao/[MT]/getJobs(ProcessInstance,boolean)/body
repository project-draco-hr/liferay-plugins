{
  try {
    List<WorkflowJob> workflowJobs=WorkflowJobUtil.findByWorkflowInstanceId(processInstance.getPrimaryKey());
    return WorkflowEntityBridgeUtil.wrapWorkflowJobs(workflowJobs);
  }
 catch (  Exception e) {
    throw new ProcessException(e.getMessage(),e);
  }
}
