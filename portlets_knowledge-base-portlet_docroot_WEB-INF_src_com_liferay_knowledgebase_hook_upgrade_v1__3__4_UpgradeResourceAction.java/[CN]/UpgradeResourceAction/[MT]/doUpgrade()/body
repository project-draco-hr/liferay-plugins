{
  if (_hasViewFeedbackResourceAction()) {
    runSQL("delete from ResourceAction where actionId = '" + ActionKeys.VIEW_SUGGESTIONS + "'");
    runSQL("update ResourceAction set actionId = '" + ActionKeys.VIEW_SUGGESTIONS + "' where actionId = '"+ _ACTION_ID_VIEW_KB_FEEDBACK+ "'");
  }
}
