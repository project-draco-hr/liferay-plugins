{
  if (isEscapedModel()) {
    return this;
  }
 else {
    return (KaleoTimer)Proxy.newProxyInstance(KaleoTimer.class.getClassLoader(),new Class[]{KaleoTimer.class},new AutoEscapeBeanHandler(this));
  }
}
