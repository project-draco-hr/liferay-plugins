{
  if (method != null) {
    method.invoke(this);
  }
  if (format.equals("json")) {
    JSONObject jsonData=JSONFactoryUtil.createJSONObject(String.valueOf(request.getAttribute("jsonData")));
    writeJSON(resourceRequest,resourceResponse,jsonData);
    return;
  }
}
