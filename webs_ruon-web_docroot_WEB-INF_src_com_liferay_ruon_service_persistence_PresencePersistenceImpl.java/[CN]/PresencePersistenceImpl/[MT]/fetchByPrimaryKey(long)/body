{
  Session session=null;
  try {
    session=openSession();
    return (Presence)session.get(PresenceImpl.class,new Long(presenceId));
  }
 catch (  Exception e) {
    throw processException(e);
  }
 finally {
    closeSession(session);
  }
}
