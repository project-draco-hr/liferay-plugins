{
  Object[] finderArgs=new Object[]{new Long(userId),Boolean.valueOf(online),String.valueOf(start),String.valueOf(end),String.valueOf(obc)};
  List<Presence> list=(List<Presence>)FinderCacheUtil.getResult(FINDER_PATH_FIND_BY_OBC_U_O,finderArgs,this);
  if (list == null) {
    Session session=null;
    try {
      session=openSession();
      StringBuilder query=new StringBuilder();
      query.append("FROM com.liferay.ruon.model.Presence WHERE ");
      query.append("userId = ?");
      query.append(" AND ");
      query.append("online_ = ?");
      query.append(" ");
      if (obc != null) {
        query.append("ORDER BY ");
        query.append(obc.getOrderBy());
      }
      Query q=session.createQuery(query.toString());
      QueryPos qPos=QueryPos.getInstance(q);
      qPos.add(userId);
      qPos.add(online);
      list=(List<Presence>)QueryUtil.list(q,getDialect(),start,end);
    }
 catch (    Exception e) {
      throw processException(e);
    }
 finally {
      if (list == null) {
        list=new ArrayList<Presence>();
      }
      cacheResult(list);
      FinderCacheUtil.putResult(FINDER_PATH_FIND_BY_OBC_U_O,finderArgs,list);
      closeSession(session);
    }
  }
  return list;
}
