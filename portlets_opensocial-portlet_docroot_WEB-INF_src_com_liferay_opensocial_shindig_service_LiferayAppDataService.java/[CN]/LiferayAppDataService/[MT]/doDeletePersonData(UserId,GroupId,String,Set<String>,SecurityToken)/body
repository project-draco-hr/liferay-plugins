{
  long companyId=getCompanyId(securityToken);
  long userIdLong=GetterUtil.getLong(userId.getUserId(securityToken));
  for (  String field : fields) {
    ExpandoColumn expandoColumn=getExpandoColumn(field);
    ExpandoValueLocalServiceUtil.deleteValue(companyId,User.class.getName(),OPEN_SOCIAL_DATA,expandoColumn.getName(),userIdLong);
  }
  return;
}
