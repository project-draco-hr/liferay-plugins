{
  long companyId=getCompanyId(securityToken);
  long userIdLong=GetterUtil.getLong(userId.getUserId(securityToken));
  for (  Entry entry : values.entrySet()) {
    String key=(String)entry.getKey();
    String value=entry.getValue().toString();
    ExpandoColumn expandoColumn=getExpandoColumn(companyId,key);
    ExpandoValueLocalServiceUtil.addValue(companyId,User.class.getName(),OPEN_SOCIAL_DATA,expandoColumn.getName(),userIdLong,value);
  }
  return;
}
