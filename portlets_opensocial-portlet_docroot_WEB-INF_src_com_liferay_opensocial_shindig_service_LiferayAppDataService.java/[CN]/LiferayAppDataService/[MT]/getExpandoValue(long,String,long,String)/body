{
  try {
    ExpandoValue expandoValue=ExpandoValueLocalServiceUtil.getValue(companyId,User.class.getName(),getTableName(appId),columnName,userId);
    return expandoValue.getData();
  }
 catch (  Exception e) {
    return StringPool.BLANK;
  }
}
