{
  int count=countByKCN_KCPK_Blocking(kaleoClassName,kaleoClassPK,blocking);
  List<KaleoTimer> list=findByKCN_KCPK_Blocking(kaleoClassName,kaleoClassPK,blocking,count - 1,count,orderByComparator);
  if (!list.isEmpty()) {
    return list.get(0);
  }
  return null;
}
