{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(MeetupsEntry meetupsEntry) method. Use update(MeetupsEntry meetupsEntry, boolean merge) instead.");
  }
  return update(meetupsEntry,false);
}
