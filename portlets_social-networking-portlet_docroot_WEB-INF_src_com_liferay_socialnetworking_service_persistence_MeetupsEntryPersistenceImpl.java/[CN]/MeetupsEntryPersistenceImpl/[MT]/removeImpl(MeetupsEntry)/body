{
  meetupsEntry=toUnwrappedModel(meetupsEntry);
  Session session=null;
  try {
    session=openSession();
    if (meetupsEntry.isCachedModel()) {
      meetupsEntry=(MeetupsEntry)session.get(MeetupsEntryImpl.class,meetupsEntry.getPrimaryKeyObj());
    }
    session.delete(meetupsEntry);
  }
 catch (  Exception e) {
    throw processException(e);
  }
 finally {
    closeSession(session);
  }
  clearCache(meetupsEntry);
  return meetupsEntry;
}
