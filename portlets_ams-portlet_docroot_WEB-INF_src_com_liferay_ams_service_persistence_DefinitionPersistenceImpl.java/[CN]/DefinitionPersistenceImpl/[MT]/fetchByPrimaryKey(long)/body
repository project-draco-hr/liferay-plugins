{
  Definition definition=(Definition)EntityCacheUtil.getResult(DefinitionModelImpl.ENTITY_CACHE_ENABLED,DefinitionImpl.class,definitionId);
  if (definition == _nullDefinition) {
    return null;
  }
  if (definition == null) {
    Session session=null;
    try {
      session=openSession();
      definition=(Definition)session.get(DefinitionImpl.class,Long.valueOf(definitionId));
      if (definition != null) {
        cacheResult(definition);
      }
 else {
        EntityCacheUtil.putResult(DefinitionModelImpl.ENTITY_CACHE_ENABLED,DefinitionImpl.class,definitionId,_nullDefinition);
      }
    }
 catch (    Exception e) {
      EntityCacheUtil.removeResult(DefinitionModelImpl.ENTITY_CACHE_ENABLED,DefinitionImpl.class,definitionId);
      throw processException(e);
    }
 finally {
      closeSession(session);
    }
  }
  return definition;
}
