{
  SyncAccountPersistence syncAccountPersistence=SyncAccountService.getPersistence();
  try {
    syncAccountPersistence.delete(_syncAccount);
  }
 catch (  Exception e) {
    _logger.error(e.getMessage(),e);
  }
}
