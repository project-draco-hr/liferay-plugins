{
  SyncAccount syncAccount=SyncAccountService.addSyncAccount("test@liferay.com","test","http://localhost:8080/api/jsonws/",null);
  _syncAccount=SyncAccountService.fetchSyncAccount(syncAccount.getSyncAccountId());
  Assert.assertNotNull(_syncAccount);
}
