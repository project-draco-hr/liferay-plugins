{
  SyncAccount syncAccount=SyncAccountService.addSyncAccount(null,"test@liferay.com","test","http://localhost:8080/api/jsonws/");
  _syncAccount=SyncAccountService.fetchSyncAccount(syncAccount.getSyncAccountId());
  Assert.assertNotNull(_syncAccount);
}
