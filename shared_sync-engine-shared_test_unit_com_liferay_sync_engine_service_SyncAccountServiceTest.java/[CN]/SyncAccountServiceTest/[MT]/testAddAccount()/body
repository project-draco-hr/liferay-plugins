{
  SyncAccount syncAccount=SyncAccountService.addSyncAccount(filePath,"test@liferay.com","test","http://localhost:8080/api/jsonws");
  _syncAccount=SyncAccountService.fetchSyncAccount(syncAccount.getSyncAccountId());
  Assert.assertNotNull(_syncAccount);
}
