{
  SyncAccount syncAccount=SyncAccountService.addSyncAccount("test@liferay.com","test","http://localhost:8080/api/jsonws/");
  _syncAccount=SyncAccountService.getSyncAccount(syncAccount.getSyncAccountId());
  Assert.assertNotNull(_syncAccount);
}
