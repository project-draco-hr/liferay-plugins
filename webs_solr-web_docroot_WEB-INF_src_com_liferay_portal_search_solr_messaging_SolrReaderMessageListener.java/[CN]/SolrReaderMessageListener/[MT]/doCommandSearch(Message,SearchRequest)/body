{
  Hits hits=_searchEngine.getSearcher().search(searchRequest.getCompanyId(),searchRequest.getQuery(),searchRequest.getSorts(),searchRequest.getStart(),searchRequest.getEnd());
  message.setPayload(hits);
  MessageBusUtil.sendMessage(message.getResponseDestination(),message);
}
