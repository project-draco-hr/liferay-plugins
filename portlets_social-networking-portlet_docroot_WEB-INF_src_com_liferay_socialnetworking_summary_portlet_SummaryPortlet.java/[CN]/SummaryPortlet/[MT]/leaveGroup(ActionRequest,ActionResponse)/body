{
  ThemeDisplay themeDisplay=(ThemeDisplay)actionRequest.getAttribute(WebKeys.THEME_DISPLAY);
  ServiceContext serviceContext=ServiceContextFactory.getInstance(Group.class.getName(),actionRequest);
  UserLocalServiceUtil.unsetGroupUsers(themeDisplay.getScopeGroupId(),new long[]{themeDisplay.getUserId()},serviceContext);
}
