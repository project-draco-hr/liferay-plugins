{
  String reportName=retriever.getReportName();
  JasperReport report=(JasperReport)_cache.get(reportName);
  if (report == null) {
    report=_delegate.compile(retriever);
    _cache.put(reportName,report);
  }
  return report;
}
