{
  if (isEscapedModel()) {
    return (AMSDefinition)this;
  }
 else {
    return (AMSDefinition)Proxy.newProxyInstance(AMSDefinition.class.getClassLoader(),new Class[]{AMSDefinition.class},new AutoEscapeBeanHandler(this));
  }
}
