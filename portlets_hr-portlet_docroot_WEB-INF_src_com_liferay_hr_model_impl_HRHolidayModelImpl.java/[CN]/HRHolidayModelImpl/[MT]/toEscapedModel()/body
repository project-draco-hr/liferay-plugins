{
  if (isEscapedModel()) {
    return (HRHoliday)this;
  }
 else {
    return (HRHoliday)Proxy.newProxyInstance(HRHoliday.class.getClassLoader(),new Class[]{HRHoliday.class},new AutoEscapeBeanHandler(this));
  }
}
