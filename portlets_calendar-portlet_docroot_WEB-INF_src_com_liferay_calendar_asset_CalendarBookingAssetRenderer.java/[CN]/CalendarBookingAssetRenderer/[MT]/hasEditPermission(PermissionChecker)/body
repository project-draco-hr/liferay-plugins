{
  Calendar calendar=null;
  try {
    calendar=CalendarLocalServiceUtil.getCalendar(_calendarBooking.getCalendarId());
  }
 catch (  Exception e) {
    _log.error(e);
  }
  return CalendarPermission.contains(permissionChecker,calendar,ActionKeys.MANAGE_BOOKINGS);
}
