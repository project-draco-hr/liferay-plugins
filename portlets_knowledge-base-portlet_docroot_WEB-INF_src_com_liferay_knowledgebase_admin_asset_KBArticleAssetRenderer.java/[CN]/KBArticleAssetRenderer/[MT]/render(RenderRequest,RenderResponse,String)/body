{
  if (template.equals(TEMPLATE_FULL_CONTENT)) {
    renderRequest.setAttribute(WebKeys.KNOWLEDGE_BASE_ARTICLE,_kbArticle);
    return "/admin/asset/" + template + ".jsp";
  }
 else {
    return null;
  }
}
