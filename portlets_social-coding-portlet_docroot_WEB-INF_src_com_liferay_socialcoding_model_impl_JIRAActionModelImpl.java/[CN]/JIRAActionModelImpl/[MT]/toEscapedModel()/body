{
  if (isEscapedModel()) {
    return (JIRAAction)this;
  }
 else {
    return (JIRAAction)Proxy.newProxyInstance(JIRAAction.class.getClassLoader(),new Class[]{JIRAAction.class},new AutoEscapeBeanHandler(this));
  }
}
