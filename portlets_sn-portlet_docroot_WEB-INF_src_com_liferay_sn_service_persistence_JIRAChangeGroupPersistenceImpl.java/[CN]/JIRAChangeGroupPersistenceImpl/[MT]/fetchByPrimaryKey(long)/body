{
  Session session=null;
  try {
    session=openSession();
    return (JIRAChangeGroup)session.get(JIRAChangeGroupImpl.class,new Long(jiraChangeGroupId));
  }
 catch (  Exception e) {
    throw processException(e);
  }
 finally {
    closeSession(session);
  }
}
