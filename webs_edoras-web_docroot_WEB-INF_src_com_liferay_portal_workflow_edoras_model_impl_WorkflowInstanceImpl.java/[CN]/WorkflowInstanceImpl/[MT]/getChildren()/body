{
  try {
    return WorkflowInstanceUtil.findByParentWorkflowInstanceId(getPrimaryKey());
  }
 catch (  SystemException e) {
    throw new ProcessException("Could not lazily fetch children for workflow instance with id [" + getPrimaryKey() + "]",e);
  }
}
