{
  try {
    return WorkflowInstanceUtil.findByParentWorkflowInstanceId(getPrimaryKey());
  }
 catch (  SystemException se) {
    throw new ProcessException("Could not fetch children for workflow instance with id " + getPrimaryKey(),se);
  }
}
