{
  for (  KBFolder kbFolder : kbFolders) {
    if (EntityCacheUtil.getResult(KBFolderModelImpl.ENTITY_CACHE_ENABLED,KBFolderImpl.class,kbFolder.getPrimaryKey()) == null) {
      cacheResult(kbFolder);
    }
 else {
      kbFolder.resetOriginalValues();
    }
  }
}
