{
  for (  HRTimeOff hrTimeOff : hrTimeOffs) {
    if (EntityCacheUtil.getResult(HRTimeOffModelImpl.ENTITY_CACHE_ENABLED,HRTimeOffImpl.class,hrTimeOff.getPrimaryKey()) == null) {
      cacheResult(hrTimeOff);
    }
  }
}
