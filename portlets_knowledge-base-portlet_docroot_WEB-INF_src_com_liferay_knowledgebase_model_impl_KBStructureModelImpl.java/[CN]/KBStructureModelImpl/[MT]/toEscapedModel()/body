{
  if (isEscapedModel()) {
    return (KBStructure)this;
  }
 else {
    return (KBStructure)Proxy.newProxyInstance(KBStructure.class.getClassLoader(),new Class[]{KBStructure.class},new AutoEscapeBeanHandler(this));
  }
}
