{
  if (value == null) {
    return;
  }
  if (value instanceof Project) {
    _project=(Project)value;
  }
 else   if (value instanceof String) {
    _project=Project.convert((String)value);
  }
 else {
    throw new IllegalArgumentException("Invalid project text:" + value);
  }
}
