{
  if (isEscapedModel()) {
    return this;
  }
 else {
    return (HRUserHistory)Proxy.newProxyInstance(HRUserHistory.class.getClassLoader(),new Class[]{HRUserHistory.class},new AutoEscapeBeanHandler(this));
  }
}
