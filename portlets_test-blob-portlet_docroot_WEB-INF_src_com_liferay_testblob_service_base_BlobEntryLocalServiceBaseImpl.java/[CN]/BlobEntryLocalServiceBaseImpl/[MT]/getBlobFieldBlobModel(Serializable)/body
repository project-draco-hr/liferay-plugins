{
  Session session=null;
  try {
    session=blobEntryPersistence.openSession();
    return (com.liferay.testblob.model.BlobEntryBlobFieldBlobModel)session.get(BlobEntryBlobFieldBlobModel.class,primaryKey);
  }
 catch (  Exception e) {
    throw blobEntryPersistence.processException(e);
  }
 finally {
    blobEntryPersistence.closeSession(session);
  }
}
