{
  Layout layout=addLayoutPrototype(companyId,defaultUserId,"Wiki","Share your knowledge with the " + "rest of the users through the wiki and navigate using tags " + "and categories.","2_columns_iii",layoutPrototypes);
  if (layout == null) {
    return;
  }
  addPortletId(layout,PortletKeys.WIKI,"column-1");
  addPortletId(layout,PortletKeys.TAGS_CATEGORIES_NAVIGATION,"column-2");
  addPortletId(layout,PortletKeys.TAGS_ENTRIES_NAVIGATION,"column-2");
}
