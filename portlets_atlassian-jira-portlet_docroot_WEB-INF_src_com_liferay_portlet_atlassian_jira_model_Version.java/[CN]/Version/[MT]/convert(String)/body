{
  int index=value.indexOf(",");
  if (index == -1) {
    throw new IllegalArgumentException("Invalid version text: " + value);
  }
  return new Version(value.substring(0,index),value.substring(index + 1,value.length()));
}
