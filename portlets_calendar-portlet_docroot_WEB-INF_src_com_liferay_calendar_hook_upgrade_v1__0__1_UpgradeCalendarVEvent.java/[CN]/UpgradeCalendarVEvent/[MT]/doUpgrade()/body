{
  if (tableHasColumn("CalendarBooking","vEventUid")) {
    return;
  }
  runSQL("alter table CalendarBooking add vEventUid VARCHAR(255)");
  runSQL("create index IX_8B23DA0E on CalendarBooking " + "(calendarId, vEventUid)");
}
