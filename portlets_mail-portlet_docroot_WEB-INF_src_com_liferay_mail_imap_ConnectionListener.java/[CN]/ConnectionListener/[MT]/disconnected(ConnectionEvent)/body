{
  if (_log.isDebugEnabled()) {
    long uptime=(System.currentTimeMillis() - _startTime) / 1000;
    _log.debug("The " + _service + " service has been disconnected after "+ uptime+ "secs of uptime.");
  }
}
