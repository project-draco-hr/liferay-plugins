{
  InputStream is=getInputStream("/images/" + name);
  return IGImageLocalServiceUtil.addImage(null,userId,serviceContext.getScopeGroupId(),folderId,name,StringPool.BLANK,name,is,"image/png",serviceContext);
}
