{
  BBBMeeting bbbMeeting=findByPrimaryKey(bbbMeetingId);
  Session session=null;
  try {
    session=openSession();
    BBBMeeting[] array=new BBBMeetingImpl[3];
    array[0]=getByBbbServerId_PrevAndNext(session,bbbMeeting,bbbServerId,orderByComparator,true);
    array[1]=bbbMeeting;
    array[2]=getByBbbServerId_PrevAndNext(session,bbbMeeting,bbbServerId,orderByComparator,false);
    return array;
  }
 catch (  Exception e) {
    throw processException(e);
  }
 finally {
    closeSession(session);
  }
}
