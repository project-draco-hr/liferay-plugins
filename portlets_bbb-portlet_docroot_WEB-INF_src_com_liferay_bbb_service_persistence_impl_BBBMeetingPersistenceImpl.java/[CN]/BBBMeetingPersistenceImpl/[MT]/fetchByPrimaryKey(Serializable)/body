{
  BBBMeeting bbbMeeting=(BBBMeeting)EntityCacheUtil.getResult(BBBMeetingModelImpl.ENTITY_CACHE_ENABLED,BBBMeetingImpl.class,primaryKey);
  if (bbbMeeting == _nullBBBMeeting) {
    return null;
  }
  if (bbbMeeting == null) {
    Session session=null;
    try {
      session=openSession();
      bbbMeeting=(BBBMeeting)session.get(BBBMeetingImpl.class,primaryKey);
      if (bbbMeeting != null) {
        cacheResult(bbbMeeting);
      }
 else {
        EntityCacheUtil.putResult(BBBMeetingModelImpl.ENTITY_CACHE_ENABLED,BBBMeetingImpl.class,primaryKey,_nullBBBMeeting);
      }
    }
 catch (    Exception e) {
      EntityCacheUtil.removeResult(BBBMeetingModelImpl.ENTITY_CACHE_ENABLED,BBBMeetingImpl.class,primaryKey);
      throw processException(e);
    }
 finally {
      closeSession(session);
    }
  }
  return bbbMeeting;
}
