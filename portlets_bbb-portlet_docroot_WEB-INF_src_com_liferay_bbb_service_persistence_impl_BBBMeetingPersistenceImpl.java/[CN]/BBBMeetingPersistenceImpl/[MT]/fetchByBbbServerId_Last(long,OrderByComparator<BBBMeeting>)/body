{
  int count=countByBbbServerId(bbbServerId);
  if (count == 0) {
    return null;
  }
  List<BBBMeeting> list=findByBbbServerId(bbbServerId,count - 1,count,orderByComparator);
  if (!list.isEmpty()) {
    return list.get(0);
  }
  return null;
}
