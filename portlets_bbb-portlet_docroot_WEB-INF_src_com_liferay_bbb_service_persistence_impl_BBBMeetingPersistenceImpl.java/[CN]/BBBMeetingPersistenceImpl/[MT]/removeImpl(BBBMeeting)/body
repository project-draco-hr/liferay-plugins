{
  bbbMeeting=toUnwrappedModel(bbbMeeting);
  Session session=null;
  try {
    session=openSession();
    if (!session.contains(bbbMeeting)) {
      bbbMeeting=(BBBMeeting)session.get(BBBMeetingImpl.class,bbbMeeting.getPrimaryKeyObj());
    }
    if (bbbMeeting != null) {
      session.delete(bbbMeeting);
    }
  }
 catch (  Exception e) {
    throw processException(e);
  }
 finally {
    closeSession(session);
  }
  if (bbbMeeting != null) {
    clearCache(bbbMeeting);
  }
  return bbbMeeting;
}
