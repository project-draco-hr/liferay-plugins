{
  int count=countByBSI_S(bbbServerId,status);
  if (count == 0) {
    return null;
  }
  List<BBBMeeting> list=findByBSI_S(bbbServerId,status,count - 1,count,orderByComparator);
  if (!list.isEmpty()) {
    return list.get(0);
  }
  return null;
}
