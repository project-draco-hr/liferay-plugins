{
  long startTime=System.currentTimeMillis();
  doRegister(filePath,recursive);
  String filePathName=FilePathNameUtil.getFilePathName(filePath);
  List<SyncFile> syncFiles=SyncFileService.findSyncFiles(filePathName,startTime,_watchEventListener.getSyncAccountId());
  for (  SyncFile syncFile : syncFiles) {
    fireWatchEventListener(SyncWatchEvent.EVENT_TYPE_DELETE,java.nio.file.Paths.get(syncFile.getFilePathName()));
  }
}
