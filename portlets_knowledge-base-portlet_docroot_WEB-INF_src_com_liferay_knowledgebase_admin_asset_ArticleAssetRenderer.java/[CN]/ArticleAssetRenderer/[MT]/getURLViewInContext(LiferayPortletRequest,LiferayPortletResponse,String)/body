{
  ThemeDisplay themeDisplay=(ThemeDisplay)liferayPortletRequest.getAttribute(WebKeys.THEME_DISPLAY);
  return KnowledgeBaseUtil.getArticleURL(_article.getResourcePrimKey(),themeDisplay.getPlid(),themeDisplay.getPortalURL());
}
