{
  byte[] bytes=getBytes(fileName);
  return DLAppLocalServiceUtil.addFileEntry(userId,serviceContext.getScopeGroupId(),folderId,fileName,"image/png",name,StringPool.BLANK,StringPool.BLANK,bytes,serviceContext);
}
