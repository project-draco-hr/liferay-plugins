{
  CalendarResourcePermission.check(getPermissionChecker(),calendarResourceId,ActionKeys.VIEW_BOOKINGS);
  return calendarBookingLocalService.getCalendarEventCalendarBookings(calendarEventId,start,end,orderByComparator);
}
