{
  CalendarResourcePermission.check(getPermissionChecker(),calendarResourceId,ActionKeys.VIEW_BOOKINGS);
  return calendarBookingLocalService.getCalendarBookings(className,classPK,start,end,orderByComparator);
}
