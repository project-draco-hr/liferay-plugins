{
  if (isEscapedModel()) {
    return this;
  }
 else {
    return (TasksEntry)Proxy.newProxyInstance(TasksEntry.class.getClassLoader(),new Class[]{TasksEntry.class},new AutoEscapeBeanHandler(this));
  }
}
