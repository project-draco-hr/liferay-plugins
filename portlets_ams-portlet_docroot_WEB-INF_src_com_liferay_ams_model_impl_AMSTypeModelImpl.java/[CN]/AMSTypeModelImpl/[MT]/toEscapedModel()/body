{
  if (isEscapedModel()) {
    return (AMSType)this;
  }
 else {
    return (AMSType)Proxy.newProxyInstance(AMSType.class.getClassLoader(),new Class[]{AMSType.class},new AutoEscapeBeanHandler(this));
  }
}
