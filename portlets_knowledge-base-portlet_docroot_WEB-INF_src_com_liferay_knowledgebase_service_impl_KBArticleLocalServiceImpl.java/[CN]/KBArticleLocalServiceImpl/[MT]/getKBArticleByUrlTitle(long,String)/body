{
  KBArticle kbArticle=fetchKBArticleByUrlTitle(groupId,urlTitle);
  if (kbArticle == null) {
    throw new NoSuchArticleException("No KBArticle exists with the key {groupId=" + groupId + ", urlTitle="+ urlTitle+ "}");
  }
  return kbArticle;
}
