{
  KBArticle article=null;
  if (version == 0) {
    article=getArticle(groupId,title,draft);
  }
 else {
    article=kbArticlePersistence.findByG_T_V_D(groupId,title,version,draft);
  }
  return article;
}
