{
  KBArticle kbArticle=kbArticlePersistence.findByPrimaryKey(kbArticleId);
  KBArticle nextKBArticle=prevAndNext[2];
  if (nextKBArticle != null) {
    return nextKBArticle;
  }
  KBArticle parentKBArticle=kbArticle.getParentKBArticle();
  if (parentKBArticle == null) {
    return null;
  }
  prevAndNext=kbArticlePersistence.findByG_P_L_PrevAndNext(parentKBArticle.getKbArticleId(),kbArticle.getGroupId(),parentKBArticle.getParentResourcePrimKey(),true,new KBArticlePriorityComparator(true));
  return getNextAntecessorKBArticle(parentKBArticle.getKbArticleId(),prevAndNext);
}
