{
  KBArticle kbArticle=fetchLatestKBArticleByUrlTitle(groupId,urlTitle,WorkflowConstants.STATUS_APPROVED);
  if (kbArticle == null) {
    kbArticle=fetchLatestKBArticleByUrlTitle(groupId,urlTitle,WorkflowConstants.STATUS_PENDING);
  }
  return kbArticle;
}
