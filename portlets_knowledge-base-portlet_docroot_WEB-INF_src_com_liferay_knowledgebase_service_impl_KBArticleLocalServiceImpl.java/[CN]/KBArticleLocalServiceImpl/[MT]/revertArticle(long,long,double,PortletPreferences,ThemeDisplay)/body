{
  KBArticle oldArticle=getArticle(resourcePrimKey,version);
  return updateArticle(userId,resourcePrimKey,0,oldArticle.getTitle(),oldArticle.getContent(),oldArticle.getDescription(),false,oldArticle.getParentResourcePrimKey(),null,prefs,themeDisplay);
}
