{
  List<KBArticle> articles=kbArticlePersistence.findByG_T_H(groupId,title,true,0,1);
  if (articles.size() > 0) {
    KBArticle article=articles.iterator().next();
    deleteArticle(article);
  }
}
