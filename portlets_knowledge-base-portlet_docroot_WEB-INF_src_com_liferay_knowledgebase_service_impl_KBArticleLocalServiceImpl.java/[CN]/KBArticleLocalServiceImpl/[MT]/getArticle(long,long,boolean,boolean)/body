{
  List<KBArticle> articles=kbArticleFinder.findByR_U_H_D(resourcePrimKey,userId,head,draft,0,1);
  if (articles.size() > 0) {
    return articles.get(0);
  }
 else {
    throw new NoSuchArticleException();
  }
}
