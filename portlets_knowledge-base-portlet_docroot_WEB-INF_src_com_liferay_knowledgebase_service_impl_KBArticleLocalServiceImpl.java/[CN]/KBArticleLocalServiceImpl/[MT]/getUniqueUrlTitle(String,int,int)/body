{
  String uniqueUrlTitle=baseUrlTitle + StringPool.DASH + suffix;
  if (uniqueUrlTitle.length() > maxLength) {
    uniqueUrlTitle=StringUtil.shorten(uniqueUrlTitle,maxLength,StringPool.DASH + suffix);
  }
  return uniqueUrlTitle;
}
