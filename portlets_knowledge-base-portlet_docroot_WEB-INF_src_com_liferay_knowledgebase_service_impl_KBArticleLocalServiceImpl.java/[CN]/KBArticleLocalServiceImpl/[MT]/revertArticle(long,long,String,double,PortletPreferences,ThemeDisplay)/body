{
  KBArticle oldArticle=getArticle(groupId,title,version);
  return updateArticle(userId,groupId,title,0,oldArticle.getContent(),oldArticle.getDescription(),KBArticleImpl.REVERTED + " to " + version,false,null,null,prefs,themeDisplay);
}
