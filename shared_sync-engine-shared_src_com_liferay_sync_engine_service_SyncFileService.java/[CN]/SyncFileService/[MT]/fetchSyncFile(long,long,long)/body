{
  try {
    return _syncFilePersistence.fetchSyncFile(syncAccountId,repositoryId,parentFolderId);
  }
 catch (  SQLException sqle) {
    if (_logger.isDebugEnabled()) {
      _logger.debug(sqle.getMessage(),sqle);
    }
    return null;
  }
}
