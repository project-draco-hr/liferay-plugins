{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(KBArticle kbArticle) method. Use update(KBArticle kbArticle, boolean merge) instead.");
  }
  return update(kbArticle,false);
}
