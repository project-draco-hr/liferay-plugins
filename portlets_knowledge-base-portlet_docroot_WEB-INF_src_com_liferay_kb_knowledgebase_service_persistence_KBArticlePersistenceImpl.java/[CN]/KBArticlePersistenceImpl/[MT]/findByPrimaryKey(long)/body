{
  KBArticle kbArticle=fetchByPrimaryKey(articleId);
  if (kbArticle == null) {
    if (_log.isWarnEnabled()) {
      _log.warn("No KBArticle exists with the primary key " + articleId);
    }
    throw new NoSuchArticleException("No KBArticle exists with the primary key " + articleId);
  }
  return kbArticle;
}
