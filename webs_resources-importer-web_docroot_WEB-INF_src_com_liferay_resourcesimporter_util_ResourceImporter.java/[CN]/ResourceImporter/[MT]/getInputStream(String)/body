{
  URL jsonURL=servletContext.getResource(resourcesDir.concat(jsonFileName));
  if (jsonURL == null) {
    return null;
  }
  URLConnection urlConnection=jsonURL.openConnection();
  return urlConnection.getInputStream();
}
