{
  URL sitemapJSONURL=servletContext.getResource(resourcesDir.concat(siteMapName));
  if (sitemapJSONURL == null) {
    return;
  }
  URLConnection urlConnection=sitemapJSONURL.openConnection();
  doAddLayouts(urlConnection.getInputStream());
}
