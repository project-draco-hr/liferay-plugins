{
  try {
    new OnAssociation(classPK,associationClassName,associationClassPK){
      @Override public void onAssociation(      User user,      Group group,      Directory directory) throws Exception {
        if (GroupLocalServiceUtil.hasUserGroup(user.getUserId(),group.getGroupId(),true)) {
          return;
        }
        GoogleMailGroupsUtil.deleteGGroupMember(directory,GoogleMailGroupsUtil.getGroupEmailAddress(group),GoogleMailGroupsUtil.getUserEmailAddress(user));
      }
    }
;
  }
 catch (  Exception e) {
    throw new ModelListenerException(e);
  }
}
