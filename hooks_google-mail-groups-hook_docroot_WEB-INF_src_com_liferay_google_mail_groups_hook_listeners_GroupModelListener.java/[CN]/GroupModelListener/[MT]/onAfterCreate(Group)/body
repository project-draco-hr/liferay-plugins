{
  if (!GoogleGroupsUtil.isSync(group)) {
    return;
  }
  try {
    GGroupManager gGroupManager=GoogleAppsConnectionFactoryUtil.getGGroupManager(group.getCompanyId());
    gGroupManager.addGGroup(GoogleGroupsUtil.getGroupEmailAddress(group),group.getDescriptiveName(),StringPool.BLANK,PortletPropsValues.GOOGLE_GROUPS_EMAIL_PERMISSION);
  }
 catch (  Exception e) {
    throw new ModelListenerException(e);
  }
}
