{
  if (!GoogleMailGroupsUtil.isSync(group)) {
    return;
  }
  try {
    GoogleMailGroupsUtil.deleteGGroup(GoogleMailGroupsUtil.getGroupEmailAddress(group));
  }
 catch (  Exception e) {
    _log.error(e,e);
  }
}
