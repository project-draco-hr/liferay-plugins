{
  if (!GoogleMailGroupsUtil.isSync(group)) {
    return;
  }
  try {
    GoogleMailGroupsUtil.deleteGGroup(GoogleMailGroupsUtil.getDirectory(),GoogleMailGroupsUtil.getGroupEmailAddress(group));
  }
 catch (  Exception e) {
    throw new ModelListenerException(e);
  }
}
