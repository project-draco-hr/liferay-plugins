{
  if (isEscapedModel()) {
    return (CalendarBooking)this;
  }
 else {
    return (CalendarBooking)Proxy.newProxyInstance(_classLoader,_escapedModelProxyInterfaces,new AutoEscapeBeanHandler(this));
  }
}
