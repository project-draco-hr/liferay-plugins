{
  if (isEscapedModel()) {
    return (CalendarBooking)this;
  }
 else {
    return (CalendarBooking)Proxy.newProxyInstance(CalendarBooking.class.getClassLoader(),new Class[]{CalendarBooking.class},new AutoEscapeBeanHandler(this));
  }
}
