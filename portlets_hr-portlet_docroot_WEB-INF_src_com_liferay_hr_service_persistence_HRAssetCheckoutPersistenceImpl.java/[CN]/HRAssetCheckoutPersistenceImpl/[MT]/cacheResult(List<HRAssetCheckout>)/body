{
  for (  HRAssetCheckout hrAssetCheckout : hrAssetCheckouts) {
    if (EntityCacheUtil.getResult(HRAssetCheckoutModelImpl.ENTITY_CACHE_ENABLED,HRAssetCheckoutImpl.class,hrAssetCheckout.getPrimaryKey()) == null) {
      cacheResult(hrAssetCheckout);
    }
  }
}
