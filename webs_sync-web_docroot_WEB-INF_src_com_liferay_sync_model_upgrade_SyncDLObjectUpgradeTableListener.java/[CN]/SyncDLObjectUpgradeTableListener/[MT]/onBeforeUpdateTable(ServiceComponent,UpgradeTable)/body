{
  if (previousServiceComponent.getBuildNumber() >= 2) {
    return;
  }
  String sqlString=upgradeTable.getCreateSQL();
  sqlString=StringUtil.replace(sqlString," primary key",StringPool.BLANK);
  upgradeTable.setCreateSQL(sqlString);
  _typePKObjectIdMap=getTypePKObjectIdMap();
}
