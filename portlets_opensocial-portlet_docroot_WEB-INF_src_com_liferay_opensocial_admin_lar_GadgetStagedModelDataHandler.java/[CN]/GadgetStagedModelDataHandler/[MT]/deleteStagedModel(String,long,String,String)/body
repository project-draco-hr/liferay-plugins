{
  Group group=GroupLocalServiceUtil.getGroup(groupId);
  Gadget gadget=fetchStagedModelByUuidAndCompanyId(uuid,group.getCompanyId());
  if (gadget != null) {
    GadgetLocalServiceUtil.deleteGadget(gadget);
  }
}
