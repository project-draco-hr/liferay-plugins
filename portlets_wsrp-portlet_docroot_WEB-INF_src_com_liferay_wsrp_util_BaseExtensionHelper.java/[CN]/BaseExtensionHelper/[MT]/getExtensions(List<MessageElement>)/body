{
  int size=messageElements.size();
  Extension[] extensions=new Extension[size];
  for (int i=0; i < size; i++) {
    MessageElement messageElement=messageElements.get(i);
    extensions[i]=new Extension(new MessageElement[]{messageElement});
  }
  return extensions;
}
