{
  List<KBFolder> kbFolders=KBFolderLocalServiceUtil.getKBFoldersByUuidAndCompanyId(uuid,companyId,QueryUtil.ALL_POS,QueryUtil.ALL_POS,new StagedModelModifiedDateComparator<KBFolder>());
  if (kbFolders.isEmpty()) {
    return null;
  }
  return kbFolders.get(0);
}
