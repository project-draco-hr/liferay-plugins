{
  List<User> users=userLocalService.getCompanyUsers(companyId,-1,-1);
  System.out.println("Users of the company " + companyId);
  for (  User user : users) {
    System.out.println("\tUser " + user.getEmailAddress() + " with id "+ user.getUserId());
  }
}
