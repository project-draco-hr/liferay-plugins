{
  for (  AMSType amsType : amsTypes) {
    if (EntityCacheUtil.getResult(AMSTypeModelImpl.ENTITY_CACHE_ENABLED,AMSTypeImpl.class,amsType.getPrimaryKey(),this) == null) {
      cacheResult(amsType);
    }
  }
}
