{
  AMSType amsType=(AMSType)EntityCacheUtil.getResult(AMSTypeModelImpl.ENTITY_CACHE_ENABLED,AMSTypeImpl.class,amsTypeId,this);
  if (amsType == null) {
    Session session=null;
    try {
      session=openSession();
      amsType=(AMSType)session.get(AMSTypeImpl.class,new Long(amsTypeId));
    }
 catch (    Exception e) {
      throw processException(e);
    }
 finally {
      if (amsType != null) {
        cacheResult(amsType);
      }
      closeSession(session);
    }
  }
  return amsType;
}
