{
  if (isEscapedModel()) {
    return (WSRPConsumer)this;
  }
 else {
    return (WSRPConsumer)Proxy.newProxyInstance(_classLoader,_escapedModelProxyInterfaces,new AutoEscapeBeanHandler(this));
  }
}
