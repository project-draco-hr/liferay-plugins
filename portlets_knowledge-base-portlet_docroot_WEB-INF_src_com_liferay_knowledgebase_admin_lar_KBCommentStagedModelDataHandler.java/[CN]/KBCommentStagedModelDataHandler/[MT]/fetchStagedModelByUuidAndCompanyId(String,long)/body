{
  List<KBComment> kbComments=KBCommentLocalServiceUtil.getKBCommentsByUuidAndCompanyId(uuid,companyId,QueryUtil.ALL_POS,QueryUtil.ALL_POS,new StagedModelModifiedDateComparator<KBComment>());
  if (ListUtil.isEmpty(kbComments)) {
    return null;
  }
  return kbComments.get(0);
}
