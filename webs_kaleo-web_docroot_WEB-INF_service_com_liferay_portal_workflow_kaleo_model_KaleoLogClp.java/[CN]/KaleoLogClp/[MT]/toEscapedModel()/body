{
  if (isEscapedModel()) {
    return this;
  }
 else {
    return (KaleoLog)Proxy.newProxyInstance(KaleoLog.class.getClassLoader(),new Class[]{KaleoLog.class},new AutoEscapeBeanHandler(this));
  }
}
