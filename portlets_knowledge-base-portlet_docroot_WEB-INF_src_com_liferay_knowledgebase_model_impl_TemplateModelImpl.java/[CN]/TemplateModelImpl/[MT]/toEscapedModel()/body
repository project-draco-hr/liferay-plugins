{
  if (isEscapedModel()) {
    return (Template)this;
  }
 else {
    return (Template)Proxy.newProxyInstance(Template.class.getClassLoader(),new Class[]{Template.class},new AutoEscapeBeanHandler(this));
  }
}
