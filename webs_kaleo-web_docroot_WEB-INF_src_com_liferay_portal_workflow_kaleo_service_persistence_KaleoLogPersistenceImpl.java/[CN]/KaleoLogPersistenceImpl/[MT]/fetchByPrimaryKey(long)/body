{
  KaleoLog kaleoLog=(KaleoLog)EntityCacheUtil.getResult(KaleoLogModelImpl.ENTITY_CACHE_ENABLED,KaleoLogImpl.class,kaleoLogId,this);
  if (kaleoLog == null) {
    Session session=null;
    try {
      session=openSession();
      kaleoLog=(KaleoLog)session.get(KaleoLogImpl.class,new Long(kaleoLogId));
    }
 catch (    Exception e) {
      throw processException(e);
    }
 finally {
      if (kaleoLog != null) {
        cacheResult(kaleoLog);
      }
      closeSession(session);
    }
  }
  return kaleoLog;
}
