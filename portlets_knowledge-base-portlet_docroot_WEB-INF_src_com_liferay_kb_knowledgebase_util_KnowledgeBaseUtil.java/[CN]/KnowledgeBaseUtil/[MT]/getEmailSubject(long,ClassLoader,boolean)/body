{
  String name;
  if (update) {
    name=PortletPropsKeys.ADMIN_EMAIL_ARTICLE_UPDATED_SUBJECT;
  }
 else {
    name=PortletPropsKeys.ADMIN_EMAIL_ARTICLE_ADDED_SUBJECT;
  }
  return PortletPrefsPropsUtil.getContent(companyId,name,classLoader);
}
