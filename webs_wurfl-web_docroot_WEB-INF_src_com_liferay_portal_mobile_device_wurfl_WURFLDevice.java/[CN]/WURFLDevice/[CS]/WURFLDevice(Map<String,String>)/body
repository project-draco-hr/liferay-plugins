{
  for (  Entry<String,String> capabilityEntry : capabilities.entrySet()) {
    Capability capability=new Capability(capabilityEntry.getKey(),capabilityEntry.getValue());
    _capabilities.put(capabilityEntry.getKey(),capability);
  }
}
