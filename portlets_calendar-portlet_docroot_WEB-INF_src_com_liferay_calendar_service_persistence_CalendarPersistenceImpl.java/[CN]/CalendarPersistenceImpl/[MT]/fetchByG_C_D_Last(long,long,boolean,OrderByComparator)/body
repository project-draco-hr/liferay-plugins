{
  int count=countByG_C_D(groupId,calendarResourceId,defaultCalendar);
  List<Calendar> list=findByG_C_D(groupId,calendarResourceId,defaultCalendar,count - 1,count,orderByComparator);
  if (!list.isEmpty()) {
    return list.get(0);
  }
  return null;
}
