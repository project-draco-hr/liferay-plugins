{
  try {
    Class<?> clazz=getClass();
    String className=clazz.getName();
    _customSQL=(CustomSQL)InstanceFactory.newInstance(className.substring(0,className.length() - 4));
  }
 catch (  Exception e) {
    _log.error(e,e);
  }
}
