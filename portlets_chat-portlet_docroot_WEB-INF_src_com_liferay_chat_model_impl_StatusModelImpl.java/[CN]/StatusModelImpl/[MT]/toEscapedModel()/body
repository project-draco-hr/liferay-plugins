{
  if (isEscapedModel()) {
    return (Status)this;
  }
 else {
    return (Status)Proxy.newProxyInstance(Status.class.getClassLoader(),new Class[]{Status.class},new AutoEscapeBeanHandler(this));
  }
}
