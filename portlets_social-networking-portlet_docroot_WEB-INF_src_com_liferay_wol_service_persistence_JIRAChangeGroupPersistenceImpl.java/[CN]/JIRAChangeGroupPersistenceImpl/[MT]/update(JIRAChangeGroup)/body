{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(JIRAChangeGroup jiraChangeGroup) method. Use update(JIRAChangeGroup jiraChangeGroup, boolean merge) instead.");
  }
  return update(jiraChangeGroup,false);
}
