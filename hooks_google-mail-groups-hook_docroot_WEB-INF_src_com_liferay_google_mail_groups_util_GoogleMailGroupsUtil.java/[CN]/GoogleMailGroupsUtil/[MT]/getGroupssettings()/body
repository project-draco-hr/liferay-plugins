{
  if (_groupssettings != null) {
    return _groupssettings;
  }
  GoogleCredential googleCredential=getGoogleCredential();
  Groupssettings.Builder builder=new Groupssettings.Builder(googleCredential.getTransport(),googleCredential.getJsonFactory(),googleCredential);
  _groupssettings=builder.build();
  return _groupssettings;
}
