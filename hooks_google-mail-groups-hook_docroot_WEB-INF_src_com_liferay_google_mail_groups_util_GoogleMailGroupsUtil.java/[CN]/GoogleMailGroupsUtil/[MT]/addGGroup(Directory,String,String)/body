{
  try {
    Directory.Groups gGroups=directory.groups();
    com.google.api.services.admin.directory.model.Group gGroup=new com.google.api.services.admin.directory.model.Group();
    gGroup.setEmail(groupEmailAddress);
    gGroup.setName(name);
    Directory.Groups.Insert insert=gGroups.insert(gGroup);
    insert.execute();
  }
 catch (  Exception e) {
    throw new PortalException(e);
  }
}
