{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(WSRPConsumerPortlet wsrpConsumerPortlet) method. Use update(WSRPConsumerPortlet wsrpConsumerPortlet, boolean merge) instead.");
  }
  return update(wsrpConsumerPortlet,false);
}
