{
  if (isEscapedModel()) {
    return (ProjectsEntry)this;
  }
 else {
    return (ProjectsEntry)Proxy.newProxyInstance(ProjectsEntry.class.getClassLoader(),new Class[]{ProjectsEntry.class},new AutoEscapeBeanHandler(this));
  }
}
