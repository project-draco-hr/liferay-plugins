{
  if (isEscapedModel()) {
    return this;
  }
 else {
    return (MicroblogsEntry)Proxy.newProxyInstance(MicroblogsEntry.class.getClassLoader(),new Class[]{MicroblogsEntry.class},new AutoEscapeBeanHandler(this));
  }
}
