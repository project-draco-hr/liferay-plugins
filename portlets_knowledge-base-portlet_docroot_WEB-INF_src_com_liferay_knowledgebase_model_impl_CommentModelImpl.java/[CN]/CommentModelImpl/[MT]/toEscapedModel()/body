{
  if (isEscapedModel()) {
    return (Comment)this;
  }
 else {
    return (Comment)Proxy.newProxyInstance(Comment.class.getClassLoader(),new Class[]{Comment.class},new AutoEscapeBeanHandler(this));
  }
}
