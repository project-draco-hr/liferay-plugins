{
  for (  SSOSession ssoSession : ssoSessions) {
    if (EntityCacheUtil.getResult(SSOSessionModelImpl.ENTITY_CACHE_ENABLED,SSOSessionImpl.class,ssoSession.getPrimaryKey(),this) == null) {
      cacheResult(ssoSession);
    }
  }
}
