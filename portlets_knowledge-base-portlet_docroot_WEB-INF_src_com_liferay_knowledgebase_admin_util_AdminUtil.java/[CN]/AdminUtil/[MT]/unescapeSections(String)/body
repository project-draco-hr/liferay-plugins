{
  String[] array=StringUtil.split(sections);
  for (int i=0; i < array.length; i++) {
    if (StringUtil.startsWith(array[i],StringPool.UNDERLINE) && StringUtil.endsWith(array[i],StringPool.UNDERLINE)) {
      array[i]=array[i].substring(1,array[i].length() - 1);
    }
  }
  return array;
}
