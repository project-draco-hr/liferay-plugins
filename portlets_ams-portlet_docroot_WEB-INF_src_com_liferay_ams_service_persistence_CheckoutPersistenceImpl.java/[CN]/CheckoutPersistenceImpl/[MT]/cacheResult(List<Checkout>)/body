{
  for (  Checkout checkout : checkouts) {
    if (EntityCacheUtil.getResult(CheckoutModelImpl.ENTITY_CACHE_ENABLED,CheckoutImpl.class,checkout.getPrimaryKey()) == null) {
      cacheResult(checkout);
    }
 else {
      checkout.resetOriginalValues();
    }
  }
}
