{
  Session session=null;
  try {
    session=openSession();
    return (AssetDefinition)session.get(AssetDefinitionImpl.class,new Long(assetDefinitionId));
  }
 catch (  Exception e) {
    throw processException(e);
  }
 finally {
    closeSession(session);
  }
}
