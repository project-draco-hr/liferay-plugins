{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(AssetDefinition assetDefinition) method. Use update(AssetDefinition assetDefinition, boolean merge) instead.");
  }
  return update(assetDefinition,false);
}
