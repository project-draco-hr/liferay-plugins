{
  try {
    _remoteMethods.get("setRollbackOnly").invoke(_remoteTransactionStatus,null);
  }
 catch (  Exception e) {
    throw new IllegalArgumentException("Could not invoke remote transaction status",e);
  }
}
