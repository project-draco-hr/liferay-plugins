{
  try {
    return (Boolean)_remoteMethods.get("hasSavepoint").invoke(_remoteTransactionStatus,null);
  }
 catch (  Exception e) {
    throw new IllegalArgumentException("Could not invoke remote transaction status",e);
  }
}
