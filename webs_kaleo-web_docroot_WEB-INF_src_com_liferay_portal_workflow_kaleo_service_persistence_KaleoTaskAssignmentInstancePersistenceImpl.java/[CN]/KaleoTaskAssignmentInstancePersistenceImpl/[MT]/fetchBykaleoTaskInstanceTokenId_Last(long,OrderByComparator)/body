{
  int count=countBykaleoTaskInstanceTokenId(kaleoTaskInstanceTokenId);
  List<KaleoTaskAssignmentInstance> list=findBykaleoTaskInstanceTokenId(kaleoTaskInstanceTokenId,count - 1,count,orderByComparator);
  if (!list.isEmpty()) {
    return list.get(0);
  }
  return null;
}
