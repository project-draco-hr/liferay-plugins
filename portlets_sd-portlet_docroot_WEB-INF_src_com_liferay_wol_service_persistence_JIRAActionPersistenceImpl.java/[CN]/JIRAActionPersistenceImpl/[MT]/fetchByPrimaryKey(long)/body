{
  Session session=null;
  try {
    session=openSession();
    return (JIRAAction)session.get(JIRAActionImpl.class,new Long(jiraActionId));
  }
 catch (  Exception e) {
    throw processException(e);
  }
 finally {
    closeSession(session);
  }
}
