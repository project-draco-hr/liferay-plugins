{
  PortletDisplay portletDisplay=themeDisplay.getPortletDisplay();
  String actionId=_formatActionId(controller,action);
  return contains(themeDisplay.getPermissionChecker(),themeDisplay.getScopeGroupId(),portletDisplay.getRootPortletId(),actionId);
}
