{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(ReportRequest reportRequest) method. Use update(ReportRequest reportRequest, boolean merge) instead.");
  }
  return update(reportRequest,false);
}
