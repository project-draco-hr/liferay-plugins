{
  ReportRequest reportRequest=(ReportRequest)EntityCacheUtil.getResult(ReportRequestModelImpl.ENTITY_CACHE_ENABLED,ReportRequestImpl.class,requestId,this);
  if (reportRequest == null) {
    Session session=null;
    try {
      session=openSession();
      reportRequest=(ReportRequest)session.get(ReportRequestImpl.class,new Long(requestId));
    }
 catch (    Exception e) {
      throw processException(e);
    }
 finally {
      if (reportRequest != null) {
        cacheResult(reportRequest);
      }
      closeSession(session);
    }
  }
  return reportRequest;
}
