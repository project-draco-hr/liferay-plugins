{
  List<KBArticle> kbArticles=KBArticleLocalServiceUtil.getKBArticlesByUuidAndCompanyId(uuid,companyId,QueryUtil.ALL_POS,QueryUtil.ALL_POS,new StagedModelModifiedDateComparator<KBArticle>());
  if (ListUtil.isEmpty(kbArticles)) {
    return null;
  }
  return kbArticles.get(0);
}
