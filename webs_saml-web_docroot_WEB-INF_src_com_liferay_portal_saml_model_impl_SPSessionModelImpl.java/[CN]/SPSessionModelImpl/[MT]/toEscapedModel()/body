{
  if (isEscapedModel()) {
    return (SPSession)this;
  }
 else {
    return (SPSession)Proxy.newProxyInstance(SPSession.class.getClassLoader(),new Class[]{SPSession.class},new AutoEscapeBeanHandler(this));
  }
}
