{
  try {
    deleteArticle(companyId,groupId,title);
  }
 catch (  SearchException se) {
  }
  Document doc=getArticleDocument(companyId,groupId,title,content,description,tagsEntries);
  SearchEngineUtil.addDocument(companyId,doc);
}
