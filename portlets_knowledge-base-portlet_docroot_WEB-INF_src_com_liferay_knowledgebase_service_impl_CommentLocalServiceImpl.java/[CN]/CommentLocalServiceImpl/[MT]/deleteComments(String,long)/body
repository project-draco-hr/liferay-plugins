{
  long classNameId=PortalUtil.getClassNameId(className);
  List<Comment> comments=commentPersistence.findByC_C(classNameId,classPK);
  for (  Comment comment : comments) {
    deleteComment(comment);
  }
}
