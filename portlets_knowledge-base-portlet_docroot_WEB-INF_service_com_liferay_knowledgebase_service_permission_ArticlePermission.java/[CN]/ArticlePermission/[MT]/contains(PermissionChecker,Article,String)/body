{
  if (permissionChecker.hasOwnerPermission(article.getCompanyId(),Article.class.getName(),article.getRootResourcePrimKey(),article.getUserId(),actionId)) {
    return true;
  }
  return permissionChecker.hasPermission(article.getGroupId(),Article.class.getName(),article.getRootResourcePrimKey(),actionId);
}
