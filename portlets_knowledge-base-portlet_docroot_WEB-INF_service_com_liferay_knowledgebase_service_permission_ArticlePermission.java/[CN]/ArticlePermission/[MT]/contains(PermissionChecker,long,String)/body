{
  if (resourcePrimKey == ArticleConstants.DEFAULT_PARENT_RESOURCE_PRIM_KEY) {
    return true;
  }
  Article article=ArticleLocalServiceUtil.getLatestArticle(resourcePrimKey,WorkflowConstants.STATUS_ANY);
  return contains(permissionChecker,article,actionId);
}
