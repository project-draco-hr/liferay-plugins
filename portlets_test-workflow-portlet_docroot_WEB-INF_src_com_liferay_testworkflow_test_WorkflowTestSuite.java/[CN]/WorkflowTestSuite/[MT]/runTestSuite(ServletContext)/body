{
  WorkflowTestSuite workflowTestSuite=new WorkflowTestSuite();
  workflowTestSuite.addTestCase(new WorkflowEngineManagerTestCase());
  workflowTestSuite.addTestCase(new WorkflowDefinitionManagerTestCase());
  workflowTestSuite.addTestCase(new WorkflowInstanceManagerTestCase());
  JSONArray testSuiteResult=JSONFactoryUtil.createJSONArray();
  for (  BaseTestCase testCase : workflowTestSuite._testCases) {
    JSONObject testCaseResult=testCase.callTestMethods(servletContext);
    testSuiteResult.put(testCaseResult);
  }
  return testSuiteResult;
}
