{
  List<HRTimeOffSoap> soapModels=new ArrayList<HRTimeOffSoap>(models.size());
  for (  HRTimeOff model : models) {
    soapModels.add(toSoapModel(model));
  }
  return soapModels.toArray(new HRTimeOffSoap[soapModels.size()]);
}
