{
  if (isEscapedModel()) {
    return this;
  }
 else {
    return (WallEntry)Proxy.newProxyInstance(WallEntry.class.getClassLoader(),new Class[]{WallEntry.class},new AutoEscapeBeanHandler(this));
  }
}
