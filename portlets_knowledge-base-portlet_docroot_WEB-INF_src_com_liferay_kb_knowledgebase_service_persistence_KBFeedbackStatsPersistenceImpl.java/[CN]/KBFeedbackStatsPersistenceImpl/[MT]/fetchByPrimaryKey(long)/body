{
  Session session=null;
  try {
    session=openSession();
    return (KBFeedbackStats)session.get(KBFeedbackStatsImpl.class,new Long(feedbackStatsId));
  }
 catch (  Exception e) {
    throw processException(e);
  }
 finally {
    closeSession(session);
  }
}
