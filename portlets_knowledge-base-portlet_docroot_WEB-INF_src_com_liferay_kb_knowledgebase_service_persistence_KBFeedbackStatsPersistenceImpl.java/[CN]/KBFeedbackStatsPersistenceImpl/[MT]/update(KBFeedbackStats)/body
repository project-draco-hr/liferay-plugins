{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(KBFeedbackStats kbFeedbackStats) method. Use update(KBFeedbackStats kbFeedbackStats, boolean merge) instead.");
  }
  return update(kbFeedbackStats,false);
}
