{
  int allowReportMessageTime=PrefsPortletPropsUtil.getInteger(companyId,PortletPropsKeys.AKISMET_ALLOW_REPORT_MESSAGE_TIME);
  Date clearBeforeDate=new Date(System.currentTimeMillis() - (allowReportMessageTime * Time.DAY));
  AkismetDataLocalServiceUtil.clearAkismetData(clearBeforeDate);
}
