{
  int count=countByG_N_A(groupId,name,active);
  if (count == 0) {
    return null;
  }
  List<CalendarResource> list=findByG_N_A(groupId,name,active,count - 1,count,orderByComparator);
  if (!list.isEmpty()) {
    return list.get(0);
  }
  return null;
}
