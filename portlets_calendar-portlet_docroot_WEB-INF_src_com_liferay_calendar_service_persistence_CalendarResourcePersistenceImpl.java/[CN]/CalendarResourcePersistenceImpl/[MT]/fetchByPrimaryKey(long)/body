{
  CalendarResource calendarResource=(CalendarResource)EntityCacheUtil.getResult(CalendarResourceModelImpl.ENTITY_CACHE_ENABLED,CalendarResourceImpl.class,calendarResourceId,this);
  if (calendarResource == null) {
    Session session=null;
    try {
      session=openSession();
      calendarResource=(CalendarResource)session.get(CalendarResourceImpl.class,Long.valueOf(calendarResourceId));
    }
 catch (    Exception e) {
      throw processException(e);
    }
 finally {
      if (calendarResource != null) {
        cacheResult(calendarResource);
      }
      closeSession(session);
    }
  }
  return calendarResource;
}
