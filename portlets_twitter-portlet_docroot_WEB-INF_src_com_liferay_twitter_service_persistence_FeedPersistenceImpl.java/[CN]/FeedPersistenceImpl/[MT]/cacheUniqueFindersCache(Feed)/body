{
  if (feed.isNew()) {
    Object[] args=new Object[]{feed.getUserId(),feed.getTwitterScreenName()};
    FinderCacheUtil.putResult(FINDER_PATH_COUNT_BY_U_TSN,args,Long.valueOf(1));
    FinderCacheUtil.putResult(FINDER_PATH_FETCH_BY_U_TSN,args,feed);
  }
 else {
    FeedModelImpl feedModelImpl=(FeedModelImpl)feed;
    if ((feedModelImpl.getColumnBitmask() & FINDER_PATH_FETCH_BY_U_TSN.getColumnBitmask()) != 0) {
      Object[] args=new Object[]{feed.getUserId(),feed.getTwitterScreenName()};
      FinderCacheUtil.putResult(FINDER_PATH_COUNT_BY_U_TSN,args,Long.valueOf(1));
      FinderCacheUtil.putResult(FINDER_PATH_FETCH_BY_U_TSN,args,feed);
    }
  }
}
