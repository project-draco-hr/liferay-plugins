{
  kbCommentPersistence.remove(kbComment);
  AssetEntry assetEntry=assetEntryLocalService.getEntry(KBComment.class.getName(),kbComment.getKbCommentId());
  socialActivityLocalService.deleteActivities(assetEntry);
}
