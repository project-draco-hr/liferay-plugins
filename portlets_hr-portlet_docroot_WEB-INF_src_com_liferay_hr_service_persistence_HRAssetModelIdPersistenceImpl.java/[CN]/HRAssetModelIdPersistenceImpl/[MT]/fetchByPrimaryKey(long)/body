{
  HRAssetModelId hrAssetModelId=(HRAssetModelId)EntityCacheUtil.getResult(HRAssetModelIdModelImpl.ENTITY_CACHE_ENABLED,HRAssetModelIdImpl.class,hrAssetModelId,this);
  if (hrAssetModelId == null) {
    Session session=null;
    try {
      session=openSession();
      hrAssetModelId=(HRAssetModelId)session.get(HRAssetModelIdImpl.class,Long.valueOf(hrAssetModelId));
    }
 catch (    Exception e) {
      throw processException(e);
    }
 finally {
      if (hrAssetModelId != null) {
        cacheResult(hrAssetModelId);
      }
      closeSession(session);
    }
  }
  return hrAssetModelId;
}
