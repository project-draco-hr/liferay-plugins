{
  if (isEscapedModel()) {
    return (HRTask)this;
  }
 else {
    return (HRTask)Proxy.newProxyInstance(HRTask.class.getClassLoader(),new Class[]{HRTask.class},new AutoEscapeBeanHandler(this));
  }
}
