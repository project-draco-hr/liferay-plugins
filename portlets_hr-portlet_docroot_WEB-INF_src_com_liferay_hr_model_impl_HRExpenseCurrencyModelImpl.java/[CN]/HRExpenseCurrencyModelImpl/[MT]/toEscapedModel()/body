{
  if (isEscapedModel()) {
    return (HRExpenseCurrency)this;
  }
 else {
    return (HRExpenseCurrency)Proxy.newProxyInstance(_classLoader,_escapedModelProxyInterfaces,new AutoEscapeBeanHandler(this));
  }
}
