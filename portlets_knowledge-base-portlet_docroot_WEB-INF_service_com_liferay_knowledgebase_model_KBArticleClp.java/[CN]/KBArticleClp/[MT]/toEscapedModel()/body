{
  if (isEscapedModel()) {
    return this;
  }
 else {
    return (KBArticle)Proxy.newProxyInstance(KBArticle.class.getClassLoader(),new Class[]{KBArticle.class},new AutoEscapeBeanHandler(this));
  }
}
