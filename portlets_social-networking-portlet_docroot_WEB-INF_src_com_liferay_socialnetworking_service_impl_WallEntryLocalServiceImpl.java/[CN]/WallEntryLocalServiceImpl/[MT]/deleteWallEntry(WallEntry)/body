{
  AssetEntry assetEntry=assetEntryLocalService.getEntry(WallEntry.class.getName(),wallEntry.getWallEntryId());
  SocialActivityLocalServiceUtil.deleteActivities(assetEntry);
  wallEntryPersistence.remove(wallEntry);
}
