{
  try {
    SubscriptionLocalServiceUtil.deleteSubscriptions(group.getCompanyId(),Article.class.getName(),group.getGroupId());
  }
 catch (  Exception e) {
    throw new ModelListenerException(e);
  }
}
