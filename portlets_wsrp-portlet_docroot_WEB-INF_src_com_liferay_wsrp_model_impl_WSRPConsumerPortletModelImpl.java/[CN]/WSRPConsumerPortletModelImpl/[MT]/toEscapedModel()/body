{
  if (isEscapedModel()) {
    return (WSRPConsumerPortlet)this;
  }
 else {
    return (WSRPConsumerPortlet)Proxy.newProxyInstance(_classLoader,_escapedModelProxyInterfaces,new AutoEscapeBeanHandler(this));
  }
}
