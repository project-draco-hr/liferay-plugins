{
  if (Validator.isEmailAddress(actorId)) {
    User user=UserLocalServiceUtil.getUserByEmailAddress(companyId,actorId);
    return user.getUserId();
  }
 else {
    return GetterUtil.getLong(actorId);
  }
}
