{
  String subject=preferences.getValue("subject",null);
  if (Validator.isNotNull(subject)) {
    return subject;
  }
  return StringUtil.read(InviteMembersPortlet.class.getResourceAsStream("dependencies/subject.tmpl"));
}
