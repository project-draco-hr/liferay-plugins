{
  String body=preferences.getValue("body",null);
  if (Validator.isNotNull(body)) {
    return body;
  }
  return StringUtil.read(InviteMembersPortlet.class.getResourceAsStream("dependencies/body.tmpl"));
}
