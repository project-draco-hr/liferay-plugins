{
  String encodedClientId=MarketplaceUtil.encodeClientId(companyId,userId,token);
  StringBundler sb=new StringBundler(5);
  sb.append(url);
  sb.append(StringPool.SLASH);
  sb.append(encodedClientId);
  sb.append(StringPool.SLASH);
  sb.append(token);
  return sb.toString();
}
