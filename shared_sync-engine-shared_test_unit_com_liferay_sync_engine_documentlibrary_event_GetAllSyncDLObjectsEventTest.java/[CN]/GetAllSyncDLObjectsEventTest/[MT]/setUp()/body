{
  super.setUp();
  SyncFileService.addSyncFile(null,null,null,FileUtil.getFileKey(syncAccount.getFilePathName()),syncAccount.getFilePathName(),null,"test",0,0,syncAccount.getSyncAccountId(),SyncFile.TYPE_FOLDER);
}
