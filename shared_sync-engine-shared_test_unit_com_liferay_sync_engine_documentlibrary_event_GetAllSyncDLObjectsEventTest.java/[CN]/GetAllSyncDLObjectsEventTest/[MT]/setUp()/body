{
  super.setUp();
  _syncSite=SyncSiteService.addSyncSite(10158,filePathName + "/test-site",10184,syncAccount.getSyncAccountId());
  SyncFileService.addSyncFile(null,null,null,FileUtil.getFileKey(syncAccount.getFilePathName()),syncAccount.getFilePathName(),null,"test",0,_syncSite.getGroupId(),syncAccount.getSyncAccountId(),SyncFile.TYPE_FOLDER);
}
