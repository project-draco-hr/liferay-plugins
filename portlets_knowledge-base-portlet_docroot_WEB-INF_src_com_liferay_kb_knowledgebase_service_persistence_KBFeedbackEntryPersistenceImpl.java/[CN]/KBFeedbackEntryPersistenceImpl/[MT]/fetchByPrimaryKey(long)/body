{
  Session session=null;
  try {
    session=openSession();
    return (KBFeedbackEntry)session.get(KBFeedbackEntryImpl.class,new Long(feedbackEntryId));
  }
 catch (  Exception e) {
    throw processException(e);
  }
 finally {
    closeSession(session);
  }
}
