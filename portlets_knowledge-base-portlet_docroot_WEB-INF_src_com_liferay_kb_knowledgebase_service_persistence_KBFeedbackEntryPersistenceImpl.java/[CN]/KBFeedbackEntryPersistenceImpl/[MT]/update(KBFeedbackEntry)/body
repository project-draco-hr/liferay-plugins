{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(KBFeedbackEntry kbFeedbackEntry) method. Use update(KBFeedbackEntry kbFeedbackEntry, boolean merge) instead.");
  }
  return update(kbFeedbackEntry,false);
}
