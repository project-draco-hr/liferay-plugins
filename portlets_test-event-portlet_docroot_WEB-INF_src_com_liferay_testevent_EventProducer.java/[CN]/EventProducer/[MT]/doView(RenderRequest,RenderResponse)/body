{
  response.setContentType("text/html");
  PrintWriter writer=response.getWriter();
  try {
    PortletURL actionURL=response.createActionURL();
    writer.write("<a href=\"" + actionURL.toString() + "\">Produce Event</a>");
  }
  finally {
    writer.close();
  }
}
