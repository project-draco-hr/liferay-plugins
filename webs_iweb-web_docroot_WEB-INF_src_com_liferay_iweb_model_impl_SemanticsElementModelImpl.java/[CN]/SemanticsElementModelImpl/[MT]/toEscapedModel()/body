{
  if (isEscapedModel()) {
    return (SemanticsElement)this;
  }
 else {
    SemanticsElement model=new SemanticsElementImpl();
    model.setEscapedModel(true);
    model.setElementURI(HtmlUtil.escape(getElementURI()));
    model.setSemanticsURI(HtmlUtil.escape(getSemanticsURI()));
    model=(SemanticsElement)Proxy.newProxyInstance(SemanticsElement.class.getClassLoader(),new Class[]{SemanticsElement.class},new ReadOnlyBeanHandler(model));
    return model;
  }
}
