{
  ProcessSetup processSetup=getSetup();
  ProcessDao processDao=processSetup.getProcessDao();
  ProcessInstance processInstance=processDao.loadProcessInstance(workflowInstanceId,retrieveChildrenInfo);
  return new WorkflowInstanceInfoImpl(processInstance,retrieveChildrenInfo);
}
