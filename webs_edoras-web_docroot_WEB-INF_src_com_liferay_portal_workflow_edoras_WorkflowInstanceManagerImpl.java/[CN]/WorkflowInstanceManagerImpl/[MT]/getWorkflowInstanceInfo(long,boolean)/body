{
  ProcessInstance processInstance=getSetup().getProcessDao().loadProcessInstance(workflowInstanceId,retrieveChildrenInfo);
  return new WorkflowInstanceInfoImpl(processInstance,retrieveChildrenInfo);
}
