{
  List<? extends MutableProcessInstance> processInstances=getSetup().getProcessDao().loadProcessInstances(relationType,relationId,retrieveChildrenInfo);
  if (processInstances.size() > 0) {
    return new WorkflowInstanceInfoImpl(processInstances.get(0),retrieveChildrenInfo);
  }
  return null;
}
