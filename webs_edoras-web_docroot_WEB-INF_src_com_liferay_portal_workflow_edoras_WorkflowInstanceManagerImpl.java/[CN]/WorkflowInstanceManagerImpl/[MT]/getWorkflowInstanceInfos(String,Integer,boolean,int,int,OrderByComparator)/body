{
  try {
    List<WorkflowInstance> workflowInstances=null;
    if (workflowDefinitionVersion == null) {
      workflowInstances=WorkflowInstanceUtil.findByWorkflowDefinitionName(workflowDefinitionName,start,end,orderByComparator);
    }
 else {
      workflowInstances=WorkflowInstanceUtil.findByN_V(workflowDefinitionName,workflowDefinitionVersion,start,end,orderByComparator);
    }
    return WorkflowManagerUtil.wrapWorkflowInstances(workflowInstances,retrieveChildrenInfo);
  }
 catch (  Exception e) {
    throw new WorkflowException(e.getMessage(),e);
  }
}
