{
  Person person=null;
  try {
    person=new Person("G-" + org.getGroup().getGroupId(),new Name(org.getName() + " (Organization)"));
    if (profileDetails.contains("phone_numbers")) {
      person.setPhoneNumbers(getPhoneNumbers(Organization.class.getName(),org.getOrganizationId()));
    }
    if (profileDetails.contains("gender")) {
      person.setGender(new Enum<Enum.Gender>(Enum.Gender.MALE));
    }
    if (token.getViewerId().equals(person.getId())) {
      person.setIsViewer(true);
    }
    if (token.getOwnerId().equals(person.getId())) {
      person.setIsOwner(true);
    }
  }
 catch (  Exception e) {
    _log.error(e,e);
  }
  return person;
}
