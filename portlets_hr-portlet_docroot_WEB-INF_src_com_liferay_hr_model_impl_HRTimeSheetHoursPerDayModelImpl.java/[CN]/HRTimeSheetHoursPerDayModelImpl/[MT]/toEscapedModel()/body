{
  if (isEscapedModel()) {
    return (HRTimeSheetHoursPerDay)this;
  }
 else {
    return (HRTimeSheetHoursPerDay)Proxy.newProxyInstance(_classLoader,_escapedModelProxyInterfaces,new AutoEscapeBeanHandler(this));
  }
}
