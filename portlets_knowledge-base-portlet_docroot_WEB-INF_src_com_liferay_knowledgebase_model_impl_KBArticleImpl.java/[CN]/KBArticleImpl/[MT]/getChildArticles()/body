{
  List<KBArticle> articles=null;
  try {
    articles=KBArticleLocalServiceUtil.getChildren(getGroupId(),true,getTitle());
  }
 catch (  Exception e) {
    articles=new ArrayList<KBArticle>();
    _log.error(e);
  }
  return articles;
}
