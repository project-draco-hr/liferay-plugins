{
  if (Validator.isNull(getParentTitle())) {
    return null;
  }
  KBArticle article=null;
  try {
    article=KBArticleLocalServiceUtil.getArticle(getGroupId(),getParentTitle());
  }
 catch (  Exception e) {
    _log.error(e);
  }
  return article;
}
