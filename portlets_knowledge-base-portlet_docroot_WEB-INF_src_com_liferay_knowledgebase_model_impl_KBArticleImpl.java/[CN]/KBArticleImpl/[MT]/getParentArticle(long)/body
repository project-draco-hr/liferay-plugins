{
  if (!hasParent()) {
    return null;
  }
  KBArticle article=null;
  try {
    article=KBArticleLocalServiceUtil.getArticle(userId,getParentResourcePrimKey(),true,false);
  }
 catch (  Exception e) {
    _log.error(e);
  }
  return article;
}
