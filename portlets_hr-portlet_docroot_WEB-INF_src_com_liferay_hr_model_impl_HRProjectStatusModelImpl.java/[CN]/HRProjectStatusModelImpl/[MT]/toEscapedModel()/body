{
  if (isEscapedModel()) {
    return (HRProjectStatus)this;
  }
 else {
    return (HRProjectStatus)Proxy.newProxyInstance(_classLoader,_escapedModelProxyInterfaces,new AutoEscapeBeanHandler(this));
  }
}
