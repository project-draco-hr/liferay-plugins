{
  for (  SPSession spSession : spSessions) {
    if (EntityCacheUtil.getResult(SPSessionModelImpl.ENTITY_CACHE_ENABLED,SPSessionImpl.class,spSession.getPrimaryKey(),this) == null) {
      cacheResult(spSession);
    }
  }
}
