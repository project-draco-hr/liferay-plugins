{
  List<MutableProcessJob> processJobs=new ArrayList<MutableProcessJob>();
  if (workflowJobs == null) {
    return processJobs;
  }
  for (  WorkflowJob workflowJob : workflowJobs) {
    processJobs.add(new WorkflowJobBridge(workflowJob));
  }
  return processJobs;
}
