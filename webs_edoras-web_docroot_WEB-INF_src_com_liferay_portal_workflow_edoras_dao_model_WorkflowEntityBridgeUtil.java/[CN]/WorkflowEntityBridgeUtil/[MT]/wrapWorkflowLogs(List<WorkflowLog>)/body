{
  List<AbstractProcessLog> processLogs=new ArrayList<AbstractProcessLog>();
  if (workflowLogs == null) {
    return processLogs;
  }
  for (  WorkflowLog workflowLog : workflowLogs) {
    processLogs.add(wrapWorkflowLog(workflowLog));
  }
  return processLogs;
}
