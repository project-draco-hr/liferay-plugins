{
  String messagePath=getMessagePath(emailAddress,folderName,messageUid);
  FileUtil.deltree(messagePath);
  try {
    User user=getUser();
    Indexer.deleteMessage(user.getCompanyId(),user.getUserId(),emailAddress,folderName,messageUid);
  }
 catch (  Exception e) {
    _log.error(e,e);
  }
}
