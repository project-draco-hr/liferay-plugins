{
  if (!isAccountExists(emailAddress)) {
    return;
  }
  JSONObject jsonMessage=getJSONMessageByUid(emailAddress,folderName,messageUid);
  JSONObject jsonFlags=jsonMessage.getJSONObject("flags");
  jsonFlags.put(flag,value);
  String filePath=getMessageFilePath(emailAddress,folderName,messageUid);
  try {
    FileUtil.write(filePath,jsonMessage.toString());
  }
 catch (  IOException ioe) {
    _log.error(ioe,ioe);
  }
}
