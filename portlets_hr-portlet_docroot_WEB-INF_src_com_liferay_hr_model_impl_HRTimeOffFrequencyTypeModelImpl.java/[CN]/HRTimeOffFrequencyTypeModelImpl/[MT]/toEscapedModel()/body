{
  if (isEscapedModel()) {
    return (HRTimeOffFrequencyType)this;
  }
 else {
    return (HRTimeOffFrequencyType)Proxy.newProxyInstance(HRTimeOffFrequencyType.class.getClassLoader(),new Class[]{HRTimeOffFrequencyType.class},new AutoEscapeBeanHandler(this));
  }
}
