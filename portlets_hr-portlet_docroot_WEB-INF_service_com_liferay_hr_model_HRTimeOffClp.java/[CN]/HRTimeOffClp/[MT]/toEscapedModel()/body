{
  if (isEscapedModel()) {
    return this;
  }
 else {
    return (HRTimeOff)Proxy.newProxyInstance(HRTimeOff.class.getClassLoader(),new Class[]{HRTimeOff.class},new AutoEscapeBeanHandler(this));
  }
}
