{
  ThemeDisplay themeDisplay=(ThemeDisplay)actionRequest.getAttribute(WebKeys.THEME_DISPLAY);
  UserLocalServiceUtil.unsetGroupUsers(themeDisplay.getScopeGroupId(),new long[]{themeDisplay.getUserId()});
}
