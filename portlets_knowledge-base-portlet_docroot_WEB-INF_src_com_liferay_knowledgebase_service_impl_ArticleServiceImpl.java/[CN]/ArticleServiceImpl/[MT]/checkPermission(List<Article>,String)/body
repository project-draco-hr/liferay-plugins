{
  List<Article> articles2=ListUtil.copy(articles);
  Iterator<Article> itr=articles2.iterator();
  while (itr.hasNext()) {
    if (!ArticlePermission.contains(getPermissionChecker(),itr.next(),actionId)) {
      itr.remove();
    }
  }
  return articles2;
}
