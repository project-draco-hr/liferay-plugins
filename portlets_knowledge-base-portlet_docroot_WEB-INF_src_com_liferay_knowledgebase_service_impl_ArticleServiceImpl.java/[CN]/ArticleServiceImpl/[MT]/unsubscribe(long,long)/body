{
  if (resourcePrimKey > 0) {
    ArticlePermission.check(getPermissionChecker(),resourcePrimKey,ActionKeys.SUBSCRIBE);
  }
 else {
    AdminPermission.check(getPermissionChecker(),groupId,ActionKeys.SUBSCRIBE);
  }
  articleLocalService.unsubscribe(groupId,getUserId(),resourcePrimKey);
}
