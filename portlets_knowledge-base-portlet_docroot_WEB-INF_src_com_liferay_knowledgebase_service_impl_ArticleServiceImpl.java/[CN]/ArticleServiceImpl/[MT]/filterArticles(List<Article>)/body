{
  articles=ListUtil.copy(articles);
  Iterator<Article> itr=articles.iterator();
  while (itr.hasNext()) {
    if (!ArticlePermission.contains(getPermissionChecker(),itr.next(),ActionKeys.VIEW)) {
      itr.remove();
    }
  }
  return articles;
}
