{
  Class<?> setupClass=_setupClassMap.get(setupId);
  if (setupClass == null) {
    try {
      setupClass=WorkflowEntityTransferUtil.class.getClassLoader().loadClass(setupId);
      _setupClassMap.put(setupId,setupClass);
    }
 catch (    ClassNotFoundException e) {
      throw new ProcessException("Could not load setup id class [" + setupId + "]",e);
    }
  }
  return setupClass;
}
