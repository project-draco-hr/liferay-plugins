{
  List<ProcessInstance> list=new ArrayList<ProcessInstance>();
  if (instanceList == null) {
    return list;
  }
  for (  WorkflowInstance inst : instanceList) {
    list.add(new WorkflowInstanceImpl(parentInstance,inst,loadChildren));
  }
  return list;
}
