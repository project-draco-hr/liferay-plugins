{
  List<KBFeedbackEntry> feedbackEntries=kbFeedbackEntryPersistence.findByArticleResourcePrimKey(articleResourcePrimKey);
  for (  KBFeedbackEntry feedbackEntry : feedbackEntries) {
    deleteFeedbackEntry(feedbackEntry);
  }
}
