{
  if (isEscapedModel()) {
    return this;
  }
 else {
    return (WSRPProducer)Proxy.newProxyInstance(WSRPProducer.class.getClassLoader(),new Class[]{WSRPProducer.class},new AutoEscapeBeanHandler(this));
  }
}
