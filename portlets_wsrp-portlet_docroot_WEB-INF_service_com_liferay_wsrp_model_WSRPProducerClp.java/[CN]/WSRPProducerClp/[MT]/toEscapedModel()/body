{
  if (isEscapedModel()) {
    return this;
  }
 else {
    WSRPProducer model=new WSRPProducerClp();
    model.setEscapedModel(true);
    model.setWsrpProducerId(getWsrpProducerId());
    model.setCompanyId(HtmlUtil.escape(getCompanyId()));
    model.setCreateDate(getCreateDate());
    model.setModifiedDate(getModifiedDate());
    model.setName(HtmlUtil.escape(getName()));
    model.setPortletIds(HtmlUtil.escape(getPortletIds()));
    model=(WSRPProducer)Proxy.newProxyInstance(WSRPProducer.class.getClassLoader(),new Class[]{WSRPProducer.class},new ReadOnlyBeanHandler(model));
    return model;
  }
}
