{
  int count=countByG_UT(groupId,urlTitle);
  if (count == 0) {
    return null;
  }
  List<KBArticle> list=findByG_UT(groupId,urlTitle,count - 1,count,orderByComparator);
  if (!list.isEmpty()) {
    return list.get(0);
  }
  return null;
}
