{
  if (!InlineSQLHelperUtil.isEnabled(groupId)) {
    return findByG_UT_PrevAndNext(kbArticleId,groupId,urlTitle,orderByComparator);
  }
  KBArticle kbArticle=findByPrimaryKey(kbArticleId);
  Session session=null;
  try {
    session=openSession();
    KBArticle[] array=new KBArticleImpl[3];
    array[0]=filterGetByG_UT_PrevAndNext(session,kbArticle,groupId,urlTitle,orderByComparator,true);
    array[1]=kbArticle;
    array[2]=filterGetByG_UT_PrevAndNext(session,kbArticle,groupId,urlTitle,orderByComparator,false);
    return array;
  }
 catch (  Exception e) {
    throw processException(e);
  }
 finally {
    closeSession(session);
  }
}
