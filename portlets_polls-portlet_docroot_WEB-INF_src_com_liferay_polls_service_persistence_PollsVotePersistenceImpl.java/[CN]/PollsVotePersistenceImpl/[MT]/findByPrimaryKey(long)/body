{
  PollsVote pollsVote=fetchByPrimaryKey(pollsVoteId);
  if (pollsVote == null) {
    if (_log.isWarnEnabled()) {
      _log.warn(_NO_SUCH_ENTITY_WITH_PRIMARY_KEY + pollsVoteId);
    }
    throw new NoSuchVoteException(_NO_SUCH_ENTITY_WITH_PRIMARY_KEY + pollsVoteId);
  }
  return pollsVote;
}
