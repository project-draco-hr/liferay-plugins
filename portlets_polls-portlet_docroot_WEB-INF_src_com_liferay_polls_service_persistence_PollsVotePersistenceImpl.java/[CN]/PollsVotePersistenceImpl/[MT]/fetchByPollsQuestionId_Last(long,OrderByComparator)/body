{
  int count=countByPollsQuestionId(pollsQuestionId);
  List<PollsVote> list=findByPollsQuestionId(pollsQuestionId,count - 1,count,orderByComparator);
  if (!list.isEmpty()) {
    return list.get(0);
  }
  return null;
}
