{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(AssetType assetType) method. Use update(AssetType assetType, boolean merge) instead.");
  }
  return update(assetType,false);
}
