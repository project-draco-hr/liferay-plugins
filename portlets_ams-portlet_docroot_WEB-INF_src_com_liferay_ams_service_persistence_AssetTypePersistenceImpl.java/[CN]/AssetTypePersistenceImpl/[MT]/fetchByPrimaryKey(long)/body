{
  Session session=null;
  try {
    session=openSession();
    return (AssetType)session.get(AssetTypeImpl.class,new Long(assetTypeId));
  }
 catch (  Exception e) {
    throw processException(e);
  }
 finally {
    closeSession(session);
  }
}
