{
  int index=value.indexOf(",");
  if (index == -1) {
    throw new IllegalArgumentException("Invalid component text: " + value);
  }
  return new Component(value.substring(0,index),value.substring(index + 1,value.length()));
}
