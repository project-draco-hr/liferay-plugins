{
  int count=countByPlatform(platform);
  if (count == 0) {
    return null;
  }
  List<PushNotificationsDevice> list=findByPlatform(platform,count - 1,count,orderByComparator);
  if (!list.isEmpty()) {
    return list.get(0);
  }
  return null;
}
