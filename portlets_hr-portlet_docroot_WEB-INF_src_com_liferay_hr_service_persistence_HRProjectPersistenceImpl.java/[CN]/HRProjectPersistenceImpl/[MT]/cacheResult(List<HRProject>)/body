{
  for (  HRProject hrProject : hrProjects) {
    if (EntityCacheUtil.getResult(HRProjectModelImpl.ENTITY_CACHE_ENABLED,HRProjectImpl.class,hrProject.getPrimaryKey(),this) == null) {
      cacheResult(hrProject);
    }
  }
}
