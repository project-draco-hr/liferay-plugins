{
  KaleoDefinition kaleoDefinition=getLatestKaleoDefinition(name,serviceContext);
  return addKaleoDefinition(kaleoDefinition.getName(),title,kaleoDefinition.getDescription(),kaleoDefinition.getVersion() + 1,serviceContext);
}
