{
  KaleoDefinition kaleoDefinition=getLatestKaleoDefinition(name,serviceContext);
  return addKaleoDefinition(kaleoDefinition.getName(),kaleoDefinition.getDescription(),kaleoDefinition.getVersion() + 1,serviceContext);
}
