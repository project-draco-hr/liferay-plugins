{
  int kbArticleCount=KBArticleServiceUtil.getKBArticlesCount(getGroupId(),getKbFolderId(),WorkflowConstants.STATUS_ANY);
  if (kbArticleCount > 0) {
    return false;
  }
  int kbFolderCount=KBFolderServiceUtil.getKBFoldersCount(getGroupId(),getKbFolderId());
  return kbFolderCount == 0;
}
