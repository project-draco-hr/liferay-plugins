{
  String value=super.getString(container,property);
  if (Validator.isNotNull(value)) {
    value=StringUtil.replace(value,new String[]{"%scheme%","%host%"},new String[]{ShindigUtil.getScheme(),ShindigUtil.getHost()});
  }
  return value;
}
