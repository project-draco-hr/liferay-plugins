{
  MeetupsEntry meetupsEntry=fetchByPrimaryKey(meetupsEntryId);
  if (meetupsEntry == null) {
    if (_log.isWarnEnabled()) {
      _log.warn("No MeetupsEntry exists with the primary key " + meetupsEntryId);
    }
    throw new NoSuchMeetupsEntryException("No MeetupsEntry exists with the primary key " + meetupsEntryId);
  }
  return meetupsEntry;
}
