{
  Object[] finderArgs=new Object[]{pk,hrHolidayPK};
  Boolean value=(Boolean)FinderCacheUtil.getResult(FINDER_PATH_CONTAINS_HRHOLIDAY,finderArgs,this);
  if (value == null) {
    try {
      value=Boolean.valueOf(containsHRHoliday.contains(pk,hrHolidayPK));
    }
 catch (    Exception e) {
      throw processException(e);
    }
 finally {
      if (value == null) {
        value=Boolean.FALSE;
      }
      FinderCacheUtil.putResult(FINDER_PATH_CONTAINS_HRHOLIDAY,finderArgs,value);
    }
  }
  return value.booleanValue();
}
