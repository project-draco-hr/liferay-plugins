{
  for (  HROffice hrOffice : hrOffices) {
    if (EntityCacheUtil.getResult(HROfficeModelImpl.ENTITY_CACHE_ENABLED,HROfficeImpl.class,hrOffice.getPrimaryKey(),this) == null) {
      cacheResult(hrOffice);
    }
  }
}
