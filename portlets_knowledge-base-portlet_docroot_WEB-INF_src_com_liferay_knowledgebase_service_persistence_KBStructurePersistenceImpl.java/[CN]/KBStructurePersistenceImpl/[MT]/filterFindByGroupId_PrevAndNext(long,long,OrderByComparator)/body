{
  if (!InlineSQLHelperUtil.isEnabled(groupId)) {
    return findByGroupId_PrevAndNext(kbStructureId,groupId,orderByComparator);
  }
  KBStructure kbStructure=findByPrimaryKey(kbStructureId);
  Session session=null;
  try {
    session=openSession();
    KBStructure[] array=new KBStructureImpl[3];
    array[0]=filterGetByGroupId_PrevAndNext(session,kbStructure,groupId,orderByComparator,true);
    array[1]=kbStructure;
    array[2]=filterGetByGroupId_PrevAndNext(session,kbStructure,groupId,orderByComparator,false);
    return array;
  }
 catch (  Exception e) {
    throw processException(e);
  }
 finally {
    closeSession(session);
  }
}
