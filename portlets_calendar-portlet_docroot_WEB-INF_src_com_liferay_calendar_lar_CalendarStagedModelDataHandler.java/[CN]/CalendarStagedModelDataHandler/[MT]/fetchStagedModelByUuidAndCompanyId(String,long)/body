{
  List<Calendar> calendars=CalendarLocalServiceUtil.getCalendarsByUuidAndCompanyId(uuid,companyId,QueryUtil.ALL_POS,QueryUtil.ALL_POS,new StagedModelModifiedDateComparator<Calendar>());
  if (ListUtil.isEmpty(calendars)) {
    return null;
  }
  return calendars.get(0);
}
