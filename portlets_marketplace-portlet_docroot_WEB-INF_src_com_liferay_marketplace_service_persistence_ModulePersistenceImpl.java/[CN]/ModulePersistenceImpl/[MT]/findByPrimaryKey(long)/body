{
  Module module=fetchByPrimaryKey(moduleId);
  if (module == null) {
    if (_log.isWarnEnabled()) {
      _log.warn(_NO_SUCH_ENTITY_WITH_PRIMARY_KEY + moduleId);
    }
    throw new NoSuchModuleException(_NO_SUCH_ENTITY_WITH_PRIMARY_KEY + moduleId);
  }
  return module;
}
