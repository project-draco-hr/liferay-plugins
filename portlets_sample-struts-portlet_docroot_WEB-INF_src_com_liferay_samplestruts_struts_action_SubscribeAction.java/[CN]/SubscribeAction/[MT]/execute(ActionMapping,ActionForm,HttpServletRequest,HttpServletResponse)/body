{
  SubscribeForm subscribeForm=(SubscribeForm)actionForm;
  if (_log.isInfoEnabled()) {
    _log.info(subscribeForm.toString());
  }
  return actionMapping.findForward("/sample_struts_portlet/subscribe_success");
}
