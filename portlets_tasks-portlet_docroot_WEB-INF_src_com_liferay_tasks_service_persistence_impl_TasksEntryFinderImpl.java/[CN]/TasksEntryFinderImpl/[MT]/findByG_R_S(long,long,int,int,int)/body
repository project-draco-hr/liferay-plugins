{
  if (status != TasksEntryConstants.STATUS_ALL) {
    if (groupId > 0) {
      return TasksEntryUtil.findByG_U_S(groupId,reporterUserId,getStatuses(status),start,end);
    }
    return TasksEntryUtil.findByU_S(reporterUserId,getStatuses(status),start,end);
  }
  if (groupId > 0) {
    return TasksEntryUtil.findByG_U(groupId,reporterUserId,start,end);
  }
  return TasksEntryUtil.findByUserId(reporterUserId,start,end);
}
