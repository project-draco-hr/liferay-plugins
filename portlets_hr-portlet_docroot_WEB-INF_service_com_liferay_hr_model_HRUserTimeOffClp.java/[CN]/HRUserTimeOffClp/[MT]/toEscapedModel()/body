{
  if (isEscapedModel()) {
    return this;
  }
 else {
    return (HRUserTimeOff)Proxy.newProxyInstance(HRUserTimeOff.class.getClassLoader(),new Class[]{HRUserTimeOff.class},new AutoEscapeBeanHandler(this));
  }
}
