{
  String name=file.getName();
  String fileName=file.getName();
  if (Validator.isNull(name)) {
    name=fileName;
  }
  ServiceContext serviceContext=importerContext.getServiceContext();
  FileEntry fileEntry=DLAppServiceUtil.addFileEntry(folder.getRepositoryId(),folder.getFolderId(),name,fileName,name,StringPool.BLANK,StringPool.BLANK,file,serviceContext);
  importerContext.addFileEntry(name,fileEntry);
  return fileEntry;
}
