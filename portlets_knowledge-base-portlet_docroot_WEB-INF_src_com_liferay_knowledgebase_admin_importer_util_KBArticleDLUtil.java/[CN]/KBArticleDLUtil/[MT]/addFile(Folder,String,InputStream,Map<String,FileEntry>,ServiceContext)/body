{
  String mimeType=MimeTypesUtil.getContentType(fileName);
  FileEntry fileEntry=DLAppServiceUtil.addFileEntry(folder.getRepositoryId(),folder.getFolderId(),fileName,mimeType,fileName,StringPool.BLANK,StringPool.BLANK,inputStream,0,serviceContext);
  fileEntriesMap.put(fileName,fileEntry);
  return fileEntry;
}
