{
  for (  HRUser hrUser : hrUsers) {
    if (EntityCacheUtil.getResult(HRUserModelImpl.ENTITY_CACHE_ENABLED,HRUserImpl.class,hrUser.getPrimaryKey(),this) == null) {
      cacheResult(hrUser);
    }
  }
}
