{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(WorkflowJob workflowJob) method. Use update(WorkflowJob workflowJob, boolean merge) instead.");
  }
  return update(workflowJob,false);
}
