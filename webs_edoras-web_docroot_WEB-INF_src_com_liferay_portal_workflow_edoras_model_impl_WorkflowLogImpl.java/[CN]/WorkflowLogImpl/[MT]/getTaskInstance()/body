{
  try {
    return WorkflowTaskUtil.findByPrimaryKey(getWorkflowTaskId());
  }
 catch (  Exception e) {
    throw new ProcessException("Could not fetch the workflow task for log with id [" + getPrimaryKey() + "]",e);
  }
}
