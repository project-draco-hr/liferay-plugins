{
  HRAssetVendorIdSoap[][] soapModels=null;
  if (models.length > 0) {
    soapModels=new HRAssetVendorIdSoap[models.length][models[0].length];
  }
 else {
    soapModels=new HRAssetVendorIdSoap[0][0];
  }
  for (int i=0; i < models.length; i++) {
    soapModels[i]=toSoapModels(models[i]);
  }
  return soapModels;
}
