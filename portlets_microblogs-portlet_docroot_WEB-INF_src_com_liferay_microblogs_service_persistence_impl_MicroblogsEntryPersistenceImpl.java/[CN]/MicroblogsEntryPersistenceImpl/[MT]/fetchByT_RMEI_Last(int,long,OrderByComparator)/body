{
  int count=countByT_RMEI(type,receiverMicroblogsEntryId);
  if (count == 0) {
    return null;
  }
  List<MicroblogsEntry> list=findByT_RMEI(type,receiverMicroblogsEntryId,count - 1,count,orderByComparator);
  if (!list.isEmpty()) {
    return list.get(0);
  }
  return null;
}
