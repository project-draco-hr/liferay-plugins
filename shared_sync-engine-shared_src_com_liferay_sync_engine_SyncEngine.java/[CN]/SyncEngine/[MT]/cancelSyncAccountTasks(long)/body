{
  Object[] syncAccountTasks=_syncAccountTasks.get(syncAccountId);
  ScheduledFuture<?> scheduledFuture=(ScheduledFuture<?>)syncAccountTasks[0];
  scheduledFuture.cancel(false);
  Watcher watcher=(Watcher)syncAccountTasks[1];
  watcher.close();
}
