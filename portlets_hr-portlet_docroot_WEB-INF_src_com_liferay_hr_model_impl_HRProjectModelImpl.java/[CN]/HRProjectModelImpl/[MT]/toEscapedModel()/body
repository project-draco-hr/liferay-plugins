{
  if (isEscapedModel()) {
    return (HRProject)this;
  }
 else {
    return (HRProject)Proxy.newProxyInstance(HRProject.class.getClassLoader(),new Class[]{HRProject.class},new AutoEscapeBeanHandler(this));
  }
}
