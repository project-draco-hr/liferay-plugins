{
  try {
    WorkflowTaskUtil.removeByWorkflowInstanceId(processInstance.getPrimaryKey());
  }
 catch (  SystemException e) {
    throw new ProcessException("Could not remove tasks for process instance with id [" + processInstance.getPrimaryKey() + "]",e);
  }
}
