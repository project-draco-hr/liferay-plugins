{
  ProcessTask processTask=(ProcessTask)workflowTask;
  try {
    WorkflowTaskUtil.remove(processTask.getPrimaryKey());
  }
 catch (  Exception e) {
    throw new ProcessException("Could not delete workflow task with id [" + processTask.getPrimaryKey() + "]",e);
  }
}
