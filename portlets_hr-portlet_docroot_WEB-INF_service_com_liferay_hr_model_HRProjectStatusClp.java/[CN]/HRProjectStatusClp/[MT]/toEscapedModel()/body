{
  if (isEscapedModel()) {
    return this;
  }
 else {
    return (HRProjectStatus)Proxy.newProxyInstance(HRProjectStatus.class.getClassLoader(),new Class[]{HRProjectStatus.class},new AutoEscapeBeanHandler(this));
  }
}
