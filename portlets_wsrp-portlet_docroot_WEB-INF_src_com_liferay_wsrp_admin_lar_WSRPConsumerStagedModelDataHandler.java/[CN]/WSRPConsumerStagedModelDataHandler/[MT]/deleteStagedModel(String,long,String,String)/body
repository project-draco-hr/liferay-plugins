{
  Group group=GroupLocalServiceUtil.getGroup(groupId);
  WSRPConsumer consumer=WSRPConsumerLocalServiceUtil.fetchWSRPConsumerByUuidAndCompanyId(uuid,group.getCompanyId());
  if (consumer != null) {
    WSRPConsumerLocalServiceUtil.deleteWSRPConsumer(consumer);
  }
}
