{
  if (isEscapedModel()) {
    return (KaleoDefinition)this;
  }
 else {
    return (KaleoDefinition)Proxy.newProxyInstance(_classLoader,_escapedModelProxyInterfaces,new AutoEscapeBeanHandler(this));
  }
}
