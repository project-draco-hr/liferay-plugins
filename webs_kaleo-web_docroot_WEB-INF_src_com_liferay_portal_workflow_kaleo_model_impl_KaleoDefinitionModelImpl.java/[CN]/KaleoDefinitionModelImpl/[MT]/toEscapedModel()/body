{
  if (isEscapedModel()) {
    return (KaleoDefinition)this;
  }
 else {
    return (KaleoDefinition)Proxy.newProxyInstance(KaleoDefinition.class.getClassLoader(),new Class[]{KaleoDefinition.class},new AutoEscapeBeanHandler(this));
  }
}
