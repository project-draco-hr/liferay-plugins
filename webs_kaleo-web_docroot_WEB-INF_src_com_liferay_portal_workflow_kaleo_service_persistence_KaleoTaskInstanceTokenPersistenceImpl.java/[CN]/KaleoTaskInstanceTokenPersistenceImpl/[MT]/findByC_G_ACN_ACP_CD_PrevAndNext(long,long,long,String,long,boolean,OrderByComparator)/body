{
  KaleoTaskInstanceToken kaleoTaskInstanceToken=findByPrimaryKey(kaleoTaskInstanceTokenId);
  Session session=null;
  try {
    session=openSession();
    KaleoTaskInstanceToken[] array=new KaleoTaskInstanceTokenImpl[3];
    array[0]=getByC_G_ACN_ACP_CD_PrevAndNext(session,kaleoTaskInstanceToken,companyId,groupId,assigneeClassName,assigneeClassPK,completed,orderByComparator,true);
    array[1]=kaleoTaskInstanceToken;
    array[2]=getByC_G_ACN_ACP_CD_PrevAndNext(session,kaleoTaskInstanceToken,companyId,groupId,assigneeClassName,assigneeClassPK,completed,orderByComparator,false);
    return array;
  }
 catch (  Exception e) {
    throw processException(e);
  }
 finally {
    closeSession(session);
  }
}
