{
  KaleoTaskInstanceToken kaleoTaskInstanceToken=findByPrimaryKey(kaleoTaskInstanceTokenId);
  Session session=null;
  try {
    session=openSession();
    KaleoTaskInstanceToken[] array=new KaleoTaskInstanceTokenImpl[3];
    array[0]=getByG_C_ACN_ACP_C_PrevAndNext(session,kaleoTaskInstanceToken,groupId,companyId,assigneeClassName,assigneeClassPK,completed,orderByComparator,true);
    array[1]=kaleoTaskInstanceToken;
    array[2]=getByG_C_ACN_ACP_C_PrevAndNext(session,kaleoTaskInstanceToken,groupId,companyId,assigneeClassName,assigneeClassPK,completed,orderByComparator,false);
    return array;
  }
 catch (  Exception e) {
    throw processException(e);
  }
 finally {
    closeSession(session);
  }
}
