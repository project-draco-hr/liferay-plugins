{
  RepositoryEntry repositoryEntry=RepositoryEntryUtil.fetchByUUID_G(entryUuid,getGroupId(),true);
  if (repositoryEntry != null) {
    throw new NoSuchRepositoryEntryException("No repository entry exits with {Uuid='" + entryUuid + "}");
  }
  return repositoryEntry.getMappedId();
}
