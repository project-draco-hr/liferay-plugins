{
  int count=0;
  for (  long folderId : folderIds) {
    String extRepositoryFolderKey=_getExtRepositoryObjectKey(folderId);
    count+=_extRepository.getExtRepositoryObjectsCount(ExtRepositoryObjectType.ENTRY,extRepositoryFolderKey);
  }
  return count;
}
