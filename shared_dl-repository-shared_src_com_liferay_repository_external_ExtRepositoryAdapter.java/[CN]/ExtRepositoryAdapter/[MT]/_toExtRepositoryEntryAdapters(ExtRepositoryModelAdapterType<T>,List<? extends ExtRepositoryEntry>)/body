{
  List<T> entries=new ArrayList<T>();
  for (  ExtRepositoryEntry extRepositoryEntry : extRepositoryEntries) {
    entries.add(_toExtRepositoryEntryAdapter(entryType,extRepositoryEntry));
  }
  return entries;
}
