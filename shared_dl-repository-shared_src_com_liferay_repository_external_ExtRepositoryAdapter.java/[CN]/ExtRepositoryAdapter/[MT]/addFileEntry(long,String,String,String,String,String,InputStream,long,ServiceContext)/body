{
  String fileName=null;
  if (Validator.isNull(title)) {
    fileName=sourceFileName;
  }
 else {
    fileName=title;
  }
  String repositoryMappedFolderId=_getRepositoryEntryMappedId(folderId);
  ExtRepositoryFileEntry extRepositoryFileEntry=_extRepository.addFileEntry(repositoryMappedFolderId,mimeType,fileName,description,changeLog,is);
  return _toExtRepositoryEntryAdapter(ExtRepositoryModelAdapterType.FILE,extRepositoryFileEntry);
}
