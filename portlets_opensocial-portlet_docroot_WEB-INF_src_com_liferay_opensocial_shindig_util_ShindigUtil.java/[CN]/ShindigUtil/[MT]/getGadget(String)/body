{
  int pos=portletName.indexOf(StringPool.UNDERLINE,GadgetPortlet.PORTLET_NAME_PREFIX.length());
  long gadgetId=GetterUtil.getLong(portletName.substring(pos + 1));
  com.liferay.opensocial.model.Gadget gadget=GadgetLocalServiceUtil.getGadget(gadgetId);
  return gadget;
}
