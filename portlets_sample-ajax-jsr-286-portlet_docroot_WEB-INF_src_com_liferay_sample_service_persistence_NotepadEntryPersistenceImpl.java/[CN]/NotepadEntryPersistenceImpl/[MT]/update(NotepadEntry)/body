{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(NotepadEntry notepadEntry) method. Use update(NotepadEntry notepadEntry, boolean merge) instead.");
  }
  return update(notepadEntry,false);
}
