{
  Session session=null;
  try {
    session=openSession();
    return (NotepadEntry)session.get(NotepadEntryImpl.class,new Long(notepadEntryId));
  }
 catch (  Exception e) {
    throw HibernateUtil.processException(e);
  }
 finally {
    closeSession(session);
  }
}
