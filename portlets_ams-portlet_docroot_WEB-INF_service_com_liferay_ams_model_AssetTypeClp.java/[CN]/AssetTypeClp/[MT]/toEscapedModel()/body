{
  if (isEscapedModel()) {
    return this;
  }
 else {
    AssetType model=new AssetTypeClp();
    model.setEscapedModel(true);
    model.setAssetTypeId(getAssetTypeId());
    model.setGroupId(getGroupId());
    model.setName(HtmlUtil.escape(getName()));
    model=(AssetType)Proxy.newProxyInstance(AssetType.class.getClassLoader(),new Class[]{AssetType.class},new ReadOnlyBeanHandler(model));
    return model;
  }
}
