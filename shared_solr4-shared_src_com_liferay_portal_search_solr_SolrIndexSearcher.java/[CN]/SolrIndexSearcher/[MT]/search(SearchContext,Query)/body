{
  try {
    return doSearch(searchContext,query);
  }
 catch (  Exception e) {
    _log.error(e,e);
    if (!_swallowException) {
      throw new SearchException(e.getMessage());
    }
    return new HitsImpl();
  }
}
