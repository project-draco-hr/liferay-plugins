{
  boolean allResults=false;
  if ((searchContext.getStart() == QueryUtil.ALL_POS) && (searchContext.getEnd() == QueryUtil.ALL_POS)) {
    allResults=true;
  }
  return subset(searchContext,query,query.getQueryConfig(),queryResponse,allResults);
}
