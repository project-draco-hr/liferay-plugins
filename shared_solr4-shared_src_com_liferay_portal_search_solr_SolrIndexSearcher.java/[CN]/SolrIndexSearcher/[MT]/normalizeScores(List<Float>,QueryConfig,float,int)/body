{
  Float[] scoresArray=scores.toArray(new Float[numDocuments]);
  if (queryConfig.isScoreEnabled() && (numDocuments > 0) && (maxScore > 0)) {
    for (int i=0; i < scoresArray.length; i++) {
      scoresArray[i]=scoresArray[i] / maxScore;
    }
  }
  return scoresArray;
}
