{
  if (obj == null) {
    return -1;
  }
  SVNRepositoryImpl svnRepository=(SVNRepositoryImpl)obj;
  int value=0;
  value=getUrl().compareTo(svnRepository.getUrl());
  if (value != 0) {
    return value;
  }
  return 0;
}
