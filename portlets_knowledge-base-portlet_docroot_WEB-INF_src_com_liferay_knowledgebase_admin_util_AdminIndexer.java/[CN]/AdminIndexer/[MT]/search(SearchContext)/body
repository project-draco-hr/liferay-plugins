{
  Hits hits=super.search(searchContext);
  String[] queryTerms=hits.getQueryTerms();
  queryTerms=ArrayUtil.append(queryTerms,splitKeywords(searchContext.getKeywords()));
  hits.setQueryTerms(queryTerms);
  return hits;
}
