{
  String[] values=KnowledgeBaseUtil.splitKeywords(searchContext.getKeywords());
  for (  String value : values) {
    searchQuery.addTerm(Field.TITLE,value,true);
    searchQuery.addTerm(Field.CONTENT,value,true);
  }
}
