{
  KBArticle kbArticle=(KBArticle)obj;
  Document document=new DocumentImpl();
  document.addUID(PORTLET_ID,kbArticle.getResourcePrimKey());
  SearchEngineUtil.deleteDocument(kbArticle.getCompanyId(),document.get(Field.UID));
}
