{
  if (isEscapedModel()) {
    return this;
  }
 else {
    return (HRBillability)Proxy.newProxyInstance(HRBillability.class.getClassLoader(),new Class[]{HRBillability.class},new AutoEscapeBeanHandler(this));
  }
}
