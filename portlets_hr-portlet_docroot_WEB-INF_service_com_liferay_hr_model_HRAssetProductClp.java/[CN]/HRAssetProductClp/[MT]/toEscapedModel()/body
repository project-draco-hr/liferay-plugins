{
  if (isEscapedModel()) {
    return this;
  }
 else {
    return (HRAssetProduct)Proxy.newProxyInstance(HRAssetProduct.class.getClassLoader(),new Class[]{HRAssetProduct.class},new AutoEscapeBeanHandler(this));
  }
}
