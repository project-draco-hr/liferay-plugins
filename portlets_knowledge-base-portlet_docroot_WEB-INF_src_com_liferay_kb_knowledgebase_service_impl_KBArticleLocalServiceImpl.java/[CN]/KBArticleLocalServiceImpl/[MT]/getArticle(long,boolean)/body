{
  List<KBArticle> articles=kbArticlePersistence.findByR_H_D(resourcePrimKey,true,draft,0,1);
  if (articles.size() > 0) {
    return articles.get(0);
  }
 else {
    throw new NoSuchArticleException();
  }
}
