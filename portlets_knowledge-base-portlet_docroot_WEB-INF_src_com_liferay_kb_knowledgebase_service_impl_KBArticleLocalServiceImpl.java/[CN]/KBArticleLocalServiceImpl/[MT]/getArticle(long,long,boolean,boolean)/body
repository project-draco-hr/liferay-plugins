{
  List<KBArticle> articles=kbArticleFinder.findByU_R_H_D(userId,resourcePrimKey,head,draft,0,1);
  if (articles.size() > 0) {
    return articles.get(0);
  }
 else {
    throw new NoSuchArticleException();
  }
}
