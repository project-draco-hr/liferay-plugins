{
  List<KBArticle> articles=kbArticlePersistence.findByG_T_H_D(groupId,title,true,draft,0,1);
  if (articles.size() > 0) {
    return articles.get(0);
  }
 else {
    throw new NoSuchArticleException();
  }
}
