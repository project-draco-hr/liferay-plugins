{
  KBArticle article=null;
  if (version == 0) {
    article=getArticle(resourcePrimKey,draft);
  }
 else {
    article=kbArticlePersistence.findByR_V_D(resourcePrimKey,version,draft);
  }
  return article;
}
