{
  SyncFile syncFile=SyncFileService.addSyncFile(null,null,null,null,filePathName,null,null,parentFolderId,0,syncAccountId,SyncFile.TYPE_FOLDER);
  syncFile.setTypePK(syncFile.getSyncFileId());
  SyncFileService.update(syncFile);
  return syncFile;
}
