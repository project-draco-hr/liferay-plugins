{
  Session session=null;
  try {
    session=openSession();
    return (UserPresence)session.get(UserPresenceImpl.class,new Long(presenceUserId));
  }
 catch (  Exception e) {
    throw processException(e);
  }
 finally {
    closeSession(session);
  }
}
