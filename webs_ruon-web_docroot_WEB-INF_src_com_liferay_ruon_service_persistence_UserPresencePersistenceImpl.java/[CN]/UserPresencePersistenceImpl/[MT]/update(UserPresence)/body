{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(UserPresence userPresence) method. Use update(UserPresence userPresence, boolean merge) instead.");
  }
  return update(userPresence,false);
}
