{
  KBTemplate kbTemplate1=(KBTemplate)obj1;
  KBTemplate kbTemplate2=(KBTemplate)obj2;
  int value=kbTemplate1.getUserName().toLowerCase().compareTo(kbTemplate2.getUserName().toLowerCase());
  if (_ascending) {
    return value;
  }
 else {
    return -value;
  }
}
