{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(InterestGroup interestGroup) method. Use update(InterestGroup interestGroup, boolean merge) instead.");
  }
  return update(interestGroup,false);
}
