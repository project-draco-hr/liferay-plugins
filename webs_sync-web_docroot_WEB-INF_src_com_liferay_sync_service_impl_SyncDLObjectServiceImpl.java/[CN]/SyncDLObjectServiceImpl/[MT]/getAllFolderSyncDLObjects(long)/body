{
  try {
    SyncUtil.checkSyncEnabled(repositoryId);
    repositoryService.checkRepository(repositoryId);
    List<SyncDLObject> syncDLObjects=syncDLObjectPersistence.findByR_T(repositoryId,SyncConstants.TYPE_FOLDER);
    return checkSyncDLObjects(syncDLObjects,repositoryId,false);
  }
 catch (  PortalException pe) {
    throw new PortalException(SyncUtil.buildExceptionMessage(pe),pe);
  }
}
