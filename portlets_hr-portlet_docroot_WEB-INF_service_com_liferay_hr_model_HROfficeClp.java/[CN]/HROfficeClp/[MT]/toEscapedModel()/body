{
  if (isEscapedModel()) {
    return this;
  }
 else {
    return (HROffice)Proxy.newProxyInstance(HROffice.class.getClassLoader(),new Class[]{HROffice.class},new AutoEscapeBeanHandler(this));
  }
}
