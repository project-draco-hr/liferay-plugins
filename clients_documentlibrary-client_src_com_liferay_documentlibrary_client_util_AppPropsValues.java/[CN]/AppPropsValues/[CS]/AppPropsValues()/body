{
  _properties=new Properties();
  InputStream is=AppPropsValues.class.getResourceAsStream("/app.properties");
  try {
    _properties.load(is);
  }
 catch (  Exception e) {
    _log.error(e,e);
  }
}
