{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(MeetupRegistration meetupRegistration) method. Use update(MeetupRegistration meetupRegistration, boolean merge) instead.");
  }
  return update(meetupRegistration,false);
}
