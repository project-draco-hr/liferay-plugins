{
  Class<?> oldModelClass=oldModel.getClass();
  String oldModelClassName=oldModelClass.getName();
  if (oldModelClassName.equals(AssetSharingEntryClp.class.getName())) {
    return translateInputAssetSharingEntry(oldModel);
  }
  return oldModel;
}
