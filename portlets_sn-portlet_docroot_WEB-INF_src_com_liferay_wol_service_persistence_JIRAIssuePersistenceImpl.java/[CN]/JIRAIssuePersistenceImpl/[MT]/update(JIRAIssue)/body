{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(JIRAIssue jiraIssue) method. Use update(JIRAIssue jiraIssue, boolean merge) instead.");
  }
  return update(jiraIssue,false);
}
