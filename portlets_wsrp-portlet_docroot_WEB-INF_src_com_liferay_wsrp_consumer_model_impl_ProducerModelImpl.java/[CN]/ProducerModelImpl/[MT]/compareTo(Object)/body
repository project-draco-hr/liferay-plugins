{
  if (obj == null) {
    return -1;
  }
  ProducerImpl producer=(ProducerImpl)obj;
  int value=0;
  value=getName().toLowerCase().compareTo(producer.getName().toLowerCase());
  if (value != 0) {
    return value;
  }
  return 0;
}
