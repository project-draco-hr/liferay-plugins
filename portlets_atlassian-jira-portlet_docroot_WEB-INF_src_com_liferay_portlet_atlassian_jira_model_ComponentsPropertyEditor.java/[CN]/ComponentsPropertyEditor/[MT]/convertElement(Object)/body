{
  if (element == null) {
    return null;
  }
 else   if (element instanceof Component) {
    return element;
  }
  if (element instanceof String) {
    return Component.convert((String)element);
  }
  throw new IllegalArgumentException("Invalid components element type: " + element);
}
