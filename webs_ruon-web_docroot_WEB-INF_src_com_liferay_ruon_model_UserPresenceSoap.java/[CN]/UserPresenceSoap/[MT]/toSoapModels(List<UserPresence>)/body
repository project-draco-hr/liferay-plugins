{
  List<UserPresenceSoap> soapModels=new ArrayList<UserPresenceSoap>(models.size());
  for (  UserPresence model : models) {
    soapModels.add(toSoapModel(model));
  }
  return soapModels.toArray(new UserPresenceSoap[soapModels.size()]);
}
