{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(ProjectsEntry projectsEntry) method. Use update(ProjectsEntry projectsEntry, boolean merge) instead.");
  }
  return update(projectsEntry,false);
}
