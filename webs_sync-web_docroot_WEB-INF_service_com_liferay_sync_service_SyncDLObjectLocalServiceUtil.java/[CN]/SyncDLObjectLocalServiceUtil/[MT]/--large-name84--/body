{
  return getService().addSyncDLObject(companyId,modifiedTime,repositoryId,parentFolderId,name,extension,mimeType,description,changeLog,extraSettings,version,size,checksum,event,lockExpirationDate,lockUserId,lockUserName,type,typePK,typeUuid);
}
