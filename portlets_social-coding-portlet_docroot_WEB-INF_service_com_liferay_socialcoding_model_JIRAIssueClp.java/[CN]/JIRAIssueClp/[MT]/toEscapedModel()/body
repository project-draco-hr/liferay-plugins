{
  if (isEscapedModel()) {
    return this;
  }
 else {
    return (JIRAIssue)Proxy.newProxyInstance(JIRAIssue.class.getClassLoader(),new Class[]{JIRAIssue.class},new AutoEscapeBeanHandler(this));
  }
}
