{
  for (  UserThread userThread : userThreads) {
    if (EntityCacheUtil.getResult(UserThreadModelImpl.ENTITY_CACHE_ENABLED,UserThreadImpl.class,userThread.getPrimaryKey(),this) == null) {
      cacheResult(userThread);
    }
  }
}
