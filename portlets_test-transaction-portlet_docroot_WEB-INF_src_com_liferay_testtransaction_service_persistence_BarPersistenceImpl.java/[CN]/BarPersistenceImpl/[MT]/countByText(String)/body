{
  Object[] finderArgs=new Object[]{text};
  Long count=(Long)FinderCacheUtil.getResult(FINDER_PATH_COUNT_BY_TEXT,finderArgs,this);
  if (count == null) {
    StringBundler query=new StringBundler(2);
    query.append(_SQL_COUNT_BAR_WHERE);
    if (text == null) {
      query.append(_FINDER_COLUMN_TEXT_TEXT_1);
    }
 else {
      if (text.equals(StringPool.BLANK)) {
        query.append(_FINDER_COLUMN_TEXT_TEXT_3);
      }
 else {
        query.append(_FINDER_COLUMN_TEXT_TEXT_2);
      }
    }
    String sql=query.toString();
    Session session=null;
    try {
      session=openSession();
      Query q=session.createQuery(sql);
      QueryPos qPos=QueryPos.getInstance(q);
      if (text != null) {
        qPos.add(text);
      }
      count=(Long)q.uniqueResult();
    }
 catch (    Exception e) {
      throw processException(e);
    }
 finally {
      if (count == null) {
        count=Long.valueOf(0);
      }
      FinderCacheUtil.putResult(FINDER_PATH_COUNT_BY_TEXT,finderArgs,count);
      closeSession(session);
    }
  }
  return count.intValue();
}
