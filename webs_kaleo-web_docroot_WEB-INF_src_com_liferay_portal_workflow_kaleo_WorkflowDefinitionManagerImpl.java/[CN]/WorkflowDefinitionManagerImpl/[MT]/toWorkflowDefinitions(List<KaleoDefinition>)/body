{
  List<WorkflowDefinition> workflowDefinitions=new ArrayList<WorkflowDefinition>(kaleoDefinitions.size());
  for (  KaleoDefinition kaleoDefinition : kaleoDefinitions) {
    workflowDefinitions.add(new WorkflowDefinitionAdapter(kaleoDefinition));
  }
  return workflowDefinitions;
}
