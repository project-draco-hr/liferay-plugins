{
  response.setContentType("text/html");
  PrintWriter writer=response.getWriter();
  writer.println("NamespaceURI:" + request.getParameter("namespaceURI"));
  writer.println("<br>LocalPart:" + request.getParameter("localpart"));
  writer.println("<br>EventValue:" + request.getParameter("key"));
  writer.println("<br>PublicRenderValue:" + request.getParameter("defaultrender1"));
}
