{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(PresenceStatus presenceStatus) method. Use update(PresenceStatus presenceStatus, boolean merge) instead.");
  }
  return update(presenceStatus,false);
}
