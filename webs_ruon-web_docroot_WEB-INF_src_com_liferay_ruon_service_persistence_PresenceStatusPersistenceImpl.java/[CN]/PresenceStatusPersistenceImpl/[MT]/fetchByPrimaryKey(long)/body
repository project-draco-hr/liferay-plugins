{
  Session session=null;
  try {
    session=openSession();
    return (PresenceStatus)session.get(PresenceStatusImpl.class,new Long(presenceStatusId));
  }
 catch (  Exception e) {
    throw processException(e);
  }
 finally {
    closeSession(session);
  }
}
