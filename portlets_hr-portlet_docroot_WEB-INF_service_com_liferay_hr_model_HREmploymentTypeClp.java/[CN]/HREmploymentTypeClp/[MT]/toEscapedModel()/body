{
  if (isEscapedModel()) {
    return this;
  }
 else {
    return (HREmploymentType)Proxy.newProxyInstance(HREmploymentType.class.getClassLoader(),new Class[]{HREmploymentType.class},new AutoEscapeBeanHandler(this));
  }
}
