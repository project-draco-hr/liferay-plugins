{
  Integer type=KaleoConditionModelImpl.TABLE_COLUMNS_MAP.get(columnName);
  if (type == null) {
    throw new IllegalArgumentException("Unknown column name " + columnName + " for table "+ KaleoConditionModelImpl.TABLE_NAME);
  }
  return type;
}
