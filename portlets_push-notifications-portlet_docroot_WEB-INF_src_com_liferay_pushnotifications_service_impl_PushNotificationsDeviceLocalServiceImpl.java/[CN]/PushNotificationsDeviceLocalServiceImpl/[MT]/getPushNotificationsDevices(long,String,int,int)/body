{
  if (userId == _ALL_USERS_USER_ID) {
    return pushNotificationsDevicePersistence.findByPlatform(platform,start,end);
  }
  return pushNotificationsDevicePersistence.findByU_P(userId,platform,start,end);
}
