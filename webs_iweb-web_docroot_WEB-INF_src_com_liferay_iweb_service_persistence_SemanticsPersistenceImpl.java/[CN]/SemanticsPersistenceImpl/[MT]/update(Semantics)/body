{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(Semantics semantics) method. Use update(Semantics semantics, boolean merge) instead.");
  }
  return update(semantics,false);
}
