{
  if (isEscapedModel()) {
    return (KBComment)this;
  }
 else {
    return (KBComment)Proxy.newProxyInstance(KBComment.class.getClassLoader(),new Class[]{KBComment.class},new AutoEscapeBeanHandler(this));
  }
}
