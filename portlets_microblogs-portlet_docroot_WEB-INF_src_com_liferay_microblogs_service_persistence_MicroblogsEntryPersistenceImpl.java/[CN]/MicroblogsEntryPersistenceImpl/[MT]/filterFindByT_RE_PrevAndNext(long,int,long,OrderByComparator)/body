{
  if (!InlineSQLHelperUtil.isEnabled()) {
    return findByT_RE_PrevAndNext(microblogsEntryId,type,receiverEntryId,orderByComparator);
  }
  MicroblogsEntry microblogsEntry=findByPrimaryKey(microblogsEntryId);
  Session session=null;
  try {
    session=openSession();
    MicroblogsEntry[] array=new MicroblogsEntryImpl[3];
    array[0]=filterGetByT_RE_PrevAndNext(session,microblogsEntry,type,receiverEntryId,orderByComparator,true);
    array[1]=microblogsEntry;
    array[2]=filterGetByT_RE_PrevAndNext(session,microblogsEntry,type,receiverEntryId,orderByComparator,false);
    return array;
  }
 catch (  Exception e) {
    throw processException(e);
  }
 finally {
    closeSession(session);
  }
}
