{
  Session session=null;
  try {
    session=openSession();
    return (Producer)session.get(ProducerImpl.class,new Long(producerId));
  }
 catch (  Exception e) {
    throw HibernateUtil.processException(e);
  }
 finally {
    closeSession(session);
  }
}
