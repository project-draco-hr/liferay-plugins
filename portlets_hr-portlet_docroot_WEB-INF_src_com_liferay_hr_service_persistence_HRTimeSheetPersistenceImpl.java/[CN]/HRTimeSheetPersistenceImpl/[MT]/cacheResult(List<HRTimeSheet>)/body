{
  for (  HRTimeSheet hrTimeSheet : hrTimeSheets) {
    if (EntityCacheUtil.getResult(HRTimeSheetModelImpl.ENTITY_CACHE_ENABLED,HRTimeSheetImpl.class,hrTimeSheet.getPrimaryKey()) == null) {
      cacheResult(hrTimeSheet);
    }
  }
}
