{
  if (isEscapedModel()) {
    return (HRWageType)this;
  }
 else {
    return (HRWageType)Proxy.newProxyInstance(HRWageType.class.getClassLoader(),new Class[]{HRWageType.class},new AutoEscapeBeanHandler(this));
  }
}
