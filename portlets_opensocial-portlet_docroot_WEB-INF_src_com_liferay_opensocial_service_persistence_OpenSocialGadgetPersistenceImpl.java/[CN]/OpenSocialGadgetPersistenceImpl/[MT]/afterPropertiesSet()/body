{
  String[] listenerClassNames=StringUtil.split(GetterUtil.getString(com.liferay.util.service.ServiceProps.get("value.object.listener.com.liferay.opensocial.model.OpenSocialGadget")));
  if (listenerClassNames.length > 0) {
    try {
      List<ModelListener<OpenSocialGadget>> listenersList=new ArrayList<ModelListener<OpenSocialGadget>>();
      for (      String listenerClassName : listenerClassNames) {
        listenersList.add((ModelListener<OpenSocialGadget>)Class.forName(listenerClassName).newInstance());
      }
      listeners=listenersList.toArray(new ModelListener[listenersList.size()]);
    }
 catch (    Exception e) {
      _log.error(e);
    }
  }
}
