{
  if (!(object instanceof SyncSite)) {
    return false;
  }
 else   if (object == this) {
    return true;
  }
  SyncSite syncSite=(SyncSite)object;
  if (syncSite.getSyncSiteId() == syncSiteId) {
    return true;
  }
 else {
    return false;
  }
}
