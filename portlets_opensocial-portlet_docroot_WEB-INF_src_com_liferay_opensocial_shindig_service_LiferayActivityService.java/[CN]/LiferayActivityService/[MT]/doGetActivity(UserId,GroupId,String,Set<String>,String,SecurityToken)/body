{
  SocialActivity socialActivity=SocialActivityLocalServiceUtil.getActivity(GetterUtil.getLong(activityId));
  ThemeDisplay themeDisplay=getThemeDisplay(securityToken);
  Activity activity=getActivity(themeDisplay,socialActivity);
  return activity;
}
