{
  String key=PortletPropsValues.ANDROID_API_KEY;
  if (Validator.isNull(key)) {
    if (_log.isWarnEnabled()) {
      _log.warn("The property \"android.api.key\" is not set in " + "portlet.properties");
    }
    return;
  }
  _sender=new Sender(key);
}
