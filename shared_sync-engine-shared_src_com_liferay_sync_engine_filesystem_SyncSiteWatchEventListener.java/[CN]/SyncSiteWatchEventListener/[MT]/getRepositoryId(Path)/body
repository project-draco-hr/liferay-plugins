{
  while (true) {
    filePath=filePath.getParent();
    if (filePath == null) {
      return 0;
    }
    SyncFile syncFile=SyncFileService.fetchSyncFile(filePath.toString(),getSyncAccountId());
    if (syncFile != null) {
      return syncFile.getRepositoryId();
    }
  }
}
