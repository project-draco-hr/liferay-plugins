{
  try {
    SyncWatchEventService.addSyncWatchEvent(eventType,FilePathNameUtil.getFilePathName(filePath),getFileType(eventType,filePath),getSyncAccountId());
  }
 catch (  Exception e) {
    _logger.error(e.getMessage(),e);
  }
}
