{
  try {
    SyncWatchEventService.addSyncWatchEvent(FilePathNameUtil.getFilePathName(filePath),getFileType(filePath,kindName),kindName,getSyncAccountId());
  }
 catch (  Exception e) {
    _logger.error(e.getMessage(),e);
  }
}
