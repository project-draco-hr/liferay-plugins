{
  List<WSRPProducer> wsrpProducers=wsrpProducerPersistence.findByUuid(uuid);
  if (wsrpProducers.isEmpty()) {
    throw new NoSuchProducerException("No producer with uuid: " + uuid);
  }
  return wsrpProducers.get(0);
}
