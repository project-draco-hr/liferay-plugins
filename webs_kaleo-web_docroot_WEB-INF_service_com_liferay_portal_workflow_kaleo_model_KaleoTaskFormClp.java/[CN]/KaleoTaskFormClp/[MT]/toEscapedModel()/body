{
  if (isEscapedModel()) {
    return this;
  }
 else {
    return (KaleoTaskForm)Proxy.newProxyInstance(KaleoTaskForm.class.getClassLoader(),new Class[]{KaleoTaskForm.class},new AutoEscapeBeanHandler(this));
  }
}
