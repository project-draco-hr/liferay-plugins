{
  if (isEscapedModel()) {
    return (AMSAsset)this;
  }
 else {
    return (AMSAsset)Proxy.newProxyInstance(AMSAsset.class.getClassLoader(),new Class[]{AMSAsset.class},new AutoEscapeBeanHandler(this));
  }
}
