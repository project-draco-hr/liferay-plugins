{
  try {
    T localValue=_localImplementationClass.newInstance();
    BeanUtils.copyProperties(remoteValue,localValue,_localImplementationClass);
    return localValue;
  }
 catch (  Exception e) {
    throw new IllegalArgumentException("Could not serialize object",e);
  }
}
