{
  if (permissionChecker.hasOwnerPermission(template.getCompanyId(),Template.class.getName(),template.getTemplateId(),template.getUserId(),actionId)) {
    return true;
  }
  return permissionChecker.hasPermission(template.getGroupId(),Template.class.getName(),template.getTemplateId(),actionId);
}
