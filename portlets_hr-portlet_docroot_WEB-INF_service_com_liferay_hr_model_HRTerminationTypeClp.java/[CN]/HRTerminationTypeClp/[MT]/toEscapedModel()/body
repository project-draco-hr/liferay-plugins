{
  if (isEscapedModel()) {
    return this;
  }
 else {
    return (HRTerminationType)Proxy.newProxyInstance(HRTerminationType.class.getClassLoader(),new Class[]{HRTerminationType.class},new AutoEscapeBeanHandler(this));
  }
}
