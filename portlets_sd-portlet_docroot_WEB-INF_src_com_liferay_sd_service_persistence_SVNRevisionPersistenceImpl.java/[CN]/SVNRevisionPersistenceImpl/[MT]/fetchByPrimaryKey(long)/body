{
  Session session=null;
  try {
    session=openSession();
    return (SVNRevision)session.get(SVNRevisionImpl.class,new Long(svnRevisionId));
  }
 catch (  Exception e) {
    throw processException(e);
  }
 finally {
    closeSession(session);
  }
}
