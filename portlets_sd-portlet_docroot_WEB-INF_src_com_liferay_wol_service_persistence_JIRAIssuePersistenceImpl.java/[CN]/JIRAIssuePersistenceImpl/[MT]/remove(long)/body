{
  Session session=null;
  try {
    session=openSession();
    JIRAIssue jiraIssue=(JIRAIssue)session.get(JIRAIssueImpl.class,new Long(jiraIssueId));
    if (jiraIssue == null) {
      if (_log.isWarnEnabled()) {
        _log.warn("No JIRAIssue exists with the primary key " + jiraIssueId);
      }
      throw new NoSuchJIRAIssueException("No JIRAIssue exists with the primary key " + jiraIssueId);
    }
    return remove(jiraIssue);
  }
 catch (  NoSuchJIRAIssueException nsee) {
    throw nsee;
  }
catch (  Exception e) {
    throw processException(e);
  }
 finally {
    closeSession(session);
  }
}
