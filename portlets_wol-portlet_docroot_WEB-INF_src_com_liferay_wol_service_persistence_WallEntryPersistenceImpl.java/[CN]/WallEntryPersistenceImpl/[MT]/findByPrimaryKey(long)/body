{
  WallEntry wallEntry=fetchByPrimaryKey(wallEntryId);
  if (wallEntry == null) {
    if (_log.isWarnEnabled()) {
      _log.warn("No WallEntry exists with the primary key " + wallEntryId);
    }
    throw new NoSuchWallEntryException("No WallEntry exists with the primary key " + wallEntryId);
  }
  return wallEntry;
}
