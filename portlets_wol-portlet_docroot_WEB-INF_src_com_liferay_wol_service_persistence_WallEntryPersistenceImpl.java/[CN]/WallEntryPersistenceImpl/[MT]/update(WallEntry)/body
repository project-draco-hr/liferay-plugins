{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(WallEntry wallEntry) method. Use update(WallEntry wallEntry, boolean merge) instead.");
  }
  return update(wallEntry,false);
}
