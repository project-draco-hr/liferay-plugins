{
  if (Validator.isNotNull(contact.getAimSn()) || Validator.isNotNull(contact.getIcqSn()) || Validator.isNotNull(contact.getJabberSn())|| Validator.isNotNull(contact.getMsnSn())|| Validator.isNotNull(contact.getSkypeSn())|| Validator.isNotNull(contact.getYmSn())) {
    return true;
  }
  return false;
}
