{
  String redirect=(String)actionRequest.getAttribute(actionResponse.getNamespace() + "redirect");
  if (Validator.isNull(redirect)) {
    return super.getRedirect(actionRequest,actionResponse);
  }
  actionRequest.removeAttribute(actionResponse.getNamespace() + "redirect");
  return redirect;
}
