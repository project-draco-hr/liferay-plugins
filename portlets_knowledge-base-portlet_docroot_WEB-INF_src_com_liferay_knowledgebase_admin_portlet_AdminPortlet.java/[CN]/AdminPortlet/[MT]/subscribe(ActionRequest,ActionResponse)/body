{
  ThemeDisplay themeDisplay=(ThemeDisplay)actionRequest.getAttribute(WebKeys.THEME_DISPLAY);
  String portletId=PortalUtil.getPortletId(actionRequest);
  ArticleServiceUtil.subscribe(themeDisplay.getCompanyId(),themeDisplay.getScopeGroupId(),themeDisplay.getPlid(),portletId,themeDisplay.getScopeGroupId());
}
