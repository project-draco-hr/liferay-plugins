{
  String[] keys=new String[]{NoSuchArticleException.class.getName(),NoSuchMessageException.class.getName(),PrincipalException.class.getName()};
  for (  String key : keys) {
    if (SessionErrors.contains(renderRequest,key)) {
      return true;
    }
  }
  return false;
}
