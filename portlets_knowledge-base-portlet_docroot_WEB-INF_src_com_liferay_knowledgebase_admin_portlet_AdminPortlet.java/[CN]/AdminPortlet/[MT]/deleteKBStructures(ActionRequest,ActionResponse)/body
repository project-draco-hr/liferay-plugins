{
  ThemeDisplay themeDisplay=(ThemeDisplay)actionRequest.getAttribute(WebKeys.THEME_DISPLAY);
  long[] kbStructureIds=StringUtil.split(ParamUtil.getString(actionRequest,"kbStructureIds"),0L);
  KBStructureServiceUtil.deleteKBStructures(themeDisplay.getScopeGroupId(),kbStructureIds);
}
