{
  ThemeDisplay themeDisplay=(ThemeDisplay)actionRequest.getAttribute(WebKeys.THEME_DISPLAY);
  long[] templateIds=StringUtil.split(ParamUtil.getString(actionRequest,"templateIds"),0L);
  TemplateServiceUtil.deleteTemplates(themeDisplay.getScopeGroupId(),templateIds);
}
