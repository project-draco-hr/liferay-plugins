{
  for (  String key : SessionErrors.keySet(renderRequest)) {
    if (key.equals(NoSuchArticleException.class.getName()) || key.equals(PrincipalException.class.getName())) {
      return "/admin/error.jsp";
    }
  }
  return null;
}
