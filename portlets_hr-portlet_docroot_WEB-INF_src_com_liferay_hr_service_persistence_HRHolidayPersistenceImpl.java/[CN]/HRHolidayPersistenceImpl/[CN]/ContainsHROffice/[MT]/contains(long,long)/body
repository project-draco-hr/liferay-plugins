{
  List<Integer> results=_mappingSqlQuery.execute(new Object[]{new Long(hrHolidayId),new Long(hrOfficeId)});
  if (results.size() > 0) {
    Integer count=results.get(0);
    if (count.intValue() > 0) {
      return true;
    }
  }
  return false;
}
