{
  String preferenceName=getPreferenceName(propertyName,notificationType,notificationTemplateType);
  String value=notificationTemplateContext.getString(preferenceName);
  if (Validator.isNotNull(value)) {
    return value;
  }
 else {
    return getTemplateContent(propertyName,notificationType,notificationTemplateType);
  }
}
