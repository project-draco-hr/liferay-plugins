{
  try {
    deleteArticle(companyId,resourcePrimKey);
  }
 catch (  SearchException se) {
  }
  Document doc=getArticleDocument(companyId,groupId,userId,resourcePrimKey,title,content,description,tagsEntries);
  SearchEngineUtil.addDocument(companyId,doc);
}
