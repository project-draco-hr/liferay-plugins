{
  if ((activitySet.getType() == _ACTIVITY_KEY_ADD_COMMENT) || (activitySet.getType() == _ACTIVITY_KEY_UPDATE_PAGE) || (activitySet.getType() == SocialActivityConstants.TYPE_ADD_COMMENT)) {
    return getBody(activitySet.getClassName(),activitySet.getClassPK(),serviceContext);
  }
  return super.getBody(activitySet,serviceContext);
}
