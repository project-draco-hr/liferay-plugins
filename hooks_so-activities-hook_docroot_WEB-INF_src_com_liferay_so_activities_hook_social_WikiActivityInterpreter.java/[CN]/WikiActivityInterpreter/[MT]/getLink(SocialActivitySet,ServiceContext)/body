{
  if (activitySet.getType() == _ACTIVITY_KEY_UPDATE_PAGE) {
    return wrapLink(getLinkURL(activitySet.getClassName(),activitySet.getClassPK(),serviceContext),serviceContext.translate("view-wiki"));
  }
  return null;
}
