{
  FilterConstraint filterConstraint=new FilterConstraint(leftFilterConstraint);
  boolean collision=filterConstraint.merge(rightFilterConstraint);
  if (collision) {
    return null;
  }
 else {
    return filterConstraint;
  }
}
