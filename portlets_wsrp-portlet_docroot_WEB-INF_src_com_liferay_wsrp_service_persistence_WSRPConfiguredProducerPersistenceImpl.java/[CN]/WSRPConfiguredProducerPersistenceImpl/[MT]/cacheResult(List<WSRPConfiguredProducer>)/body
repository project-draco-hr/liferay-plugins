{
  for (  WSRPConfiguredProducer wsrpConfiguredProducer : wsrpConfiguredProducers) {
    if (EntityCacheUtil.getResult(WSRPConfiguredProducerModelImpl.ENTITY_CACHE_ENABLED,WSRPConfiguredProducerImpl.class,wsrpConfiguredProducer.getPrimaryKey(),this) == null) {
      cacheResult(wsrpConfiguredProducer);
    }
  }
}
