{
  String[] listenerClassNames=StringUtil.split(GetterUtil.getString(com.liferay.util.service.ServiceProps.get("value.object.listener.com.liferay.wsrp.model.WSRPConfiguredProducer")));
  if (listenerClassNames.length > 0) {
    try {
      List<ModelListener<WSRPConfiguredProducer>> listenersList=new ArrayList<ModelListener<WSRPConfiguredProducer>>();
      for (      String listenerClassName : listenerClassNames) {
        listenersList.add((ModelListener<WSRPConfiguredProducer>)Class.forName(listenerClassName).newInstance());
      }
      listeners=listenersList.toArray(new ModelListener[listenersList.size()]);
    }
 catch (    Exception e) {
      _log.error(e);
    }
  }
}
