{
  if (isEscapedModel()) {
    return (HRBillability)this;
  }
 else {
    return (HRBillability)Proxy.newProxyInstance(_classLoader,_escapedModelProxyInterfaces,new AutoEscapeBeanHandler(this));
  }
}
