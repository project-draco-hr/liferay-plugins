{
  String[] listenerClassNames=StringUtil.split(GetterUtil.getString(com.liferay.util.service.ServiceProps.get("value.object.listener.com.liferay.calendar.model.CalendarNotificationTemplate")));
  if (listenerClassNames.length > 0) {
    try {
      List<ModelListener<CalendarNotificationTemplate>> listenersList=new ArrayList<ModelListener<CalendarNotificationTemplate>>();
      for (      String listenerClassName : listenerClassNames) {
        listenersList.add((ModelListener<CalendarNotificationTemplate>)InstanceFactory.newInstance(getClassLoader(),listenerClassName));
      }
      listeners=listenersList.toArray(new ModelListener[listenersList.size()]);
    }
 catch (    Exception e) {
      _log.error(e);
    }
  }
}
