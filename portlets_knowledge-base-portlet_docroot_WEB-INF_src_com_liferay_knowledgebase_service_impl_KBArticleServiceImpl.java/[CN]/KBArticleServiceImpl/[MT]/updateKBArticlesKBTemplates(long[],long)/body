{
  if (!KBTemplatePermission.contains(getPermissionChecker(),kbTemplateId,ActionKeys.DELETE) && !KBTemplatePermission.contains(getPermissionChecker(),kbTemplateId,ActionKeys.UPDATE)) {
    throw new PrincipalException();
  }
  kbArticleLocalService.updateKBArticlesKBTemplates(kbArticleIds);
}
