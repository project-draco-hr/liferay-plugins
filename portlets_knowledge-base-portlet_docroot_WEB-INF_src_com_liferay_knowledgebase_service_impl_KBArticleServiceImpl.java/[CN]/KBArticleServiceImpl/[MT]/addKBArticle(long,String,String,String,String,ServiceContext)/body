{
  AdminPermission.check(getPermissionChecker(),serviceContext.getScopeGroupId(),ActionKeys.ADD_KB_ARTICLE);
  return kbArticleLocalService.addKBArticle(getUserId(),parentResourcePrimKey,title,content,description,dirName,serviceContext);
}
