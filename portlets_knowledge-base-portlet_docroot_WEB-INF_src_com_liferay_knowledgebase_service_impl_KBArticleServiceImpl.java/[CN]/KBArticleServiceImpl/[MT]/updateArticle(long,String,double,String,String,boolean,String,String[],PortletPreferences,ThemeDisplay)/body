{
  KBArticlePermission.check(getPermissionChecker(),groupId,title,ActionKeys.UPDATE);
  return kbArticleLocalService.updateArticle(getUserId(),groupId,title,version,content,description,minorEdit,parentTitle,tagsEntries,prefs,themeDisplay);
}
