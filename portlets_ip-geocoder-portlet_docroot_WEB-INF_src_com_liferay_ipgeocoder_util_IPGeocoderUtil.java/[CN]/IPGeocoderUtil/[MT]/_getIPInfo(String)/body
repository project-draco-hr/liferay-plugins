{
  if (_lookupService == null) {
    throw new RuntimeException("MaxMind GeoIP City is not properly installed");
  }
  Location location=_lookupService.getLocation(ipAddress);
  return new IPInfo(ipAddress,location);
}
