{
  try {
    _lookupService=new LookupService(PortletProps.get("maxmind.database.file"),LookupService.GEOIP_MEMORY_CACHE);
  }
 catch (  IOException ioe) {
    _log.error("MaxMind GeoIP City is not properly installed",ioe);
  }
}
