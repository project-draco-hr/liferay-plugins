{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(WorkflowLog workflowLog) method. Use update(WorkflowLog workflowLog, boolean merge) instead.");
  }
  return update(workflowLog,false);
}
