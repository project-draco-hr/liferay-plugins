{
  UnsubscribeForm unsubscribeForm=(UnsubscribeForm)actionForm;
  if (_log.isInfoEnabled()) {
    _log.info(unsubscribeForm.toString());
  }
  return actionMapping.findForward("/sample_struts_portlet/unsubscribe_success");
}
