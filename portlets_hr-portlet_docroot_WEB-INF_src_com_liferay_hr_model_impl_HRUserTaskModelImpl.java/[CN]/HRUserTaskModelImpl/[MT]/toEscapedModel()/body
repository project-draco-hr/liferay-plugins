{
  if (isEscapedModel()) {
    return (HRUserTask)this;
  }
 else {
    return (HRUserTask)Proxy.newProxyInstance(HRUserTask.class.getClassLoader(),new Class[]{HRUserTask.class},new AutoEscapeBeanHandler(this));
  }
}
