{
  return getService().updateArticle(userId,resourcePrimKey,version,title,htmlTitle,content,description,minorEdit,template,draft,parentResourcePrimKey,tagsEntries,categoriesEntries,prefs,themeDisplay);
}
