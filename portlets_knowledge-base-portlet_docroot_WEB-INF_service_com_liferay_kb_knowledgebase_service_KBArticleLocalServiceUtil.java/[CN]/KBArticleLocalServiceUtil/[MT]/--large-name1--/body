{
  return getService().addArticle(uuid,userId,groupId,title,htmlTitle,version,content,description,minorEdit,head,template,draft,parentResourcePrimKey,tagsEntries,categoriesEntries,prefs,themeDisplay);
}
