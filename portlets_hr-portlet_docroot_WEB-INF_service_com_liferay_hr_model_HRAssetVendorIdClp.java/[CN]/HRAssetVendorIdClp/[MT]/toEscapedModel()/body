{
  if (isEscapedModel()) {
    return this;
  }
 else {
    return (HRAssetVendorId)Proxy.newProxyInstance(HRAssetVendorId.class.getClassLoader(),new Class[]{HRAssetVendorId.class},new AutoEscapeBeanHandler(this));
  }
}
