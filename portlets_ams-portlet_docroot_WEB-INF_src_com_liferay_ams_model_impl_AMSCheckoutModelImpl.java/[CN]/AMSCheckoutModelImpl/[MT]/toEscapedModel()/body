{
  if (isEscapedModel()) {
    return (AMSCheckout)this;
  }
 else {
    return (AMSCheckout)Proxy.newProxyInstance(AMSCheckout.class.getClassLoader(),new Class[]{AMSCheckout.class},new AutoEscapeBeanHandler(this));
  }
}
