{
  calendarBookingPersistence.remove(calendarBooking);
  resourceLocalService.deleteResource(calendarBooking.getCompanyId(),CalendarBooking.class.getName(),ResourceConstants.SCOPE_INDIVIDUAL,calendarBooking.getCalendarBookingId());
}
