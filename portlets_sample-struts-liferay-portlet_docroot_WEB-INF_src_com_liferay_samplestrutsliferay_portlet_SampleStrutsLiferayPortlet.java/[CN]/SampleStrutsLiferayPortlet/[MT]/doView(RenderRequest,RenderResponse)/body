{
  PortletSession portletSession=renderRequest.getPortletSession();
  portletSession.setAttribute("chart_name","Soda Survey",PortletSession.APPLICATION_SCOPE);
  super.doView(renderRequest,renderResponse);
}
