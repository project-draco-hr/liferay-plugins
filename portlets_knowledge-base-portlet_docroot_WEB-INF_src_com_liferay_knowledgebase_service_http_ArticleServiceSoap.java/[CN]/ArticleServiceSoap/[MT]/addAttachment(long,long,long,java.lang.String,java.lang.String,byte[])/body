{
  try {
    ArticleServiceUtil.addAttachment(companyId,groupId,resourcePrimKey,dirName,shortFileName,bytes);
  }
 catch (  Exception e) {
    _log.error(e,e);
    throw new RemoteException(e.getMessage());
  }
}
