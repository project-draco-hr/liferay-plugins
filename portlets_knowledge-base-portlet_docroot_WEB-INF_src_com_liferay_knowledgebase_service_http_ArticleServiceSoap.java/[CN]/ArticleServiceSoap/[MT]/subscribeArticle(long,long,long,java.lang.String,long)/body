{
  try {
    ArticleServiceUtil.subscribeArticle(companyId,groupId,plid,portletId,resourcePrimKey);
  }
 catch (  Exception e) {
    _log.error(e,e);
    throw new RemoteException(e.getMessage());
  }
}
