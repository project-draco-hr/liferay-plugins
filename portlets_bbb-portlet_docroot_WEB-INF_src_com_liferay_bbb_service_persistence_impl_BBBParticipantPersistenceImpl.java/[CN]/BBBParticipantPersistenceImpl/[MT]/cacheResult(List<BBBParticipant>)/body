{
  for (  BBBParticipant bbbParticipant : bbbParticipants) {
    if (EntityCacheUtil.getResult(BBBParticipantModelImpl.ENTITY_CACHE_ENABLED,BBBParticipantImpl.class,bbbParticipant.getPrimaryKey()) == null) {
      cacheResult(bbbParticipant);
    }
 else {
      bbbParticipant.resetOriginalValues();
    }
  }
}
