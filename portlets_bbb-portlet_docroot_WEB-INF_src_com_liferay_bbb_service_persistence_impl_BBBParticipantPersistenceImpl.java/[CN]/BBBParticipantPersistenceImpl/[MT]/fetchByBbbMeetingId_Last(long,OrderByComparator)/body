{
  int count=countByBbbMeetingId(bbbMeetingId);
  if (count == 0) {
    return null;
  }
  List<BBBParticipant> list=findByBbbMeetingId(bbbMeetingId,count - 1,count,orderByComparator);
  if (!list.isEmpty()) {
    return list.get(0);
  }
  return null;
}
