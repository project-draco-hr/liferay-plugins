{
  if (isEscapedModel()) {
    return (WSRPProducer)this;
  }
 else {
    return (WSRPProducer)Proxy.newProxyInstance(_classLoader,_escapedModelProxyInterfaces,new AutoEscapeBeanHandler(this));
  }
}
