{
  int count=countByS_S(sharedToClassNameId,sharedToClassPK);
  if (count == 0) {
    return null;
  }
  List<AssetSharingEntry> list=findByS_S(sharedToClassNameId,sharedToClassPK,count - 1,count,orderByComparator);
  if (!list.isEmpty()) {
    return list.get(0);
  }
  return null;
}
