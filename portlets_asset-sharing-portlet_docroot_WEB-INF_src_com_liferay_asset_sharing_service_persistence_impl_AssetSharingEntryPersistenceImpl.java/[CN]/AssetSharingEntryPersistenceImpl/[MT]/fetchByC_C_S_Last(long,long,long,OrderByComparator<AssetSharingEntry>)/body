{
  int count=countByC_C_S(classNameId,classPK,sharedToClassNameId);
  if (count == 0) {
    return null;
  }
  List<AssetSharingEntry> list=findByC_C_S(classNameId,classPK,sharedToClassNameId,count - 1,count,orderByComparator);
  if (!list.isEmpty()) {
    return list.get(0);
  }
  return null;
}
