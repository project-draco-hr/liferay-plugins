{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(MeetupEntry meetupEntry) method. Use update(MeetupEntry meetupEntry, boolean merge) instead.");
  }
  return update(meetupEntry,false);
}
