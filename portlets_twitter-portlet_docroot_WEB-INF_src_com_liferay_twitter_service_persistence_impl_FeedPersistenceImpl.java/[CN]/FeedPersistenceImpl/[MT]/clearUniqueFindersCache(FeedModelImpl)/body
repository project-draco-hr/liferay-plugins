{
  Object[] args=new Object[]{feedModelImpl.getUserId(),feedModelImpl.getTwitterScreenName()};
  finderCache.removeResult(FINDER_PATH_COUNT_BY_U_TSN,args);
  finderCache.removeResult(FINDER_PATH_FETCH_BY_U_TSN,args);
  if ((feedModelImpl.getColumnBitmask() & FINDER_PATH_FETCH_BY_U_TSN.getColumnBitmask()) != 0) {
    args=new Object[]{feedModelImpl.getOriginalUserId(),feedModelImpl.getOriginalTwitterScreenName()};
    finderCache.removeResult(FINDER_PATH_COUNT_BY_U_TSN,args);
    finderCache.removeResult(FINDER_PATH_FETCH_BY_U_TSN,args);
  }
}
