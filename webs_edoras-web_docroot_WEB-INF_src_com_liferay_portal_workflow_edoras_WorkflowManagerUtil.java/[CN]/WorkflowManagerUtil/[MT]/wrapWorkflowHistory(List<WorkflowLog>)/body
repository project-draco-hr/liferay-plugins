{
  List<WorkflowInstanceHistory> history=new ArrayList<WorkflowInstanceHistory>(workflowLogs.size());
  for (  WorkflowLog workflowLog : workflowLogs) {
    history.add(new WorkflowInstanceHistoryImpl(workflowLog));
  }
  return history;
}
