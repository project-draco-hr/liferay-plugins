{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(JIRAAction jiraAction) method. Use update(JIRAAction jiraAction, boolean merge) instead.");
  }
  return update(jiraAction,false);
}
