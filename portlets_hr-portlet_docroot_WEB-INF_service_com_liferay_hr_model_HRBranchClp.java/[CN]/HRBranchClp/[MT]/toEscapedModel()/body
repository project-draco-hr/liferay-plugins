{
  if (isEscapedModel()) {
    return this;
  }
 else {
    return (HRBranch)Proxy.newProxyInstance(HRBranch.class.getClassLoader(),new Class[]{HRBranch.class},new AutoEscapeBeanHandler(this));
  }
}
