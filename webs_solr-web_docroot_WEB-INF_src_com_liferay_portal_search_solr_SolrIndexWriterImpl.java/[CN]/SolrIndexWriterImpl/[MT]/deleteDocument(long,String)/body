{
  com.liferay.portal.kernel.xml.Document xmlDoc=SAXReaderUtil.createDocument();
  Element deleteEl=xmlDoc.addElement("delete");
  Element idEl=deleteEl.addElement("id");
  idEl.setText(uid);
  try {
    write(xmlDoc);
  }
 catch (  Exception e) {
    throw new SearchException(e);
  }
}
