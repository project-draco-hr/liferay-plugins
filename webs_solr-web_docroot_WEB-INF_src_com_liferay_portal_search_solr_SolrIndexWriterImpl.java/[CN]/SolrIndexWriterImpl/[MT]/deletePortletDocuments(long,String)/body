{
  try {
    _solrServer.deleteByQuery(Field.PORTLET_ID + StringPool.COLON + portletId);
    if (_commit) {
      _solrServer.commit();
    }
  }
 catch (  Exception e) {
    throw new SearchException(e);
  }
}
