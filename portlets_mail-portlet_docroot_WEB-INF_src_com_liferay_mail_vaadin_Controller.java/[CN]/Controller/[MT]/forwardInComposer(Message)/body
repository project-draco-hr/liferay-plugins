{
  String newMessageSubject="Fwd: " + originalMessage.getSubject();
  String newMessageBody=MessageUtil.createForwardMessage(originalMessage);
  Composer c=compose(null,null,newMessageSubject,newMessageBody);
  c.focusToField();
  return c;
}
