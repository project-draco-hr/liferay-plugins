{
  if ((logTypes == null) || logTypes.isEmpty()) {
    return kaleoLogPersistence.findByKaleoTaskInstanceTokenId(kaleoTaskId,start,end,orderByComparator);
  }
 else {
    DynamicQuery dynamicQuery=buildDynamicQuery(kaleoTaskId,logTypes);
    return dynamicQuery(dynamicQuery,start,end,orderByComparator);
  }
}
