{
  if (isEscapedModel()) {
    return (HRAssetVendor)this;
  }
 else {
    return (HRAssetVendor)Proxy.newProxyInstance(HRAssetVendor.class.getClassLoader(),new Class[]{HRAssetVendor.class},new AutoEscapeBeanHandler(this));
  }
}
