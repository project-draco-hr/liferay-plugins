{
  if (isEscapedModel()) {
    return (HRAssetVendor)this;
  }
 else {
    return (HRAssetVendor)Proxy.newProxyInstance(_classLoader,_escapedModelProxyInterfaces,new AutoEscapeBeanHandler(this));
  }
}
