{
  Mockito.when(SessionManager.getSession(Mockito.anyLong())).thenReturn(_session);
  Mockito.when(_session.executeGet(Mockito.anyString(),Mockito.any(ResponseHandler.class))).thenReturn(readResponse(fileName));
}
