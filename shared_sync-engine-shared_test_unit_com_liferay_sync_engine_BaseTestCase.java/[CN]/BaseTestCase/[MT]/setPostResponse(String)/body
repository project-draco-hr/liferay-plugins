{
  Mockito.when(SessionManager.getSession(Mockito.anyLong())).thenReturn(_session);
  String response=readResponse(fileName);
  Mockito.when(_session.executePost(Mockito.anyString(),Mockito.anyMap(),Mockito.any(ResponseHandler.class))).thenReturn(response);
}
