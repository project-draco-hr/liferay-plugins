{
  Mockito.when(SessionManager.getSession(Mockito.anyLong())).thenReturn(session);
  String response=readResponse(fileName);
  Mockito.when(session.executePost(Mockito.anyString(),Mockito.anyMap(),Mockito.any(ResponseHandler.class))).thenReturn(response);
}
