{
  Mockito.when(SessionManager.getSession(Mockito.anyLong())).thenReturn(_session);
  Mockito.when(_session.executePost(Mockito.anyString(),Mockito.anyMap(),Mockito.any(ResponseHandler.class))).thenReturn(readResponse(fileName));
}
