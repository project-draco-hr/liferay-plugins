{
  SyncGroup syncGroup=null;
  try {
    syncGroup=_syncGroupPersistence.getSyncGroup(syncAccountId,groupId);
  }
 catch (  SQLException sqle) {
    _logger.debug(sqle.getMessage(),sqle);
  }
  return syncGroup;
}
