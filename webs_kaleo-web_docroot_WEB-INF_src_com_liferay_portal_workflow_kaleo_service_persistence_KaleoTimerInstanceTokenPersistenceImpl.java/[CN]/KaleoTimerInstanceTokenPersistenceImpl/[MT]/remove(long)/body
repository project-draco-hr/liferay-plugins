{
  Session session=null;
  try {
    session=openSession();
    KaleoTimerInstanceToken kaleoTimerInstanceToken=(KaleoTimerInstanceToken)session.get(KaleoTimerInstanceTokenImpl.class,Long.valueOf(kaleoTimerInstanceTokenId));
    if (kaleoTimerInstanceToken == null) {
      if (_log.isWarnEnabled()) {
        _log.warn(_NO_SUCH_ENTITY_WITH_PRIMARY_KEY + kaleoTimerInstanceTokenId);
      }
      throw new NoSuchTimerInstanceTokenException(_NO_SUCH_ENTITY_WITH_PRIMARY_KEY + kaleoTimerInstanceTokenId);
    }
    return kaleoTimerInstanceTokenPersistence.remove(kaleoTimerInstanceToken);
  }
 catch (  NoSuchTimerInstanceTokenException nsee) {
    throw nsee;
  }
catch (  Exception e) {
    throw processException(e);
  }
 finally {
    closeSession(session);
  }
}
