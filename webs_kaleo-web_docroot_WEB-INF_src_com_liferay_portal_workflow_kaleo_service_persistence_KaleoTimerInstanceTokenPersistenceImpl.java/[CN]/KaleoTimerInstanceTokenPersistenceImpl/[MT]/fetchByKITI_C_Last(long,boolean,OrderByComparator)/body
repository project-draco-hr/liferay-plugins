{
  int count=countByKITI_C(kaleoInstanceTokenId,completed);
  List<KaleoTimerInstanceToken> list=findByKITI_C(kaleoInstanceTokenId,completed,count - 1,count,orderByComparator);
  if (!list.isEmpty()) {
    return list.get(0);
  }
  return null;
}
