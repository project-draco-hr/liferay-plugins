{
  if (!(object instanceof SyncFile)) {
    return false;
  }
 else   if (object == this) {
    return true;
  }
  SyncFile syncFile=(SyncFile)object;
  if (syncFile.getSyncFileId() == syncFileId) {
    return true;
  }
 else {
    return false;
  }
}
