{
  Attachment attachment=attachmentPersistence.findByPrimaryKey(attachmentId);
  return DLServiceUtil.getFile(attachment.getCompanyId(),_repositoryId,getFilePath(attachment.getMessageId(),attachment.getFileName()));
}
