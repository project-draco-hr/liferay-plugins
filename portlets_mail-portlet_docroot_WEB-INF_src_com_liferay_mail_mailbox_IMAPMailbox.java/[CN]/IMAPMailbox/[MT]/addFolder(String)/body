{
  IMAPUtil imapUtil=getIMAPUtil();
  String[] folderData=imapUtil.addFolder(displayName);
  return FolderLocalServiceUtil.addFolder(user.getUserId(),account.getAccountId(),folderData[0],folderData[1],0);
}
