{
  ExpandoTable table=null;
  try {
    table=ExpandoTableLocalServiceUtil.getTable(companyId,classNameId,name);
  }
 catch (  PortalException e) {
    throw new SystemException(e);
  }
  return getRow(table,classPK);
}
