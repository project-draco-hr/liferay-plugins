{
  ExpandoTable table=null;
  try {
    table=ExpandoTableLocalServiceUtil.getTable(tableId);
  }
 catch (  PortalException e) {
    throw new SystemException(e);
  }
  return getRowsCount(table.getCompanyId(),table.getClassName(),table.getName());
}
