{
  if (parentResourcePrimKey == ArticleConstants.DEFAULT_PARENT_RESOURCE_PRIM_KEY) {
    return resourcePrimKey;
  }
  Article article=getLatestArticle(parentResourcePrimKey,WorkflowConstants.STATUS_ANY);
  return article.getRootResourcePrimKey();
}
