{
  subscriptionLocalService.deleteSubscription(subscription);
  expandoValueLocalService.deleteValue(subscription.getCompanyId(),Subscription.class.getName(),"KB","portletId",subscription.getSubscriptionId());
}
