{
  if (resourcePrimKey <= 0) {
    subscriptionLocalService.addSubscription(userId,Article.class.getName(),groupId);
  }
 else {
    subscriptionLocalService.addSubscription(userId,Article.class.getName(),resourcePrimKey);
  }
}
