{
  if (resourcePrimKey <= 0) {
    SubscriptionLocalServiceUtil.addSubscription(userId,Article.class.getName(),groupId);
  }
 else {
    SubscriptionLocalServiceUtil.addSubscription(userId,Article.class.getName(),resourcePrimKey);
  }
}
