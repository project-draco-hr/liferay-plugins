{
  List<Article> articles=getGroupArticles(groupId,QueryUtil.ALL_POS,QueryUtil.ALL_POS,new ArticleCreateDateComparator(true));
  for (  Article article : articles) {
    deleteArticles(article);
  }
}
