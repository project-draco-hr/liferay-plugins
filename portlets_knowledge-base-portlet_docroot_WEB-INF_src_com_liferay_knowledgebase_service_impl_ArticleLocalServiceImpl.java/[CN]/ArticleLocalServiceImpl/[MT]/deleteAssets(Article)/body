{
  assetEntryLocalService.deleteEntry(Article.class.getName(),article.getClassPK());
  if (!article.isApproved() && !article.isFirstVersion()) {
    assetEntryLocalService.deleteEntry(Article.class.getName(),article.getResourcePrimKey());
  }
}
