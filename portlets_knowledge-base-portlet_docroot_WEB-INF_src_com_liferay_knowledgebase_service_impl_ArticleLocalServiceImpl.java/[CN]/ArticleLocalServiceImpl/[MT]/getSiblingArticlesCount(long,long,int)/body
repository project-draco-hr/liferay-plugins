{
  if (status == WorkflowConstants.STATUS_ANY) {
    return articlePersistence.countByG_P_L(groupId,parentResourcePrimKey,true);
  }
 else   if (status == WorkflowConstants.STATUS_APPROVED) {
    return articlePersistence.countByG_P_M(groupId,parentResourcePrimKey,true);
  }
  return articlePersistence.countByG_P_S(groupId,parentResourcePrimKey,status);
}
