{
  if (resourcePrimKey <= 0) {
    SubscriptionLocalServiceUtil.deleteSubscription(userId,Article.class.getName(),groupId);
  }
 else {
    SubscriptionLocalServiceUtil.deleteSubscription(userId,Article.class.getName(),resourcePrimKey);
  }
}
