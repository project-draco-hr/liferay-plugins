{
  if (resourcePrimKey <= 0) {
    subscriptionLocalService.deleteSubscription(userId,Article.class.getName(),groupId);
  }
 else {
    subscriptionLocalService.deleteSubscription(userId,Article.class.getName(),resourcePrimKey);
  }
}
