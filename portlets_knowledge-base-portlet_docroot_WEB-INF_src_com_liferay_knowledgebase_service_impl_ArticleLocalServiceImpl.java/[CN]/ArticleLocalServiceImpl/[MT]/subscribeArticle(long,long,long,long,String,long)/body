{
  Subscription subscription=subscriptionLocalService.addSubscription(userId,groupId,Article.class.getName(),resourcePrimKey);
  expandoValueLocalService.addValue(subscription.getCompanyId(),Subscription.class.getName(),"KB","portletPrimKeys",subscription.getSubscriptionId(),new String[]{ArticleConstants.getPortletPrimKey(plid,portletId)});
}
