{
  List<Object> objects=dynamicQuery(getMaxVersionQuery(params),start,end,obc);
  List<Article> articles=new ArrayList<Article>();
  for (  Object object : objects) {
    articles.add((Article)object);
  }
  return articles;
}
