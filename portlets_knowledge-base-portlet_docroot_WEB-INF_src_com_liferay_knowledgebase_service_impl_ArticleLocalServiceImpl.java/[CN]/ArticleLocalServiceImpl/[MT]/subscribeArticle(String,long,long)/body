{
  Subscription subscription=subscriptionLocalService.addSubscription(userId,Article.class.getName(),resourcePrimKey);
  expandoValueLocalService.addValue(subscription.getCompanyId(),Subscription.class.getName(),"KB","portletId",subscription.getSubscriptionId(),portletId);
}
