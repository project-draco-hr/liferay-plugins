{
  if (status == WorkflowConstants.STATUS_ANY) {
    return articlePersistence.findByG_L(groupId,true,start,end,orderByComparator);
  }
 else   if (status == WorkflowConstants.STATUS_APPROVED) {
    return articlePersistence.findByG_M(groupId,true,start,end,orderByComparator);
  }
  return articlePersistence.findByG_S(groupId,status,start,end,orderByComparator);
}
