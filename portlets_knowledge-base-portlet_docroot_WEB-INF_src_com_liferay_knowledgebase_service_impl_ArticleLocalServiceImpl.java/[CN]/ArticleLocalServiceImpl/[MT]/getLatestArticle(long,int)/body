{
  if (status == WorkflowConstants.STATUS_ANY) {
    return articlePersistence.findByResourcePrimKey_First(resourcePrimKey,new ArticleVersionComparator());
  }
  return articlePersistence.findByR_S_First(resourcePrimKey,status,new ArticleVersionComparator());
}
