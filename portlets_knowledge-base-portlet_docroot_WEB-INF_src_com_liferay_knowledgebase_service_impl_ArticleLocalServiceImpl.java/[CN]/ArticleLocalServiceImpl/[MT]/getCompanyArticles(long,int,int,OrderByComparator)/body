{
  Map<String,Long> params=new HashMap<String,Long>();
  params.put("companyId",new Long(companyId));
  List<Object> results=dynamicQuery(getDynamicQuery(params),start,end,orderByComparator);
  return toArticles(results);
}
