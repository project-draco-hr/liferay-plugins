{
  Session session=null;
  try {
    session=openSession();
    return (SemanticsElement)session.get(SemanticsElementImpl.class,elementURI);
  }
 catch (  Exception e) {
    throw processException(e);
  }
 finally {
    closeSession(session);
  }
}
