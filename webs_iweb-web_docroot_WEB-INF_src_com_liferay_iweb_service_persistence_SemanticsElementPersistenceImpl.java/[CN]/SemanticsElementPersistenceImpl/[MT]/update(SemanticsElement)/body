{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(SemanticsElement semanticsElement) method. Use update(SemanticsElement semanticsElement, boolean merge) instead.");
  }
  return update(semanticsElement,false);
}
