{
  List<Gadget> gadgets=gadgetPersistence.findByUuid(gadgetUuid);
  if (gadgets.isEmpty()) {
    throw new NoSuchGadgetException("No OpenSocial gadget exists with uuid " + gadgetUuid);
  }
  return gadgets.get(0);
}
