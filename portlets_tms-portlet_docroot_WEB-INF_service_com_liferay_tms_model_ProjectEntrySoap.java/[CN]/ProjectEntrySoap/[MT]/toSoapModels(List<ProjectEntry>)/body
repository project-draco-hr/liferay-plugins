{
  List<ProjectEntrySoap> soapModels=new ArrayList<ProjectEntrySoap>(models.size());
  for (  ProjectEntry model : models) {
    soapModels.add(toSoapModel(model));
  }
  return soapModels.toArray(new ProjectEntrySoap[soapModels.size()]);
}
