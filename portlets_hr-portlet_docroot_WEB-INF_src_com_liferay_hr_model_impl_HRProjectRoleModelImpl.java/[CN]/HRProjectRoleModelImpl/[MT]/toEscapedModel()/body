{
  if (isEscapedModel()) {
    return (HRProjectRole)this;
  }
 else {
    return (HRProjectRole)Proxy.newProxyInstance(HRProjectRole.class.getClassLoader(),new Class[]{HRProjectRole.class},new AutoEscapeBeanHandler(this));
  }
}
