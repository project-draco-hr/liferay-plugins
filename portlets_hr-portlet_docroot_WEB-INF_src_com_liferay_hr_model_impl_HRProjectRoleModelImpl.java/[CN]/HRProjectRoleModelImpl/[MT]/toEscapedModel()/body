{
  if (isEscapedModel()) {
    return (HRProjectRole)this;
  }
 else {
    return (HRProjectRole)Proxy.newProxyInstance(_classLoader,_escapedModelProxyInterfaces,new AutoEscapeBeanHandler(this));
  }
}
