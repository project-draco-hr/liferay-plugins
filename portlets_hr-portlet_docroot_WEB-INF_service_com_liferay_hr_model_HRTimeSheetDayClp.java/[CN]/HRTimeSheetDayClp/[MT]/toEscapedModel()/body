{
  if (isEscapedModel()) {
    return this;
  }
 else {
    return (HRTimeSheetDay)Proxy.newProxyInstance(HRTimeSheetDay.class.getClassLoader(),new Class[]{HRTimeSheetDay.class},new AutoEscapeBeanHandler(this));
  }
}
