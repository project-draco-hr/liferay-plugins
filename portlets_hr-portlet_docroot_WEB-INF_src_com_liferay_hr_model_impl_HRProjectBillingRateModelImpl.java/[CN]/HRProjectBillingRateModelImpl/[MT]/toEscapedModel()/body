{
  if (isEscapedModel()) {
    return (HRProjectBillingRate)this;
  }
 else {
    return (HRProjectBillingRate)Proxy.newProxyInstance(_classLoader,_escapedModelProxyInterfaces,new AutoEscapeBeanHandler(this));
  }
}
