{
  Set<Community> commset=new TreeSet();
  try {
    commset.addAll(communityPersistence.findAll());
    return commset;
  }
 catch (  SystemException e) {
    if (e.getCause() instanceof com.liferay.portal.NoSuchResourceException) {
      return commset;
    }
 else {
      throw new IWebException(e);
    }
  }
}
