{
  try {
    for (    com.liferay.portal.iweb.model.Semantics semantics : semanticss) {
      addSemantics.add(pk,semantics.getPrimaryKey());
    }
  }
 catch (  Exception e) {
    throw processException(e);
  }
 finally {
    FinderCacheUtil.clearCache("Communities_Semantics");
  }
}
