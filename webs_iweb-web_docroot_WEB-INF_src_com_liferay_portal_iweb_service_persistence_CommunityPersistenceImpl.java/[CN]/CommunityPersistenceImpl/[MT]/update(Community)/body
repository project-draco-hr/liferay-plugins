{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(Community community) method. Use update(Community community, boolean merge) instead.");
  }
  return update(community,false);
}
