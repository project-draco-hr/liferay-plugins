{
  for (  Checkout checkout : checkouts) {
    if (entityCache.getResult(CheckoutModelImpl.ENTITY_CACHE_ENABLED,CheckoutImpl.class,checkout.getPrimaryKey()) == null) {
      cacheResult(checkout);
    }
 else {
      checkout.resetOriginalValues();
    }
  }
}
