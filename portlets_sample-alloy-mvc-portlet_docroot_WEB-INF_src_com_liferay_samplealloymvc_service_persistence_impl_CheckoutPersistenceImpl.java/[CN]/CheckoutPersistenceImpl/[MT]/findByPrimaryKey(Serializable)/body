{
  Checkout checkout=fetchByPrimaryKey(primaryKey);
  if (checkout == null) {
    if (_log.isDebugEnabled()) {
      _log.debug(_NO_SUCH_ENTITY_WITH_PRIMARY_KEY + primaryKey);
    }
    throw new NoSuchCheckoutException(_NO_SUCH_ENTITY_WITH_PRIMARY_KEY + primaryKey);
  }
  return checkout;
}
