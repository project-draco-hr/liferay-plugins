{
  if (_kbArticle == null) {
    return KBCommentServiceUtil.getKBComments(_groupId,status,searchContainer.getStart(),searchContainer.getEnd());
  }
 else {
    return KBCommentLocalServiceUtil.getKBComments(KBArticleConstants.getClassName(),_kbArticle.getClassPK(),new int[]{status},searchContainer.getStart(),searchContainer.getEnd());
  }
}
