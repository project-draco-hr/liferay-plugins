{
  JSONArray jsonArray=JSONFactoryUtil.createJSONArray();
  if (calendarBookings != null) {
    for (    CalendarBooking calendarBooking : calendarBookings) {
      jsonArray.put(toCalendarJSONObject(request,calendarBooking.getCalendar()));
    }
  }
  return jsonArray;
}
