{
  HRClient hrClient=fetchByPrimaryKey(hrClientId);
  if (hrClient == null) {
    if (_log.isWarnEnabled()) {
      _log.warn(_NO_SUCH_ENTITY_WITH_PRIMARY_KEY + hrClientId);
    }
    throw new NoSuchClientException(_NO_SUCH_ENTITY_WITH_PRIMARY_KEY + hrClientId);
  }
  return hrClient;
}
