{
  Event event=eventRequest.getEvent();
  HashMap<String,String> hashMap=(HashMap<String,String>)event.getValue();
  String value=hashMap.get("hello");
  if (Objects.equals(value,"world")) {
    _result=true;
  }
 else {
    _result=false;
  }
}
