{
  long mbThreadId=0;
  List<User> recipients=new ArrayList<User>();
  MBMessage mbMessage=MBMessageLocalServiceUtil.getMBMessage(parentMessageId);
  recipients.add(UserLocalServiceUtil.getUser(mbMessage.getUserId()));
  return addPrivateMessage(userId,mbThreadId,parentMessageId,recipients,subject,body,files);
}
