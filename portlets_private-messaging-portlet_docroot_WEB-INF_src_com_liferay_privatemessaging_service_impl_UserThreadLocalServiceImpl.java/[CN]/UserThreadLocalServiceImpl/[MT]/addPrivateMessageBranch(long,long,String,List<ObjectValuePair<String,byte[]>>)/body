{
  long mbThreadId=0;
  MBMessage parentMessage=MBMessageLocalServiceUtil.getMBMessage(parentMessageId);
  long[] recipientUserIds=new long[]{parentMessage.getUserId()};
  return addPrivateMessage(userId,mbThreadId,parentMessageId,recipientUserIds,parentMessage.getSubject(),body,files);
}
