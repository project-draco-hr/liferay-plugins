{
  long mbThreadId=0;
  MBMessage parentMessage=MBMessageLocalServiceUtil.getMBMessage(parentMBMessageId);
  long[] recipientUserIds={parentMessage.getUserId()};
  return addPrivateMessage(userId,mbThreadId,parentMBMessageId,recipientUserIds,parentMessage.getSubject(),body,files,themeDisplay);
}
