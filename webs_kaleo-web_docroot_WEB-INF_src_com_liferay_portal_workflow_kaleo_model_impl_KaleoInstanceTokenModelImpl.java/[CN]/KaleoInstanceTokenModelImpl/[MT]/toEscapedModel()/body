{
  if (isEscapedModel()) {
    return (KaleoInstanceToken)this;
  }
 else {
    return (KaleoInstanceToken)Proxy.newProxyInstance(KaleoInstanceToken.class.getClassLoader(),new Class[]{KaleoInstanceToken.class},new AutoEscapeBeanHandler(this));
  }
}
