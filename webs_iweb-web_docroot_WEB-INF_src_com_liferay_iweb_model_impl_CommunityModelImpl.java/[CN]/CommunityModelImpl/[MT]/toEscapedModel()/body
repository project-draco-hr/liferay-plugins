{
  if (isEscapedModel()) {
    return (Community)this;
  }
 else {
    Community model=new CommunityImpl();
    model.setEscapedModel(true);
    model.setCid(getCid());
    model.setType(getType());
    model=(Community)Proxy.newProxyInstance(Community.class.getClassLoader(),new Class[]{Community.class},new ReadOnlyBeanHandler(model));
    return model;
  }
}
