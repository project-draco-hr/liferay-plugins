{
  calendarPersistence.remove(calendar);
  calendarBookingLocalService.deleteCalendarBookings(calendar.getCalendarId());
  resourceLocalService.deleteResource(calendar.getCompanyId(),Calendar.class.getName(),ResourceConstants.SCOPE_INDIVIDUAL,calendar.getCalendarId());
}
