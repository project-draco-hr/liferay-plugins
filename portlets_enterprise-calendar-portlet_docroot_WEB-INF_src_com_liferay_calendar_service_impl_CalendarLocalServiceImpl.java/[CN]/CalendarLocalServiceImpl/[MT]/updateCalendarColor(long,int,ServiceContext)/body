{
  Calendar calendar=calendarPersistence.findByPrimaryKey(calendarId);
  return updateCalendar(calendarId,calendar.getNameMap(),calendar.getDescriptionMap(),color,calendar.isDefaultCalendar(),serviceContext);
}
