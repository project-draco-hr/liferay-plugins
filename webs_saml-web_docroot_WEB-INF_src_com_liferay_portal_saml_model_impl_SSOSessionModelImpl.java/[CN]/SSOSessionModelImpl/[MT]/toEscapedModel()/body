{
  if (isEscapedModel()) {
    return (SSOSession)this;
  }
 else {
    return (SSOSession)Proxy.newProxyInstance(SSOSession.class.getClassLoader(),new Class[]{SSOSession.class},new AutoEscapeBeanHandler(this));
  }
}
