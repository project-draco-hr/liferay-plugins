{
  if (isEscapedModel()) {
    return (HRTimeOffPolicy)this;
  }
 else {
    return (HRTimeOffPolicy)Proxy.newProxyInstance(_classLoader,_escapedModelProxyInterfaces,new AutoEscapeBeanHandler(this));
  }
}
