{
  if (isEscapedModel()) {
    return (HRTimeOffPolicy)this;
  }
 else {
    return (HRTimeOffPolicy)Proxy.newProxyInstance(HRTimeOffPolicy.class.getClassLoader(),new Class[]{HRTimeOffPolicy.class},new AutoEscapeBeanHandler(this));
  }
}
