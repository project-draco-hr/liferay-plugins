{
  SyncSiteService.setActiveSyncSiteIds(syncAccount.getSyncAccountId(),null);
  SyncSiteService.unregisterModelListener(_syncSiteModelListener);
  SyncSiteService.deleteSyncSite(_syncSite.getSyncSiteId());
  super.tearDown();
}
