{
  Session session=null;
  try {
    session=openSession();
    return (JIRAIssue)session.get(JIRAIssueImpl.class,new Long(jiraIssueId));
  }
 catch (  Exception e) {
    throw HibernateUtil.processException(e);
  }
 finally {
    closeSession(session);
  }
}
