{
  try {
    KBArticleServiceUtil.deleteAttachment(companyId,groupId,portletId,resourcePrimKey,fileName);
  }
 catch (  Exception e) {
    _log.error(e,e);
    throw new RemoteException(e.getMessage());
  }
}
