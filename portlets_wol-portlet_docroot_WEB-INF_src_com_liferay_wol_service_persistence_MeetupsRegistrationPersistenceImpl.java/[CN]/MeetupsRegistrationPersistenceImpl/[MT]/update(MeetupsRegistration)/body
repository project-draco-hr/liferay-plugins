{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(MeetupsRegistration meetupsRegistration) method. Use update(MeetupsRegistration meetupsRegistration, boolean merge) instead.");
  }
  return update(meetupsRegistration,false);
}
