{
  Session session=null;
  try {
    session=openSession();
    return (MeetupsRegistration)session.get(MeetupsRegistrationImpl.class,new Long(meetupsRegistrationId));
  }
 catch (  Exception e) {
    throw processException(e);
  }
 finally {
    closeSession(session);
  }
}
