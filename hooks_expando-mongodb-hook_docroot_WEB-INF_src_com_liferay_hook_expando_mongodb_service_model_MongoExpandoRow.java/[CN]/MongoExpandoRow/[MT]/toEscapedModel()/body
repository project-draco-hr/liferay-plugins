{
  if (isEscapedModel()) {
    return this;
  }
 else {
    return (ExpandoRow)Proxy.newProxyInstance(ExpandoRow.class.getClassLoader(),new Class[]{ExpandoRow.class},new AutoEscapeBeanHandler(this));
  }
}
