{
  Session session=null;
  try {
    session=openSession();
    return (TaskEntry)session.get(TaskEntryImpl.class,new Long(taskEntryId));
  }
 catch (  Exception e) {
    throw processException(e);
  }
 finally {
    closeSession(session);
  }
}
