{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(TaskEntry taskEntry) method. Use update(TaskEntry taskEntry, boolean merge) instead.");
  }
  return update(taskEntry,false);
}
