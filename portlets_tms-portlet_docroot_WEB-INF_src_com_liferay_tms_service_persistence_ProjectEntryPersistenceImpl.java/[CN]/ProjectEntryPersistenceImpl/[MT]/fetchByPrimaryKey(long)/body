{
  Session session=null;
  try {
    session=openSession();
    return (ProjectEntry)session.get(ProjectEntryImpl.class,new Long(projectEntryId));
  }
 catch (  Exception e) {
    throw processException(e);
  }
 finally {
    closeSession(session);
  }
}
