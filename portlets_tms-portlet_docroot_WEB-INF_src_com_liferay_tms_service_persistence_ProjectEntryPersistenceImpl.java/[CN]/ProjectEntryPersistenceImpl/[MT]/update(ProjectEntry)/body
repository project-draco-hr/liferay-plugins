{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(ProjectEntry projectEntry) method. Use update(ProjectEntry projectEntry, boolean merge) instead.");
  }
  return update(projectEntry,false);
}
