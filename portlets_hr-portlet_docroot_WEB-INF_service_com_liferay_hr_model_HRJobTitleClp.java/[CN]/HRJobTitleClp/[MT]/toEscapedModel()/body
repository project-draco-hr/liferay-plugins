{
  if (isEscapedModel()) {
    return this;
  }
 else {
    return (HRJobTitle)Proxy.newProxyInstance(HRJobTitle.class.getClassLoader(),new Class[]{HRJobTitle.class},new AutoEscapeBeanHandler(this));
  }
}
