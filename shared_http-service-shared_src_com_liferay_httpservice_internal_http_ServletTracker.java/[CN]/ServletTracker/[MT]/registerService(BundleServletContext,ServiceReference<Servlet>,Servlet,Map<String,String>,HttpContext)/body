{
  String servletName=GetterUtil.getString(serviceReference.getProperty("servletName"));
  String urlPattern=GetterUtil.getString(serviceReference.getProperty("urlPattern"));
  if (Validator.isNull(urlPattern)) {
    return;
  }
  bundleServletContext.registerServlet(servletName,urlPattern,servlet,initParameters,httpContext);
}
