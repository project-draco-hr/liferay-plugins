{
  try {
    if (index == 0) {
      GroupLocalServiceUtil.updateFriendlyURL(groupId,friendlyURL);
    }
 else {
      GroupLocalServiceUtil.updateFriendlyURL(groupId,friendlyURL + index);
    }
  }
 catch (  Exception e) {
    updateFriendlyURL(groupId,friendlyURL,index + 1);
  }
}
