{
  List<WikiPage> wikiPages=null;
  try {
    wikiPages=WikiPageServiceUtil.getNodePages(nodeId,WikiNavigationConstants.MAX_PAGES);
  }
 catch (  Exception e) {
    return new LinkedList<MenuItem>();
  }
  return _fromWikiNode(wikiPages,1,depth,portletURL);
}
