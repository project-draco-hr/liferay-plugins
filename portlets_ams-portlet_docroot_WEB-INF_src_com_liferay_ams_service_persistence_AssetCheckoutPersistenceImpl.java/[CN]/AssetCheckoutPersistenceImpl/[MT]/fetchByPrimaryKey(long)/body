{
  Session session=null;
  try {
    session=openSession();
    return (AssetCheckout)session.get(AssetCheckoutImpl.class,new Long(assetCheckoutId));
  }
 catch (  Exception e) {
    throw processException(e);
  }
 finally {
    closeSession(session);
  }
}
