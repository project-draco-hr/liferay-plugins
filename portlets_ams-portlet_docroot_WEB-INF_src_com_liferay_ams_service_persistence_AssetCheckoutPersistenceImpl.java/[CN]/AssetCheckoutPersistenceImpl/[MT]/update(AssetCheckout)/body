{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(AssetCheckout assetCheckout) method. Use update(AssetCheckout assetCheckout, boolean merge) instead.");
  }
  return update(assetCheckout,false);
}
