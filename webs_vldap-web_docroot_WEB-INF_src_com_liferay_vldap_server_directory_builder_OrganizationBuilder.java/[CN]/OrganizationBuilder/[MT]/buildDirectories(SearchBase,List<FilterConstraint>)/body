{
  List<Directory> directories=new ArrayList<Directory>();
  List<Organization> organizations=getOrganizations(searchBase,filterConstraints);
  for (  Organization organization : organizations) {
    Directory directory=new OrganizationDirectory(searchBase.getTop(),searchBase.getCompany(),organization);
    directories.add(directory);
  }
  return directories;
}
