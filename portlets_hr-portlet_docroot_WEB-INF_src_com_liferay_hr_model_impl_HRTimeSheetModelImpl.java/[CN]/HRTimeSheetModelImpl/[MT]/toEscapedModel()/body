{
  if (isEscapedModel()) {
    return (HRTimeSheet)this;
  }
 else {
    return (HRTimeSheet)Proxy.newProxyInstance(HRTimeSheet.class.getClassLoader(),new Class[]{HRTimeSheet.class},new AutoEscapeBeanHandler(this));
  }
}
