{
  Session session=null;
  try {
    session=openSession();
    return (AssetEntry)session.get(AssetEntryImpl.class,new Long(assetEntryId));
  }
 catch (  Exception e) {
    throw processException(e);
  }
 finally {
    closeSession(session);
  }
}
