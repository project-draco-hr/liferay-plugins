{
  BBBMeetingPermission.check(getPermissionChecker(),bbbMeetingId,ActionKeys.UPDATE);
  return bbbParticipantLocalService.updateBBBParticipant(bbbParticipantId,bbbMeetingId,name,emailAddress,type,serviceContext);
}
