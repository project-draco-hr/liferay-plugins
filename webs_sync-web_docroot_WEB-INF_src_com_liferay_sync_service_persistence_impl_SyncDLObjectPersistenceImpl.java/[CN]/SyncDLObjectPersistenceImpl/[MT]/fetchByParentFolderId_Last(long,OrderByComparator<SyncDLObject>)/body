{
  int count=countByParentFolderId(parentFolderId);
  if (count == 0) {
    return null;
  }
  List<SyncDLObject> list=findByParentFolderId(parentFolderId,count - 1,count,orderByComparator);
  if (!list.isEmpty()) {
    return list.get(0);
  }
  return null;
}
