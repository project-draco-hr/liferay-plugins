{
  int count=countByC_R_T(companyId,repositoryId,type);
  if (count == 0) {
    return null;
  }
  List<SyncDLObject> list=findByC_R_T(companyId,repositoryId,type,count - 1,count,orderByComparator);
  if (!list.isEmpty()) {
    return list.get(0);
  }
  return null;
}
