{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(PostEntry postEntry) method. Use update(PostEntry postEntry, boolean merge) instead.");
  }
  return update(postEntry,false);
}
