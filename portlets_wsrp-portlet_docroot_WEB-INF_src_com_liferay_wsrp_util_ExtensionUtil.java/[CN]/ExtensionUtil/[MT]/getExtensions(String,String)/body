{
  MessageElement messageElement=new MessageElement("liferay",localPart);
  messageElement.setValue(value);
  MessageElement[] messageElements=new MessageElement[]{messageElement};
  Extension extension=new Extension(messageElements);
  return new Extension[]{extension};
}
