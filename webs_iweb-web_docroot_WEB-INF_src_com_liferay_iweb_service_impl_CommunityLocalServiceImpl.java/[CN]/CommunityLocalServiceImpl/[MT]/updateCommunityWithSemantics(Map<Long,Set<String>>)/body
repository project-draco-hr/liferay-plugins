{
  Set<Map.Entry<Long,Set<String>>> entries=communitySemanticsMap.entrySet();
  for (  Map.Entry<Long,Set<String>> entry : entries) {
    updateCommunityWithSemantics(entry.getKey(),entry.getValue());
  }
}
