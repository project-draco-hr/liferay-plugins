{
  if (isEscapedModel()) {
    return this;
  }
 else {
    return (HRAssetModelId)Proxy.newProxyInstance(HRAssetModelId.class.getClassLoader(),new Class[]{HRAssetModelId.class},new AutoEscapeBeanHandler(this));
  }
}
