{
  AdminPermission.check(getPermissionChecker(),serviceContext.getScopeGroupId(),ActionKeys.ADD_TEMPLATE);
  return templateLocalService.addTemplate(null,getUserId(),title,content,description,serviceContext);
}
