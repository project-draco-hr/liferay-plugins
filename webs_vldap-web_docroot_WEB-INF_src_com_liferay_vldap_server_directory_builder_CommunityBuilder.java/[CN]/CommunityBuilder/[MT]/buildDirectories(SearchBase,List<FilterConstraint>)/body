{
  List<Directory> directories=new ArrayList<Directory>();
  List<Group> groups=getGroups(searchBase,filterConstraints);
  for (  Group group : groups) {
    Directory directory=new CommunityDirectory(searchBase.getTop(),searchBase.getCompany(),group);
    directories.add(directory);
  }
  return directories;
}
