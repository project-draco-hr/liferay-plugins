{
  String namespace=ServicePreAction.NAMESPACE;
  currentURL=HttpUtil.setParameter(currentURL,namespace + Constants.CMD,Constants.FIND_ARTICLE);
  currentURL=HttpUtil.setParameter(currentURL,namespace + "resourcePrimKey",resourcePrimKey);
  return currentURL;
}
