{
  String emailArticleUpdatedBody=jxPreferences.getValue("email-article-updated-body",StringPool.BLANK);
  if (Validator.isNotNull(emailArticleUpdatedBody)) {
    return emailArticleUpdatedBody;
  }
  return ContentUtil.get(PortletProps.get("admin.email.article.updated.body"));
}
