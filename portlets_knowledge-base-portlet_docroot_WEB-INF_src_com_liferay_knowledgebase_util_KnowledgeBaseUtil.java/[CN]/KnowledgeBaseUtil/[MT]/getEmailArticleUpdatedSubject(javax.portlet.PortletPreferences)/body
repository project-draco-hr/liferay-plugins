{
  String emailArticleUpdatedSubject=jxPreferences.getValue("email-article-updated-subject",StringPool.BLANK);
  if (Validator.isNotNull(emailArticleUpdatedSubject)) {
    return emailArticleUpdatedSubject;
  }
  return ContentUtil.get(PortletProps.get("admin.email.article.updated.subject"));
}
