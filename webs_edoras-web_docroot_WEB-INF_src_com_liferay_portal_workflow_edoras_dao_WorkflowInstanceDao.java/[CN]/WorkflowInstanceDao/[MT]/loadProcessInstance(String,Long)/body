{
  List<? extends MutableProcessInstance> processInstances=loadProcessInstances(relationType,relationId);
  if ((processInstances != null) && !processInstances.isEmpty()) {
    return (T)processInstances.get(0);
  }
  return null;
}
