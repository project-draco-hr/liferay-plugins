{
  Foo foo=fetchByPrimaryKey(fooId);
  if (foo == null) {
    if (_log.isWarnEnabled()) {
      _log.warn("No Foo exists with the primary key " + fooId);
    }
    throw new NoSuchFooException("No Foo exists with the primary key " + fooId);
  }
  return foo;
}
