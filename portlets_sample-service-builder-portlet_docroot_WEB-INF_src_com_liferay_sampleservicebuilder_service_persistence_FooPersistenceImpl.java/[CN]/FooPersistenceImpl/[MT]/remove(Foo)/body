{
  for (  ModelListener<Foo> listener : listeners) {
    listener.onBeforeRemove(foo);
  }
  foo=removeImpl(foo);
  for (  ModelListener<Foo> listener : listeners) {
    listener.onAfterRemove(foo);
  }
  return foo;
}
