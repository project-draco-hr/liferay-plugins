{
  for (  ModelListener listener : listeners) {
    listener.onBeforeRemove(foo);
  }
  foo=removeImpl(foo);
  for (  ModelListener listener : listeners) {
    listener.onAfterRemove(foo);
  }
  return foo;
}
