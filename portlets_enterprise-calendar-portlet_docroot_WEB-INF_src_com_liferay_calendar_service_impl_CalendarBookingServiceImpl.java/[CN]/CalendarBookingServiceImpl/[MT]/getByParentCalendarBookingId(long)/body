{
  List<CalendarBooking> calendarBookings=calendarBookingLocalService.getByParentCalendarBookingId(parentCalendarBookingId);
  for (  CalendarBooking calendarBooking : calendarBookings) {
    filterCalendarBooking(calendarBooking);
  }
  return calendarBookings;
}
