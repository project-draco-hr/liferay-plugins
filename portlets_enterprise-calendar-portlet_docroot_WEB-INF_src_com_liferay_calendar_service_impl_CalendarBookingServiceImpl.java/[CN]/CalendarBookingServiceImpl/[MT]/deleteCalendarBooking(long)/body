{
  CalendarBooking calendarBooking=calendarBookingLocalService.getCalendarBooking(calendarBookingId);
  CalendarPermission.check(getPermissionChecker(),calendarBooking.getCalendarId(),ActionKeys.MANAGE_BOOKINGS);
  calendarBookingLocalService.deleteCalendarBooking(calendarBookingId);
}
