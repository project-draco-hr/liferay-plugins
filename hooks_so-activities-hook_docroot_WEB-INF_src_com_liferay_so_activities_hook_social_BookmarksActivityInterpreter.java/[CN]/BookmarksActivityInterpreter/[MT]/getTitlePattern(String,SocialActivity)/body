{
  if (activity.getType() == SocialActivityKeyConstants.BOOKMARKS_ADD_ENTRY) {
    return "added-a-new-bookmark";
  }
 else   if (activity.getType() == SocialActivityKeyConstants.BOOKMARKS_UPDATE_ENTRY) {
    return "updated-a-bookmark";
  }
  return StringPool.BLANK;
}
