{
  if (activitySet.getType() == SocialActivityKeyConstants.BOOKMARKS_ADD_ENTRY) {
    return "added-x-new-bookmarks";
  }
 else   if (activitySet.getType() == SocialActivityKeyConstants.BOOKMARKS_UPDATE_ENTRY) {
    return "made-x-updates-to-a-bookmark";
  }
  return StringPool.BLANK;
}
