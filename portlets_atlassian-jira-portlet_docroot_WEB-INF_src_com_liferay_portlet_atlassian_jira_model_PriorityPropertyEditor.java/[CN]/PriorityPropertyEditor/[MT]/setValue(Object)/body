{
  if (value == null) {
    return;
  }
  if (value instanceof Priority) {
    _type=(Priority)value;
  }
 else   if (value instanceof String) {
    _type=Priority.convert((String)value);
  }
 else {
    throw new IllegalArgumentException("Invalid priority text:" + value);
  }
}
