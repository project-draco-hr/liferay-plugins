{
  if (isEscapedModel()) {
    return this;
  }
 else {
    return (HRExpenseAccount)Proxy.newProxyInstance(HRExpenseAccount.class.getClassLoader(),new Class[]{HRExpenseAccount.class},new AutoEscapeBeanHandler(this));
  }
}
