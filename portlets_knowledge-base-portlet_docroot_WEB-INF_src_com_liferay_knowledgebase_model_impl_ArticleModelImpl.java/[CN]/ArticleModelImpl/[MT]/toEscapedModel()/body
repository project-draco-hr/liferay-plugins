{
  if (isEscapedModel()) {
    return (Article)this;
  }
 else {
    return (Article)Proxy.newProxyInstance(Article.class.getClassLoader(),new Class[]{Article.class},new AutoEscapeBeanHandler(this));
  }
}
