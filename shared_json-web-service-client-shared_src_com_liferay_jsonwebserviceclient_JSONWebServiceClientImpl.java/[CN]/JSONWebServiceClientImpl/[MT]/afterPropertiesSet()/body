{
  PoolingClientConnectionManager poolingClientConnectionManager=new PoolingClientConnectionManager();
  poolingClientConnectionManager.setMaxTotal(20);
  _defaultHttpClient=new DefaultHttpClient(poolingClientConnectionManager);
  HttpParams httpParams=_defaultHttpClient.getParams();
  httpParams.setIntParameter(CoreConnectionPNames.CONNECTION_TIMEOUT,60000);
}
