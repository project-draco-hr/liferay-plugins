{
  String requestId=message.getResponseId();
  RequestRegistryEntry entry=_entries.get(requestId);
  ReportResultContainer results=(ReportResultContainer)message.getPayload();
  entry.setResults(results);
}
