{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(WorkflowDefinition workflowDefinition) method. Use update(WorkflowDefinition workflowDefinition, boolean merge) instead.");
  }
  return update(workflowDefinition,false);
}
