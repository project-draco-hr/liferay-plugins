{
  if (value == null) {
    return;
  }
  if (value instanceof IssueType) {
    _type=(IssueType)value;
  }
 else   if (value instanceof String) {
    _type=IssueType.convert((String)value);
  }
 else {
    throw new IllegalArgumentException("Invalid issue type text:" + value);
  }
}
