{
  if (isEscapedModel()) {
    return this;
  }
 else {
    return (HRExpenseCurrency)Proxy.newProxyInstance(HRExpenseCurrency.class.getClassLoader(),new Class[]{HRExpenseCurrency.class},new AutoEscapeBeanHandler(this));
  }
}
