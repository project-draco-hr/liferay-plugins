{
  try {
    _portalKaleoManager.deployKaleoDefaults(company);
  }
 catch (  Exception e) {
    if (_log.isErrorEnabled()) {
      _log.error("Unable to initialize kaleo workflow defaults",e);
    }
  }
}
