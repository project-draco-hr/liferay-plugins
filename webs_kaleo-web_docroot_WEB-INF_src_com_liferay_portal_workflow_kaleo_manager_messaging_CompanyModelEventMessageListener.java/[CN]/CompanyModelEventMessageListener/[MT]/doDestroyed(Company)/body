{
  try {
    _portalKaleoManager.deleteKaleoData(company);
  }
 catch (  Exception e) {
    if (_log.isErrorEnabled()) {
      _log.error("Unable to delete kaleo workflow data on company deletion",e);
    }
  }
}
