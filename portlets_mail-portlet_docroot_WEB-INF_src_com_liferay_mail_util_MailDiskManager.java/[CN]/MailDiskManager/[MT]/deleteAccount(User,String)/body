{
  String accountPath=_getAccountPath(user,emailAddress);
  FileUtil.deltree(accountPath);
  try {
    MessageIndexer.deleteMessages(user.getCompanyId(),user.getUserId(),emailAddress);
  }
 catch (  Exception e) {
    _log.error(e,e);
  }
}
