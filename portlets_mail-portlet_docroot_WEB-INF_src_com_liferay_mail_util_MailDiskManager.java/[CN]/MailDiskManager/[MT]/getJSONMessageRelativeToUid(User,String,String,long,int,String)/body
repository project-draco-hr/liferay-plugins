{
  searchString=searchString.trim();
  String[] messageUids=null;
  if (searchString.equals(StringPool.BLANK)) {
    messageUids=_getMessageUidsByFolder(user,emailAddress,folderName);
  }
 else {
    messageUids=_getMessageUidsBySearch(user,emailAddress,folderName,searchString);
  }
  for (int i=0; i < messageUids.length; i++) {
    long tempMessageUid=GetterUtil.getLong(messageUids[i]);
    if (messageUid == tempMessageUid) {
      return getJSONMessageByUid(user,emailAddress,folderName,messageUids[i + offset]);
    }
  }
  return null;
}
