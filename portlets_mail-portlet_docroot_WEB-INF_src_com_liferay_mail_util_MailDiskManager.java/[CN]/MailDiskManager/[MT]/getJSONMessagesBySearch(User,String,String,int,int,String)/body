{
  searchString=searchString.trim();
  long[] messageUids=_getMessageUidsBySearch(user,emailAddress,folderName,searchString);
  int messageCount=messageUids.length;
  return _getJSONPaginatedMessages(user,emailAddress,folderName,messageUids,pageNumber,messagesPerPage,messageCount,messageCount);
}
