{
  String messagePath=getMessagePath(user,emailAddress,folderName,messageUid);
  FileUtil.deltree(messagePath);
  try {
    Indexer.deleteMessage(user.getCompanyId(),user.getUserId(),emailAddress,folderName,messageUid);
  }
 catch (  Exception e) {
    _log.error(e,e);
  }
}
