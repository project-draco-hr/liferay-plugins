{
  searchString=searchString.trim();
  String[] messageUids=getMessageUidsBySearch(user,mailAccount,folderName,searchString);
  int messageCount=messageUids.length;
  return getPaginatedJSONMessages(user,mailAccount,folderName,messageUids,pageNumber,messagesPerPage,messageCount,messageCount);
}
