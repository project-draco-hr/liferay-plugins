{
  searchString=searchString.trim();
  String[] messageUids=_getMessageUidsBySearch(user,mailAccount,folderName,searchString);
  int messageCount=messageUids.length;
  return _getJSONPaginatedMessages(user,mailAccount,folderName,messageUids,pageNumber,messagesPerPage,messageCount,messageCount);
}
