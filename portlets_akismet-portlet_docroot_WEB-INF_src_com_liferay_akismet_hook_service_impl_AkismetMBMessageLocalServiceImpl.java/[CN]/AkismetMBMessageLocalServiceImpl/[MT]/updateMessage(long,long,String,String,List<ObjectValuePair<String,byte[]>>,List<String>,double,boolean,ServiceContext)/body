{
  if (_isSpam(userId,body,serviceContext)) {
    throw new MessageBodyException();
  }
  return super.updateMessage(userId,messageId,subject,body,files,existingFiles,priority,allowPingbacks,serviceContext);
}
