{
  if (isSpam(userId,body,serviceContext)) {
    throw new MessageBodyException();
  }
  return super.addMessage(userId,userName,groupId,categoryId,threadId,parentMessageId,subject,body,files,anonymous,priority,allowPingbacks,serviceContext);
}
