{
  for (  BlobEntry blobEntry : blobEntries) {
    if (EntityCacheUtil.getResult(BlobEntryModelImpl.ENTITY_CACHE_ENABLED,BlobEntryImpl.class,blobEntry.getPrimaryKey()) == null) {
      cacheResult(blobEntry);
    }
 else {
      blobEntry.resetOriginalValues();
    }
  }
}
