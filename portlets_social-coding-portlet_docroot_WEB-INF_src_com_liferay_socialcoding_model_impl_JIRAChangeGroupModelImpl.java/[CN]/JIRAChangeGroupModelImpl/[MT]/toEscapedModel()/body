{
  if (isEscapedModel()) {
    return (JIRAChangeGroup)this;
  }
 else {
    return (JIRAChangeGroup)Proxy.newProxyInstance(_classLoader,_escapedModelProxyInterfaces,new AutoEscapeBeanHandler(this));
  }
}
