{
  if (isEscapedModel()) {
    return (JIRAChangeGroup)this;
  }
 else {
    return (JIRAChangeGroup)Proxy.newProxyInstance(JIRAChangeGroup.class.getClassLoader(),new Class[]{JIRAChangeGroup.class},new AutoEscapeBeanHandler(this));
  }
}
