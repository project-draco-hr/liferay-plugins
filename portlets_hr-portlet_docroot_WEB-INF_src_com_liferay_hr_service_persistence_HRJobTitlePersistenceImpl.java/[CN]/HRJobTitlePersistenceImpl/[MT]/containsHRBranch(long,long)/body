{
  Object[] finderArgs=new Object[]{pk,hrBranchPK};
  Boolean value=(Boolean)FinderCacheUtil.getResult(FINDER_PATH_CONTAINS_HRBRANCH,finderArgs,this);
  if (value == null) {
    try {
      value=Boolean.valueOf(containsHRBranch.contains(pk,hrBranchPK));
    }
 catch (    Exception e) {
      throw processException(e);
    }
 finally {
      if (value == null) {
        value=Boolean.FALSE;
      }
      FinderCacheUtil.putResult(FINDER_PATH_CONTAINS_HRBRANCH,finderArgs,value);
    }
  }
  return value.booleanValue();
}
