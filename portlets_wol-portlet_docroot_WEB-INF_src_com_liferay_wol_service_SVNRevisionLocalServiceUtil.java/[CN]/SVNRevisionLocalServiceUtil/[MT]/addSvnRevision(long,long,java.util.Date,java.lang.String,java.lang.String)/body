{
  SVNRevisionLocalService svnRevisionLocalService=SVNRevisionLocalServiceFactory.getService();
  return svnRevisionLocalService.addSvnRevision(svnRepositoryId,revisionNumber,date,author,comments);
}
