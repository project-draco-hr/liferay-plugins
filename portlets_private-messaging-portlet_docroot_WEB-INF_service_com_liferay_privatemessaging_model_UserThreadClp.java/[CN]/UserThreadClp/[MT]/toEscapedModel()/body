{
  if (isEscapedModel()) {
    return this;
  }
 else {
    return (UserThread)Proxy.newProxyInstance(UserThread.class.getClassLoader(),new Class[]{UserThread.class},new AutoEscapeBeanHandler(this));
  }
}
