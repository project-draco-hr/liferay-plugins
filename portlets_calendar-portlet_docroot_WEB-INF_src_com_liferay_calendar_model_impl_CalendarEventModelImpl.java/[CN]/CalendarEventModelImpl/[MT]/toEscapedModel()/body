{
  if (isEscapedModel()) {
    return (CalendarEvent)this;
  }
 else {
    return (CalendarEvent)Proxy.newProxyInstance(_classLoader,_escapedModelProxyInterfaces,new AutoEscapeBeanHandler(this));
  }
}
