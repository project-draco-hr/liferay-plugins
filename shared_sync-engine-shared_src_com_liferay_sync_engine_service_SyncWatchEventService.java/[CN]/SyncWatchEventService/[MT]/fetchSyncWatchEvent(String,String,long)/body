{
  try {
    return _syncWatchEventPersistence.fetchSyncWatchEvent(filePathName,kindName,timestamp);
  }
 catch (  SQLException sqle) {
    if (_logger.isDebugEnabled()) {
      _logger.debug(sqle.getMessage(),sqle);
    }
    return null;
  }
}
