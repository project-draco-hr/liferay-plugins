{
  if (!queryConfig.isHighlightEnabled()) {
    return;
  }
  Map<String,HighlightField> highlightFields=hit.getHighlightFields();
  if ((highlightFields == null) || highlightFields.isEmpty()) {
    return;
  }
  addSnippets(document,highlightFields,Field.CONTENT,queryConfig.getLocale());
  addSnippets(document,highlightFields,Field.DESCRIPTION,queryConfig.getLocale());
  addSnippets(document,highlightFields,Field.TITLE,queryConfig.getLocale());
}
