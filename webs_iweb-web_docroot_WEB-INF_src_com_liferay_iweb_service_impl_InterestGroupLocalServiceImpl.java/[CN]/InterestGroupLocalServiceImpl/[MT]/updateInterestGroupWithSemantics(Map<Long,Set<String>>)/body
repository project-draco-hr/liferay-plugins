{
  Set<Map.Entry<Long,Set<String>>> entries=interestGroupSemanticsMap.entrySet();
  for (  Map.Entry<Long,Set<String>> entry : entries) {
    updateInterestGroupWithSemantics(entry.getKey(),entry.getValue());
  }
}
