{
  Session session=null;
  try {
    session=openSession();
    return (TasksEntry)session.get(TasksEntryImpl.class,new Long(tasksEntryId));
  }
 catch (  Exception e) {
    throw processException(e);
  }
 finally {
    closeSession(session);
  }
}
