{
  TasksEntry tasksEntry=fetchByPrimaryKey(tasksEntryId);
  if (tasksEntry == null) {
    if (_log.isWarnEnabled()) {
      _log.warn("No TasksEntry exists with the primary key " + tasksEntryId);
    }
    throw new NoSuchTasksEntryException("No TasksEntry exists with the primary key " + tasksEntryId);
  }
  return tasksEntry;
}
