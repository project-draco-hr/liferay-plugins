{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(TasksEntry tasksEntry) method. Use update(TasksEntry tasksEntry, boolean merge) instead.");
  }
  return update(tasksEntry,false);
}
