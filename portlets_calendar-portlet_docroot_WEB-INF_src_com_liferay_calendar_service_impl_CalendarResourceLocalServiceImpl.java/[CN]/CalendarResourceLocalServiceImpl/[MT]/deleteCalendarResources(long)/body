{
  List<CalendarResource> calendarResources=calendarResourcePersistence.findByGroupId(groupId);
  for (  CalendarResource calendarResource : calendarResources) {
    deleteCalendarResource(calendarResource);
  }
}
