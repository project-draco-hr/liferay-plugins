{
  if (isEscapedModel()) {
    return (KaleoCondition)this;
  }
 else {
    return (KaleoCondition)Proxy.newProxyInstance(_classLoader,_escapedModelProxyInterfaces,new AutoEscapeBeanHandler(this));
  }
}
