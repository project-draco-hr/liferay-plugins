{
  Path filePath=Paths.get(syncWatchEvent.getFilePathName());
  SyncFile syncFile=SyncFileService.fetchSyncFile(filePath.toString(),_syncAccountId);
  if (syncFile == null) {
    return;
  }
  SyncFileService.deleteFolderSyncFile(_syncAccountId,syncFile);
}
