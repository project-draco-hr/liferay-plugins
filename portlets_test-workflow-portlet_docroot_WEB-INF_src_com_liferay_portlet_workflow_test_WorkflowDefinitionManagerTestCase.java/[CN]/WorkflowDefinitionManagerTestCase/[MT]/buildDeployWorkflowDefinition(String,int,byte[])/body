{
  ResourceRetriever contentRetriever=null;
  if (workflowDefinitionContent != null) {
    contentRetriever=new ByteArrayResourceRetriever(workflowDefinitionContent);
  }
  return new DefaultWorkflowDefinition(contentRetriever,workflowDefinitionName,workflowDefinitionVersion,null);
}
