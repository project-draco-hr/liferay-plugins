{
  String accountPath=MailDiskManager.getAccountPath(_user,_mailAccount.getEmailAddress());
  FileUtil.deltree(accountPath);
  JSONObject jsonObj=JSONFactoryUtil.createJSONObject();
  jsonObj.put("success",true);
  return jsonObj;
}
