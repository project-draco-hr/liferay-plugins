{
  try {
    IMAPFolder imapFolder=(IMAPFolder)folder;
    String jsonMessage=getJSONMessage(imapFolder,message).toString();
    String filePath=MailDiskManager.getMessageFilePath(_user,_mailAccount.getEmailAddress(),folder.getFullName(),imapFolder.getUID(message));
    FileUtil.write(filePath,jsonMessage);
  }
 catch (  IOException ioe) {
    _log.error(ioe,ioe);
  }
catch (  MessagingException me) {
    _log.error(me,me);
  }
}
