{
  Session session=null;
  try {
    session=openSession();
    return (KBArticleResource)session.get(KBArticleResourceImpl.class,new Long(resourcePrimKey));
  }
 catch (  Exception e) {
    throw processException(e);
  }
 finally {
    closeSession(session);
  }
}
