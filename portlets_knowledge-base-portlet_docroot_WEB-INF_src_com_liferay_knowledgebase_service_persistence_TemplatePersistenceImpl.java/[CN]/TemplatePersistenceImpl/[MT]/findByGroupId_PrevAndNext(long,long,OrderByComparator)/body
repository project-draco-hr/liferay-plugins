{
  Template template=findByPrimaryKey(templateId);
  Session session=null;
  try {
    session=openSession();
    Template[] array=new TemplateImpl[3];
    array[0]=getByGroupId_PrevAndNext(session,template,groupId,orderByComparator,true);
    array[1]=template;
    array[2]=getByGroupId_PrevAndNext(session,template,groupId,orderByComparator,false);
    return array;
  }
 catch (  Exception e) {
    throw processException(e);
  }
 finally {
    closeSession(session);
  }
}
