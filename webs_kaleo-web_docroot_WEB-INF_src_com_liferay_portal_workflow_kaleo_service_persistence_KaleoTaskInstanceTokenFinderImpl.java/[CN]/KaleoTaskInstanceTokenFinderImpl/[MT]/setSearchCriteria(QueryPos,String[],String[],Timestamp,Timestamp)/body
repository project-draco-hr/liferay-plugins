{
  if (((taskNames == null) || (taskNames.length == 0)) && ((assetTypes == null) || (assetTypes.length == 0)) && (dueDateGT_TS == null)&& (dueDateLT_TS == null)) {
    return;
  }
  if (taskNames != null) {
    qPos.add(taskNames,2);
  }
  if (assetTypes != null) {
    qPos.add(assetTypes,2);
  }
  setDueDate(qPos,dueDateGT_TS,dueDateLT_TS);
}
