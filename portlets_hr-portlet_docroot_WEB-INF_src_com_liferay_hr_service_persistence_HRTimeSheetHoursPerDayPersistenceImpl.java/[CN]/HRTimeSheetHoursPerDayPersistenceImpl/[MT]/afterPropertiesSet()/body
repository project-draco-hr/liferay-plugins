{
  String[] listenerClassNames=StringUtil.split(GetterUtil.getString(com.liferay.util.service.ServiceProps.get("value.object.listener.com.liferay.hr.model.HRTimeSheetHoursPerDay")));
  if (listenerClassNames.length > 0) {
    try {
      List<ModelListener<HRTimeSheetHoursPerDay>> listenersList=new ArrayList<ModelListener<HRTimeSheetHoursPerDay>>();
      for (      String listenerClassName : listenerClassNames) {
        listenersList.add((ModelListener<HRTimeSheetHoursPerDay>)InstanceFactory.newInstance(listenerClassName));
      }
      listeners=listenersList.toArray(new ModelListener[listenersList.size()]);
    }
 catch (    Exception e) {
      _log.error(e);
    }
  }
}
