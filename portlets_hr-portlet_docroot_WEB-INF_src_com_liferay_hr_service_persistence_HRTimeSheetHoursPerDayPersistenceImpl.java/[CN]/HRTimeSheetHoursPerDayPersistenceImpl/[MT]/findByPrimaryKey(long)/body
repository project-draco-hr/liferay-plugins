{
  HRTimeSheetHoursPerDay hrTimeSheetHoursPerDay=fetchByPrimaryKey(hrTimeSheetHoursPerDay);
  if (hrTimeSheetHoursPerDay == null) {
    if (_log.isWarnEnabled()) {
      _log.warn(_NO_SUCH_ENTITY_WITH_PRIMARY_KEY + hrTimeSheetHoursPerDay);
    }
    throw new NoSuchTimeSheetHoursPerDayException(_NO_SUCH_ENTITY_WITH_PRIMARY_KEY + hrTimeSheetHoursPerDay);
  }
  return hrTimeSheetHoursPerDay;
}
