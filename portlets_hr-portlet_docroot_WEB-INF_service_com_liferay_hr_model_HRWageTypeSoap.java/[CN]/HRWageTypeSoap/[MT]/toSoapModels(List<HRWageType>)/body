{
  List<HRWageTypeSoap> soapModels=new ArrayList<HRWageTypeSoap>(models.size());
  for (  HRWageType model : models) {
    soapModels.add(toSoapModel(model));
  }
  return soapModels.toArray(new HRWageTypeSoap[soapModels.size()]);
}
