{
  byte[] definitionData=FileUtil.getBytes(inputStream);
  try {
    WorkflowDefinitionManagerUtil.validateWorkflowDefinition(definitionData);
  }
 catch (  WorkflowException we) {
    fail(we.getMessage());
  }
}
