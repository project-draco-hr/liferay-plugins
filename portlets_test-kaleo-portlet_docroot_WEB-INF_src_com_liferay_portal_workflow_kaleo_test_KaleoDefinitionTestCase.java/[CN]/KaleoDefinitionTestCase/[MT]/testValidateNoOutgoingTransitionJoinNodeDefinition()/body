{
  InputStream inputStream=getResource("/META-INF/definitions/no-outgoing-join.xml");
  String error=assertInvalid(inputStream);
  assertEquals("No outgoing transition found for join join",error);
}
