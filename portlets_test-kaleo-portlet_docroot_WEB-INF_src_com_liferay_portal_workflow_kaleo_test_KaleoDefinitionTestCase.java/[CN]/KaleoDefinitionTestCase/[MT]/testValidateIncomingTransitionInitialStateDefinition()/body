{
  InputStream inputStream=getResource("/META-INF/definitions/incoming-initial-state.xml");
  String error=assertInvalid(inputStream);
  assertEquals("Incoming transitions were found for initial state start",error);
}
