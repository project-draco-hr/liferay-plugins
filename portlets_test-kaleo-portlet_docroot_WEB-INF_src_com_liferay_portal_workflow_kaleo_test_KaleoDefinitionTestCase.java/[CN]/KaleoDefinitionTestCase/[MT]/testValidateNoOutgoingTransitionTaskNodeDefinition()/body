{
  InputStream inputStream=getResource("/META-INF/definitions/no-outgoing-task.xml");
  String error=assertInvalid(inputStream);
  assertEquals("No outgoing transition found for task task",error);
}
