{
  hrTimeOffType=toUnwrappedModel(hrTimeOffType);
  Session session=null;
  try {
    session=openSession();
    BatchSessionUtil.delete(session,hrTimeOffType);
  }
 catch (  Exception e) {
    throw processException(e);
  }
 finally {
    closeSession(session);
  }
  FinderCacheUtil.clearCache(FINDER_CLASS_NAME_LIST);
  EntityCacheUtil.removeResult(HRTimeOffTypeModelImpl.ENTITY_CACHE_ENABLED,HRTimeOffTypeImpl.class,hrTimeOffType.getPrimaryKey());
  return hrTimeOffType;
}
