{
  Session session=null;
  try {
    session=testBlobEntryPersistence.openSession();
    return (com.liferay.testblob.model.TestBlobEntryBlobFieldBlobModel)session.get(TestBlobEntryBlobFieldBlobModel.class,primaryKey);
  }
 catch (  Exception e) {
    throw testBlobEntryPersistence.processException(e);
  }
 finally {
    testBlobEntryPersistence.closeSession(session);
  }
}
