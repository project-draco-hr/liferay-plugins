{
  if (isEscapedModel()) {
    return (HRClient)this;
  }
 else {
    return (HRClient)Proxy.newProxyInstance(HRClient.class.getClassLoader(),new Class[]{HRClient.class},new AutoEscapeBeanHandler(this));
  }
}
