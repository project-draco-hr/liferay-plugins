{
  if (isEscapedModel()) {
    return (HRExpenseType)this;
  }
 else {
    return (HRExpenseType)Proxy.newProxyInstance(_classLoader,_escapedModelProxyInterfaces,new AutoEscapeBeanHandler(this));
  }
}
