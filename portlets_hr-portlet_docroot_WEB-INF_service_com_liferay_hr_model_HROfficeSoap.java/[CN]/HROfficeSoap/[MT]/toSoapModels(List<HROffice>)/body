{
  List<HROfficeSoap> soapModels=new ArrayList<HROfficeSoap>(models.size());
  for (  HROffice model : models) {
    soapModels.add(toSoapModel(model));
  }
  return soapModels.toArray(new HROfficeSoap[soapModels.size()]);
}
