{
  int buildNumber=SyncPropsService.getInteger("buildNumber");
  if (buildNumber == 0) {
    createTables();
  }
 else   if (buildNumber == ReleaseInfo.getBuildNumber()) {
    return;
  }
  SyncPropsService.addSyncProps("buildNumber",ReleaseInfo.getBuildNumber());
}
