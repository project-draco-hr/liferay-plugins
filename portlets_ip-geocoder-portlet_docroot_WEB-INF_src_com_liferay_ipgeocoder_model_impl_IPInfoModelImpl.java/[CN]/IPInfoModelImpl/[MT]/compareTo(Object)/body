{
  if (obj == null) {
    return -1;
  }
  IPInfoImpl ipInfo=(IPInfoImpl)obj;
  long pk=ipInfo.getPrimaryKey();
  if (getPrimaryKey() < pk) {
    return -1;
  }
 else   if (getPrimaryKey() > pk) {
    return 1;
  }
 else {
    return 0;
  }
}
