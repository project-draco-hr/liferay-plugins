{
  if (isEscapedModel()) {
    return (KaleoTransition)this;
  }
 else {
    return (KaleoTransition)Proxy.newProxyInstance(_classLoader,_escapedModelProxyInterfaces,new AutoEscapeBeanHandler(this));
  }
}
