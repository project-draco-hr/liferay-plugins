{
  String sitemapJSON=FileUtil.read(sitemapJSONFile);
  return StringUtil.replace(sitemapJSON,new String[]{"${companyId}","${groupId}","${groupId}"},new String[]{String.valueOf(companyId),String.valueOf(groupId),String.valueOf(userId)});
}
