{
  if (fileExtension.equals(TemplateConstants.LANG_TYPE_CSS) || fileExtension.equals(TemplateConstants.LANG_TYPE_FTL) || fileExtension.equals(TemplateConstants.LANG_TYPE_VM)|| fileExtension.equals(TemplateConstants.LANG_TYPE_XSL)) {
    return fileExtension;
  }
  return TemplateConstants.LANG_TYPE_VM;
}
