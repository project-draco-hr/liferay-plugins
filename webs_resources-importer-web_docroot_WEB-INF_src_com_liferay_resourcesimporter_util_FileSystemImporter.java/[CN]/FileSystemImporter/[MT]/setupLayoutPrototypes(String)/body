{
  JSONObject jsonObject=getJSONObject(fileName);
  JSONArray layoutPrototypesJSONArray=jsonObject.getJSONArray("layoutPrototypes");
  if (layoutPrototypesJSONArray == null) {
    return;
  }
  for (int i=0; i < layoutPrototypesJSONArray.length(); i++) {
    JSONObject layoutPrototypeJSONObject=layoutPrototypesJSONArray.getJSONObject(i);
    addLayoutPrototype(layoutPrototypeJSONObject);
  }
}
