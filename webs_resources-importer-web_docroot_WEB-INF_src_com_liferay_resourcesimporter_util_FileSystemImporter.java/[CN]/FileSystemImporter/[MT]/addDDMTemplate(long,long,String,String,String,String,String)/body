{
  fileName=FileUtil.getShortFileName(fileName);
  fileName=FileUtil.stripExtension(fileName);
  String name=getName(fileName);
  String templateKey=getKey(fileName);
  DDMTemplate ddmTemplate=DDMTemplateLocalServiceUtil.fetchTemplate(groupId,PortalUtil.getClassNameId(DDMStructure.class),templateKey);
  if (ddmTemplate != null) {
    if (!developerModeEnabled) {
      if (_log.isInfoEnabled()) {
        _log.info("DDM template with name " + name + " and version "+ version+ " already exists");
      }
      return;
    }
    DDMTemplateLocalServiceUtil.deleteTemplate(ddmTemplate);
  }
  DDMTemplateLocalServiceUtil.addTemplate(userId,templateGroupId,PortalUtil.getClassNameId(DDMStructure.class),ddmStructureId,templateKey,getMap(name),null,type,mode,language,script,false,false,StringPool.BLANK,null,serviceContext);
}
