{
  File resourceDir=file;
  while (resourceDir != null) {
    resourceDir=resourceDir.getParentFile();
    String name=resourceDir.getName();
    if (resourceDir.isDirectory() && name.equals("resources-importer")) {
      return resourceDir;
    }
  }
  return null;
}
