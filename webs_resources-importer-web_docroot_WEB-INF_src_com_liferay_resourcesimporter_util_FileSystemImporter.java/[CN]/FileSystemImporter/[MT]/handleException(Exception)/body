{
  if (continueOnFailure) {
    if (_log.isErrorEnabled()) {
      if (verbose) {
        _log.error(e);
      }
 else {
        _log.error(e.toString() + "@" + e.getStackTrace()[0]);
      }
    }
  }
 else {
    throw e;
  }
}
