{
  if (isEscapedModel()) {
    return this;
  }
 else {
    return (Definition)Proxy.newProxyInstance(Definition.class.getClassLoader(),new Class[]{Definition.class},new AutoEscapeBeanHandler(this));
  }
}
