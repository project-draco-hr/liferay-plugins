{
  if (isEscapedModel()) {
    return this;
  }
 else {
    return (HRTimeSheetHoursPerDay)Proxy.newProxyInstance(HRTimeSheetHoursPerDay.class.getClassLoader(),new Class[]{HRTimeSheetHoursPerDay.class},new AutoEscapeBeanHandler(this));
  }
}
