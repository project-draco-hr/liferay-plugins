{
  Object[] finderArgs=new Object[]{pk,kbArticlePK};
  Boolean value=(Boolean)FinderCacheUtil.getResult(FINDER_PATH_CONTAINS_KBARTICLE,finderArgs,this);
  if (value == null) {
    try {
      value=Boolean.valueOf(containsKBArticle.contains(pk,kbArticlePK));
    }
 catch (    Exception e) {
      throw processException(e);
    }
 finally {
      if (value == null) {
        value=Boolean.FALSE;
      }
      FinderCacheUtil.putResult(FINDER_PATH_CONTAINS_KBARTICLE,finderArgs,value);
    }
  }
  return value.booleanValue();
}
