{
  AWSCredentials awsCredentials=new BasicAWSCredentials(accessKey,secretKey);
  AmazonIdentityManagementClient amazonIdentityManagementClient=new AmazonIdentityManagementClient(awsCredentials);
  return amazonIdentityManagementClient;
}
