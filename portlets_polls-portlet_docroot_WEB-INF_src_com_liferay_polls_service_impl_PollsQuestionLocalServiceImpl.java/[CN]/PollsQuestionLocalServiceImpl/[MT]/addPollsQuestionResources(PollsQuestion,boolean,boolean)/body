{
  resourceLocalService.addResources(pollsQuestion.getCompanyId(),pollsQuestion.getGroupId(),pollsQuestion.getUserId(),PollsQuestion.class.getName(),pollsQuestion.getPollsQuestionId(),false,addGroupPermissions,addGuestPermissions);
}
