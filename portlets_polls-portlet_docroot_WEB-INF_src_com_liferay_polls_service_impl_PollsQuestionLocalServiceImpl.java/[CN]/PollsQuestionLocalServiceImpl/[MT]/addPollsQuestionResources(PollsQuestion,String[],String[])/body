{
  resourceLocalService.addModelResources(pollsQuestion.getCompanyId(),pollsQuestion.getGroupId(),pollsQuestion.getUserId(),PollsQuestion.class.getName(),pollsQuestion.getPollsQuestionId(),groupPermissions,guestPermissions);
}
