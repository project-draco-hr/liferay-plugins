{
  PermissionChecker permissionChecker=getPermissionChecker();
  PortletPermissionUtil.check(permissionChecker,plid,"1","ADD_PRODUCER");
  return producerLocalService.addProducer(getUserId(),plid,name,wsdlURL);
}
