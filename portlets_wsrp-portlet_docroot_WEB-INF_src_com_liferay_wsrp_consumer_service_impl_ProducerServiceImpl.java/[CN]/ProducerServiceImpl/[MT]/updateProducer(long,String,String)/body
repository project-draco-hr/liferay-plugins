{
  PermissionChecker permissionChecker=getPermissionChecker();
  ProducerPermission.check(permissionChecker,producerId,ActionKeys.UPDATE);
  producerLocalService.updateProducer(producerId,name,wsdlURL);
}
