{
  if (isEscapedModel()) {
    return (KBTemplate)this;
  }
 else {
    return (KBTemplate)Proxy.newProxyInstance(KBTemplate.class.getClassLoader(),new Class[]{KBTemplate.class},new AutoEscapeBeanHandler(this));
  }
}
