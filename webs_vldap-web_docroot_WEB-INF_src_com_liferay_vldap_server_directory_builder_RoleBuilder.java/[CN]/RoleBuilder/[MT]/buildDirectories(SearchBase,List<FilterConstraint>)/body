{
  List<Directory> directories=new ArrayList<Directory>();
  List<Role> roles=getRoles(searchBase,filterConstraints);
  for (  Role role : roles) {
    Directory directory=new RoleDirectory(searchBase.getTop(),searchBase.getCompany(),role);
    directories.add(directory);
  }
  return directories;
}
