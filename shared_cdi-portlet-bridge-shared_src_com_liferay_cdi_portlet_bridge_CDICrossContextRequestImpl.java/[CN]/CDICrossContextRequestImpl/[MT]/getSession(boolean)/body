{
  if (_httpSession == null) {
    HttpServletRequest httpServletRequest=getRequest();
    HttpSession session=new CDICrossContextSessionImpl(httpServletRequest.getSession(create));
    if (create) {
      _httpSession=session;
    }
    return session;
  }
 else {
    return _httpSession;
  }
}
