{
  int count=countByG_P_S_L(groupId,parentResourcePrimKey,sections,latest);
  List<KBArticle> list=findByG_P_S_L(groupId,parentResourcePrimKey,sections,latest,count - 1,count,orderByComparator);
  if (!list.isEmpty()) {
    return list.get(0);
  }
  return null;
}
