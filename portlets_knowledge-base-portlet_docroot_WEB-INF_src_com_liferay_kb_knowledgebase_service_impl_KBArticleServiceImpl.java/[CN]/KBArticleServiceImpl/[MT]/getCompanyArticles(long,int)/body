{
  List<KBArticle> articles=kbArticleLocalService.getCompanyArticles(companyId,true,false,false,0,_MAX_END);
  if (articles == null) {
    return Collections.EMPTY_LIST;
  }
  Iterator<KBArticle> itr=articles.iterator();
  return checkViewPermission(itr,max);
}
