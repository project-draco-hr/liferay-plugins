{
  KBPermission.check(getPermissionChecker(),plid,ActionKeys.SUBSCRIBE);
  Layout layout=LayoutLocalServiceUtil.getLayout(plid);
  kbArticleLocalService.unsubscribe(getUserId(),layout.getGroupId());
}
