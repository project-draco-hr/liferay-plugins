{
  try {
    String wsdl=HttpUtil.URLtoString(url);
    WSRPConsumerManager wsrpConsumerMananger=WSRPConsumerManagerFactory.getWSRPConsumerManager(wsdl);
    return wsdl;
  }
 catch (  Exception e) {
    throw new WSRPConsumerWSDLException(e);
  }
}
