{
  try {
    _transactionManagerMethods.get("commit").invoke(_transactionManager,unwrapTransactionStatus(status));
  }
 catch (  Exception e) {
    throw new TransactionSystemException("Could not commit transaction via remote transaction manager",e);
  }
}
