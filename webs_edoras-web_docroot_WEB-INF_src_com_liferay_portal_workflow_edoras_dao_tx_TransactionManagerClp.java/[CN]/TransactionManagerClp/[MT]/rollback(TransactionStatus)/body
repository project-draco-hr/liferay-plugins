{
  try {
    _transactionManagerMethods.get("rollback").invoke(_transactionManager,unwrapTransactionStatus(status));
  }
 catch (  Exception e) {
    throw new TransactionSystemException("Could not rollback transaction via remote transaction manager",e);
  }
}
