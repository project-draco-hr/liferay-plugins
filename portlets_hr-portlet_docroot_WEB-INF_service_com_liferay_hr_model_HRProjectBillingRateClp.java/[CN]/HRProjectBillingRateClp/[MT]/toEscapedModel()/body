{
  if (isEscapedModel()) {
    return this;
  }
 else {
    return (HRProjectBillingRate)Proxy.newProxyInstance(HRProjectBillingRate.class.getClassLoader(),new Class[]{HRProjectBillingRate.class},new AutoEscapeBeanHandler(this));
  }
}
