{
  if (isEscapedModel()) {
    return (JIRAChangeItem)this;
  }
 else {
    return (JIRAChangeItem)Proxy.newProxyInstance(_classLoader,_escapedModelProxyInterfaces,new AutoEscapeBeanHandler(this));
  }
}
