{
  Map<String,List<String>> mapSuggestions=spellCheckKeywords(searchContext,1);
  List<String> tokens=TokenizerUtil.tokenize(Field.SPELL_CHECK_WORD,searchContext.getKeywords(),searchContext.getLanguageId());
  return CollatorUtil.collate(mapSuggestions,tokens);
}
