{
  _hotDeployMessageListener=new WSRPHotDeployMessageListener(ClpSerializer.getServletContextName());
  MessageBusUtil.registerMessageListener(DestinationNames.HOT_DEPLOY,_hotDeployMessageListener);
}
