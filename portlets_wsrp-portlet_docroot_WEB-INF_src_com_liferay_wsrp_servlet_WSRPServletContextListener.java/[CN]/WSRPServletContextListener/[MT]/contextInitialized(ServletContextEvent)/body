{
  PortalInitableUtil.init(this);
  portalInitThreadClassLoader=PortletClassLoaderUtil.getClassLoader();
  if (portalInitThreadClassLoader == null) {
    portalInitThreadClassLoader=Thread.currentThread().getContextClassLoader();
  }
}
