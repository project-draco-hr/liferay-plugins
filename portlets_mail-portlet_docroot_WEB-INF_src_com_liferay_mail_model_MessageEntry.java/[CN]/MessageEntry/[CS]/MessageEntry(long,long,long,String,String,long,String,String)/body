{
  setCompanyId(companyId);
  setGroupId(groupId);
  setUserId(userId);
  setEmailAddress(emailAddress);
  setFolderName(folderName);
  setMessageUid(messageUid);
  setTitle(title);
  setContent(content);
}
