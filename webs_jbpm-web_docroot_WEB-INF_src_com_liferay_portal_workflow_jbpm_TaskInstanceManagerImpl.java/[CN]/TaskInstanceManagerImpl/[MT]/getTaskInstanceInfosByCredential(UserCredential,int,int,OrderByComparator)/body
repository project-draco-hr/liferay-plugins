{
  long[] roleIds=ArrayUtil.toArray(userCredential.getRoleIds().toArray(new Long[0]));
  List<TaskInstanceInfo> pooledActorTaskInstanceInfos=getTaskInstanceInfos(-1,roleIds,true,null,start,end,orderByComparator);
  List<TaskInstanceInfo> singleActorTaskInstanceInfos=getTaskInstanceInfos(-1,new long[]{userCredential.getUserId()},false,null,start,end,orderByComparator);
  List<TaskInstanceInfo> taskInstanceInfos=new ArrayList<TaskInstanceInfo>(pooledActorTaskInstanceInfos.size() + singleActorTaskInstanceInfos.size());
  taskInstanceInfos.addAll(pooledActorTaskInstanceInfos);
  taskInstanceInfos.addAll(singleActorTaskInstanceInfos);
  return taskInstanceInfos;
}
