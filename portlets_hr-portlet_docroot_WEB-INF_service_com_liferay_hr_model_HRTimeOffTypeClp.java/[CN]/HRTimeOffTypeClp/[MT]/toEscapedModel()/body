{
  if (isEscapedModel()) {
    return this;
  }
 else {
    return (HRTimeOffType)Proxy.newProxyInstance(HRTimeOffType.class.getClassLoader(),new Class[]{HRTimeOffType.class},new AutoEscapeBeanHandler(this));
  }
}
