{
  for (  HRUserTask hrUserTask : hrUserTasks) {
    if (EntityCacheUtil.getResult(HRUserTaskModelImpl.ENTITY_CACHE_ENABLED,HRUserTaskImpl.class,hrUserTask.getPrimaryKey()) == null) {
      cacheResult(hrUserTask);
    }
  }
}
