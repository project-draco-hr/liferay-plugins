{
  if (isEscapedModel()) {
    return this;
  }
 else {
    return (ExpandoValue)Proxy.newProxyInstance(ExpandoValue.class.getClassLoader(),new Class[]{ExpandoValue.class},new AutoEscapeBeanHandler(this));
  }
}
