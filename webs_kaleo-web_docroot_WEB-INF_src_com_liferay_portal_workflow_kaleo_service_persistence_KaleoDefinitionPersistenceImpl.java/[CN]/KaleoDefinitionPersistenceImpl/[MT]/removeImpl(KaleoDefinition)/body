{
  kaleoDefinition=toUnwrappedModel(kaleoDefinition);
  Session session=null;
  try {
    session=openSession();
    BatchSessionUtil.delete(session,kaleoDefinition);
  }
 catch (  Exception e) {
    throw processException(e);
  }
 finally {
    closeSession(session);
  }
  FinderCacheUtil.clearCache(FINDER_CLASS_NAME_LIST_WITH_PAGINATION);
  FinderCacheUtil.clearCache(FINDER_CLASS_NAME_LIST_WITHOUT_PAGINATION);
  KaleoDefinitionModelImpl kaleoDefinitionModelImpl=(KaleoDefinitionModelImpl)kaleoDefinition;
  FinderCacheUtil.removeResult(FINDER_PATH_FETCH_BY_C_N_V,new Object[]{Long.valueOf(kaleoDefinitionModelImpl.getCompanyId()),kaleoDefinitionModelImpl.getName(),Integer.valueOf(kaleoDefinitionModelImpl.getVersion())});
  EntityCacheUtil.removeResult(KaleoDefinitionModelImpl.ENTITY_CACHE_ENABLED,KaleoDefinitionImpl.class,kaleoDefinition.getPrimaryKey());
  return kaleoDefinition;
}
