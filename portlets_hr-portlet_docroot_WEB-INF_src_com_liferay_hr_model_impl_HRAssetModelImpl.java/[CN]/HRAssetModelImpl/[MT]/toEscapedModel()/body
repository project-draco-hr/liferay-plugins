{
  if (isEscapedModel()) {
    return (HRAsset)this;
  }
 else {
    return (HRAsset)Proxy.newProxyInstance(HRAsset.class.getClassLoader(),new Class[]{HRAsset.class},new AutoEscapeBeanHandler(this));
  }
}
