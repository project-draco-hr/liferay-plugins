{
  if (isEscapedModel()) {
    return this;
  }
 else {
    return (KaleoInstance)Proxy.newProxyInstance(KaleoInstance.class.getClassLoader(),new Class[]{KaleoInstance.class},new AutoEscapeBeanHandler(this));
  }
}
