{
  wsrpConsumer=toUnwrappedModel(wsrpConsumer);
  Session session=null;
  try {
    session=openSession();
    if (wsrpConsumer.isCachedModel()) {
      wsrpConsumer=(WSRPConsumer)session.get(WSRPConsumerImpl.class,wsrpConsumer.getPrimaryKeyObj());
    }
    session.delete(wsrpConsumer);
  }
 catch (  Exception e) {
    throw processException(e);
  }
 finally {
    closeSession(session);
  }
  clearCache(wsrpConsumer);
  return wsrpConsumer;
}
