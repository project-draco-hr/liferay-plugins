{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(WSRPConsumer wsrpConsumer) method. Use update(WSRPConsumer wsrpConsumer, boolean merge) instead.");
  }
  return update(wsrpConsumer,false);
}
