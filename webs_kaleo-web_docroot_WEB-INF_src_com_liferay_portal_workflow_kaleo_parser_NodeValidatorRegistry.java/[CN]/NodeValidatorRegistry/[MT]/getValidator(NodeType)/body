{
  NodeValidator nodeValidator=_nodeValidators.get(nodeType);
  if (nodeValidator == null) {
    throw new IllegalArgumentException("No validator found for: " + nodeType);
  }
  return nodeValidator;
}
