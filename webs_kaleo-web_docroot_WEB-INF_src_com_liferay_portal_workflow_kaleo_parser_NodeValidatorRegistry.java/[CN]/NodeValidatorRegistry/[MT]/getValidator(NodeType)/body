{
  NodeValidator<Node> nodeValidator=_nodeValidators.get(nodeType);
  if (nodeValidator == null) {
    throw new IllegalArgumentException("No node validator found for " + nodeType);
  }
  return nodeValidator;
}
