{
  Session session=null;
  try {
    session=openSession();
    return (ProjectMilestone)session.get(ProjectMilestoneImpl.class,new Long(projectMilestoneId));
  }
 catch (  Exception e) {
    throw processException(e);
  }
 finally {
    closeSession(session);
  }
}
