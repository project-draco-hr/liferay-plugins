{
  for (  ModelListener listener : listeners) {
    listener.onBeforeRemove(projectMilestone);
  }
  projectMilestone=removeImpl(projectMilestone);
  for (  ModelListener listener : listeners) {
    listener.onAfterRemove(projectMilestone);
  }
  return projectMilestone;
}
