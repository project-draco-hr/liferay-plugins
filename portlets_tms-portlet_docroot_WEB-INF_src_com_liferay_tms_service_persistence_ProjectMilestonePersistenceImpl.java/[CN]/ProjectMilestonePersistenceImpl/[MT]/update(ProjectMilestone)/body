{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(ProjectMilestone projectMilestone) method. Use update(ProjectMilestone projectMilestone, boolean merge) instead.");
  }
  return update(projectMilestone,false);
}
