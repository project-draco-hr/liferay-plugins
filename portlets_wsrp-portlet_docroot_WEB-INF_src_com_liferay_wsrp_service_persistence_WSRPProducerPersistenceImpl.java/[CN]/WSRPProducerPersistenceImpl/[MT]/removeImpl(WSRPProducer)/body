{
  wsrpProducer=toUnwrappedModel(wsrpProducer);
  Session session=null;
  try {
    session=openSession();
    if (wsrpProducer.isCachedModel()) {
      wsrpProducer=(WSRPProducer)session.get(WSRPProducerImpl.class,wsrpProducer.getPrimaryKeyObj());
    }
    session.delete(wsrpProducer);
  }
 catch (  Exception e) {
    throw processException(e);
  }
 finally {
    closeSession(session);
  }
  clearCache(wsrpProducer);
  return wsrpProducer;
}
