{
  Set<Recipient> recipients=_recipientsMap.get(recipient.getEmailRecipientType());
  if (recipients == null) {
    recipients=new HashSet<Recipient>();
  }
  recipients.add(recipient);
  _recipientsMap.put(recipient.getEmailRecipientType(),recipients);
}
