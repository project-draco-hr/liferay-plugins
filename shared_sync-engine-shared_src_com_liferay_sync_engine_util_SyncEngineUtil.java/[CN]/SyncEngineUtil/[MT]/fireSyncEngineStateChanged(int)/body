{
  for (  final SyncEngineListener syncEngineListener : _syncEngineListeners) {
    _executor.submit(new Runnable(){
      @Override public void run(){
        syncEngineListener.syncEngineStateChanged(syncEngineState);
      }
    }
);
  }
}
