{
  Attachment attachment=AttachmentLocalServiceUtil.getAttachment(attachmentId);
  Mailbox mailbox=MailboxFactoryUtil.getMailbox(_user.getUserId(),attachment.getAccountId());
  return mailbox.getAttachment(attachmentId);
}
