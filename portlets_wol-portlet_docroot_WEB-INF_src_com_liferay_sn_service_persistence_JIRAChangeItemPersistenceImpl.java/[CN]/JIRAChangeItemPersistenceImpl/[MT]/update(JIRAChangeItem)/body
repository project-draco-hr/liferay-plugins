{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(JIRAChangeItem jiraChangeItem) method. Use update(JIRAChangeItem jiraChangeItem, boolean merge) instead.");
  }
  return update(jiraChangeItem,false);
}
