{
  Group group=GroupLocalServiceUtil.getGroup(groupId);
  WSRPProducer producer=WSRPProducerLocalServiceUtil.fetchWSRPProducerByUuidAndCompanyId(uuid,group.getCompanyId());
  if (producer != null) {
    WSRPProducerLocalServiceUtil.deleteWSRPProducer(producer);
  }
}
