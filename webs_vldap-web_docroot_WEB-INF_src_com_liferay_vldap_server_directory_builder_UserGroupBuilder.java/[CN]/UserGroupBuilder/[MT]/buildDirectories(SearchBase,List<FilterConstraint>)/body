{
  List<Directory> directories=new ArrayList<Directory>();
  List<UserGroup> userGroups=getUserGroups(searchBase,filterConstraints);
  for (  UserGroup userGroup : userGroups) {
    Directory directory=new UserGroupDirectory(searchBase.getTop(),searchBase.getCompany(),userGroup);
    directories.add(directory);
  }
  return directories;
}
