{
  Session session=null;
  try {
    session=openSession();
    return (SemanticsFile)session.get(SemanticsFileImpl.class,semanticsURI);
  }
 catch (  Exception e) {
    throw processException(e);
  }
 finally {
    closeSession(session);
  }
}
