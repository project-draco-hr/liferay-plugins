{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(SemanticsFile semanticsFile) method. Use update(SemanticsFile semanticsFile, boolean merge) instead.");
  }
  return update(semanticsFile,false);
}
