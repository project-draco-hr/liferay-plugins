{
  _forwardCookies=new String[0];
  if (Validator.isNotNull(forwardCookies)) {
    forwardCookies=forwardCookies.toLowerCase();
    _forwardCookies=StringUtil.split(forwardCookies,StringPool.COMMA);
  }
}
