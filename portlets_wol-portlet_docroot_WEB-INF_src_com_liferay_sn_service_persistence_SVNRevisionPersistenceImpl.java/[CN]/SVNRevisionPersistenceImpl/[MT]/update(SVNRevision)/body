{
  if (_log.isWarnEnabled()) {
    _log.warn("Using the deprecated update(SVNRevision svnRevision) method. Use update(SVNRevision svnRevision, boolean merge) instead.");
  }
  return update(svnRevision,false);
}
