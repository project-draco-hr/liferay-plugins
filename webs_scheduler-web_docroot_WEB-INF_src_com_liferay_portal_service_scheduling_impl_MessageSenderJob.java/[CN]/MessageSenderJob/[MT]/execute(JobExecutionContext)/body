{
  try {
    JobDetail detail=jobExecutionContext.getJobDetail();
    String messageBody=detail.getJobDataMap().getString(SchedulingEngine.MESSAGE_BODY);
    String destinationName=detail.getJobDataMap().getString(SchedulingEngine.DESTINATION_NAME);
    MessageBusUtil.sendMessage(destinationName,messageBody);
  }
 catch (  Exception e) {
    throw new JobExecutionException("Unable to send message",e);
  }
}
