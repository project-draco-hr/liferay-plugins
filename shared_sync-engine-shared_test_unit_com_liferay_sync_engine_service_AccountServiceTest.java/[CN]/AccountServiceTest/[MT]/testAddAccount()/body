{
  Account account=AccountService.addAccount("test@liferay.com","test","http://localhost:8080/api/jsonws/");
  _account=AccountService.getAccount(account.getAccountId());
  Assert.assertNotNull(_account);
}
