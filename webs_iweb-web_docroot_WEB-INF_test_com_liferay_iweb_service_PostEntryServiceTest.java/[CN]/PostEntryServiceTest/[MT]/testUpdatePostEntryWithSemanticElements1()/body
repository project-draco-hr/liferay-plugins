{
  Set<String> semelements=new TreeSet();
  semelements.add(semanticselemuri1);
  PostEntryLocalServiceUtil.updatePostEntriesWithSemanticsElements(postid1,"blog",semelements);
  semelements.remove(semanticselemuri1);
  semelements.add(semanticselemuri2);
  PostEntryLocalServiceUtil.updatePostEntriesWithSemanticsElements(postid2,"blog",semelements);
  semelements.remove(semanticselemuri2);
  semelements.add(semanticselemuri3);
  PostEntryLocalServiceUtil.updatePostEntriesWithSemanticsElements(postid3,"blog",semelements);
}
