{
  if (isEscapedModel()) {
    return (KaleoTaskAssignment)this;
  }
 else {
    return (KaleoTaskAssignment)Proxy.newProxyInstance(KaleoTaskAssignment.class.getClassLoader(),new Class[]{KaleoTaskAssignment.class},new AutoEscapeBeanHandler(this));
  }
}
