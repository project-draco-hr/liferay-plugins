{
  try {
    if (!context.addPrimaryKey(KBPortletDataHandlerImpl.class,"deleteData")) {
      KBArticleLocalServiceUtil.deleteArticles(context.getGroupId(),false);
    }
    return null;
  }
 catch (  Exception e) {
    throw new PortletDataException(e);
  }
}
