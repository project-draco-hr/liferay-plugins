{
  try {
    _engine.getWriter().deletePortletDocuments(companyId,portletId);
  }
 catch (  Exception e) {
    _log.error("Unable to delete portlet documents: companyId=" + companyId + ", portletId="+ portletId,e);
  }
}
