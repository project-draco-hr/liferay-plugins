{
  try {
    return _engine.getSearcher().search(companyId,query,sort,start,end);
  }
 catch (  Exception e) {
    _log.error("Unable to search: companyId=" + companyId + ", query="+ query+ ", sort="+ sort,e);
    return new HitsImpl();
  }
}
