{
  try {
    return _engine.getSearcher().search(companyId,query,start,end);
  }
 catch (  Exception e) {
    _log.error("Unable to search: companyId=" + companyId + ", query="+ query,e);
    return new HitsImpl();
  }
}
