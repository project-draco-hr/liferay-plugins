{
  while (!remainingPathElement.isEmpty()) {
    PathElement pathElement=remainingPathElement.remove(0);
    follow(pathElement.getStartNode(),pathElement.getTargetNode(),remainingPathElement,pathElement.getExecutionContext());
  }
}
