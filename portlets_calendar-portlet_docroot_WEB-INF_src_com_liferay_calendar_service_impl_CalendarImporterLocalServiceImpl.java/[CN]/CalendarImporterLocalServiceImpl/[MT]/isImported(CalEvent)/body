{
  Group group=groupLocalService.getCompanyGroup(calEvent.getCompanyId());
  CalendarBooking calendarBooking=calendarBookingPersistence.fetchByUUID_G(calEvent.getUuid(),group.getGroupId());
  if (calendarBooking != null) {
    return true;
  }
  return false;
}
