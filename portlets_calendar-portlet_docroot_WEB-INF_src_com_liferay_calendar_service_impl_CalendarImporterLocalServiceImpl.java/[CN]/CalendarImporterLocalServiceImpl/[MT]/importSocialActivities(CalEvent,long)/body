{
  List<SocialActivity> socialActivities=socialActivityPersistence.findByC_C(PortalUtil.getClassNameId(CalEvent.class),calEvent.getEventId());
  for (  SocialActivity socialActivity : socialActivities) {
    importSocialActivity(socialActivity,calendarBookingId);
  }
}
