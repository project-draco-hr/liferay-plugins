{
  if (obj == null) {
    return -1;
  }
  PresenceClp presence=(PresenceClp)obj;
  long pk=presence.getPrimaryKey();
  if (getPrimaryKey() < pk) {
    return -1;
  }
 else   if (getPrimaryKey() > pk) {
    return 1;
  }
 else {
    return 0;
  }
}
