{
  if (isEscapedModel()) {
    return this;
  }
 else {
    return (KaleoCondition)Proxy.newProxyInstance(KaleoCondition.class.getClassLoader(),new Class[]{KaleoCondition.class},new AutoEscapeBeanHandler(this));
  }
}
