{
  if ((producerKey != _producerKey) || ((producerKey != null) && !producerKey.equals(_producerKey))) {
    _producerKey=producerKey;
    if (_originalProducerKey == null) {
      _originalProducerKey=producerKey;
    }
  }
}
