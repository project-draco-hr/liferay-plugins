{
  if (isEscapedModel()) {
    return this;
  }
 else {
    return (HRAssetDefinition)Proxy.newProxyInstance(HRAssetDefinition.class.getClassLoader(),new Class[]{HRAssetDefinition.class},new AutoEscapeBeanHandler(this));
  }
}
