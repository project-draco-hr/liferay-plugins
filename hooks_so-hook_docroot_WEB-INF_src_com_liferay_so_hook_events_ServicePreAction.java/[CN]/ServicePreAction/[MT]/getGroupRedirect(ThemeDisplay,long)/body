{
  List<Layout> layouts=LayoutLocalServiceUtil.getLayouts(groupId,false,LayoutConstants.DEFAULT_PARENT_LAYOUT_ID,0,1);
  if (layouts.size() > 0) {
    Layout layout=layouts.get(0);
    return PortalUtil.getLayoutURL(layout,themeDisplay);
  }
  return null;
}
