{
  if (isEscapedModel()) {
    return this;
  }
 else {
    return (KaleoNotification)Proxy.newProxyInstance(KaleoNotification.class.getClassLoader(),new Class[]{KaleoNotification.class},new AutoEscapeBeanHandler(this));
  }
}
