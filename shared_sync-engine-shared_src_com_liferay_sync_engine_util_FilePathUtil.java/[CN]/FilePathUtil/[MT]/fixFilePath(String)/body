{
  if (filePath == null) {
    return "";
  }
  filePath=filePath.replace("\\","/");
  if (filePath.endsWith("/")) {
    filePath=filePath.substring(0,filePath.length() - 1);
  }
  return filePath;
}
