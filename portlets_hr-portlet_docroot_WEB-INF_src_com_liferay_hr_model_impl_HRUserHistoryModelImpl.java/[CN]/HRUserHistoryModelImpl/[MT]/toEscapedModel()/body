{
  if (isEscapedModel()) {
    return (HRUserHistory)this;
  }
 else {
    return (HRUserHistory)Proxy.newProxyInstance(_classLoader,_escapedModelProxyInterfaces,new AutoEscapeBeanHandler(this));
  }
}
