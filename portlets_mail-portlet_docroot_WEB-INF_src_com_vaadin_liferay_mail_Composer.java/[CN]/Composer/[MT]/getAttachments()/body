{
  List<MailFile> a=new ArrayList<MailFile>();
  for (  AttachmentLayout attachment : attachments) {
    if (attachment.hasFile()) {
      MailFile mf=new MailFile(attachment.getFile(),attachment.getFilename(),attachment.getSize());
      a.add(mf);
    }
  }
  return a;
}
