{
  List<BaseModel<?>> models=new ArrayList<BaseModel<?>>();
  models.addAll(UserFinderUtil.findByC_FN_MN_LN_SN_EA_S(companyId,fullNames,fullNames,fullNames,fullNames,emailAddresses,0,null,andOperator,start,end,new UserLastNameComparator(true)));
  if (models.size() < (end - start)) {
    int count=UserFinderUtil.countByC_FN_MN_LN_SN_EA_S(companyId,fullNames,fullNames,fullNames,fullNames,emailAddresses,0,null,andOperator);
    start-=count;
    end-=count;
    models.addAll(findByU_FN_EA(userId,fullNames,emailAddresses,andOperator,start,end));
  }
  return models;
}
